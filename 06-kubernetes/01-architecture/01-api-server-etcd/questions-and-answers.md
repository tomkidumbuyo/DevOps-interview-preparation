# Kubernetes API server and etcd — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### KUBERNETES-API-SERVER-AND-ETCD-JN-01

- [ ] **Question:** What is API groups/versions, and why does it matter in Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — API groups/versions](README.md#api-groupsversions)

**Answer:** **Definition.** Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. **Mechanism.** The API server resolves group, version and resource from the URL, decodes and validates that schema, defaults fields and converts the object to an internal representation. Before persistence it converts to the configured storage version; reads reverse the conversion for the requested served version. CRDs can use conversion webhooks when schemas differ materially. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Discovery endpoints and CRD/APIService status show what is served; `status.storedVersions` and deprecation metrics reveal migration work. Removing a served version before clients/manifests/controllers migrate causes request failure, while leaving old stored objects can break later conversion or rollback plans. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which API groups/versions matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Kubernetes API server and etcd journey rather than an administrative green status alone.

### KUBERNETES-API-SERVER-AND-ETCD-JN-02

- [ ] **Question:** What is Request path, and why does it matter in Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Definition.** The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. **Mechanism.** After TLS termination and request parsing, authentication establishes user and groups; authorization evaluates the verb/resource/name/namespace; mutating admission can change the object; schema defaulting and validation establish a valid representation; validating admission enforces final policy; the registry layer applies concurrency and persistence through etcd. Audit stages can record receipt, response start and completion. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** HTTP status, audit events, admission messages, API latency histograms and etcd metrics localize failure. Webhook timeouts or broad failure-closed policies can block unrelated writes, while failure-open can bypass policy, so webhook scope, timeout and availability are production design decisions. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Request path matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Kubernetes API server and etcd journey rather than an administrative green status alone.

### KUBERNETES-API-SERVER-AND-ETCD-JN-03

- [ ] **Question:** What is Watch, and why does it matter in Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — Watch](README.md#watch)

**Answer:** **Definition.** A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. **Mechanism.** A client lists objects and records the returned `resourceVersion`, starts a watch from that revision, then processes ADDED, MODIFIED, DELETED and BOOKMARK events. Client libraries relist or resume after disconnect. etcd compaction eventually removes old revisions, so a client that falls too far behind receives an expired-resource response and must rebuild its cache. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Watch count, event rate, cache/list latency and client relist loops reveal health. Slow consumers, oversized objects, very broad watches and synchronized relists can overload the API server and etcd even when ordinary point reads still succeed. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Watch matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Kubernetes API server and etcd journey rather than an administrative green status alone.

### KUBERNETES-API-SERVER-AND-ETCD-JN-04

- [ ] **Question:** What is ResourceVersion, and why does it matter in Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — ResourceVersion](README.md#resourceversion)

**Answer:** **Definition.** `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. **Mechanism.** The API server returns the token on reads and lists. A conditional update includes the last observed value; if another writer changed the object, optimistic concurrency rejects the stale update. Watches use a collection revision to request later events, subject to cache and compaction semantics. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Conflicts are normal evidence of concurrent writers and should trigger read-modify-retry with bounded logic, not forced overwrite. Expired versions require relist; copying tokens between unrelated objects or treating them as timestamps leads to incorrect ordering assumptions. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which ResourceVersion matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Kubernetes API server and etcd journey rather than an administrative green status alone.

### KUBERNETES-API-SERVER-AND-ETCD-JN-05

- [ ] **Question:** What is Server-side apply, and why does it matter in Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — Server-side apply](README.md#server-side-apply)

**Answer:** **Definition.** Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. **Mechanism.** A client sends an apply patch with a field-manager name. The API server compares the submitted field set with `managedFields`, merges associative structures using the schema, removes fields that manager previously owned but now omitted and rejects conflicting changes unless ownership is deliberately forced. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Inspect `metadata.managedFields` and conflict responses to find competing controllers or tools. Reusing one manager name across unrelated automation hides ownership; forcing conflicts can steal fields from an active controller and cause an endless reconciliation fight. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Server-side apply matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Kubernetes API server and etcd journey rather than an administrative green status alone.

### KUBERNETES-API-SERVER-AND-ETCD-JN-06

- [ ] **Question:** What is etcd quorum, and why does it matter in Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — etcd quorum](README.md#etcd-quorum)

**Answer:** **Definition.** An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. **Mechanism.** The leader appends a log entry and replicates it to followers; after a majority acknowledges durable log state, the entry commits and the state machine applies it. Leader election also requires majority communication. Adding members increases fault tolerance only at particular odd sizes and increases replication latency and operational complexity. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Endpoint status, leader changes, proposal latency/failures and peer network/disk latency show health. Spreading members across unreliable high-latency links, losing quorum during maintenance or restoring members independently can threaten availability or consistency; recovery follows the documented snapshot/member procedure. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which etcd quorum matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Kubernetes API server and etcd journey rather than an administrative green status alone.

### KUBERNETES-API-SERVER-AND-ETCD-JN-07

- [ ] **Question:** What is Compaction, and why does it matter in Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — Compaction](README.md#compaction)

**Answer:** **Definition.** etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. **Mechanism.** etcd stores multiple key revisions. Periodic or explicit compaction marks older revisions unavailable; later backend maintenance can reclaim physical space. Kubernetes clients that receive an expired revision relist current objects and restart their watch from the new list revision. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Revision age, database size, watch expiry/relist rate and API list load must be considered together. Compacting does not immediately shrink the backend file, and overly slow clients can cause expensive relist storms after their revision disappears. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Compaction matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Kubernetes API server and etcd journey rather than an administrative green status alone.

### KUBERNETES-API-SERVER-AND-ETCD-JN-08

- [ ] **Code:** **Question:** What does `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table` help you verify for Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Kubernetes API server and etcd concept is Defragmentation: etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Kubernetes API server and etcd rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### KUBERNETES-API-SERVER-AND-ETCD-JN-09

- [ ] **Code:** **Question:** What does `kubectl get --raw '/readyz?verbose'` help you verify for Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl get --raw '/readyz?verbose'` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant Kubernetes API server and etcd concept is Encryption at rest: Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Kubernetes API server and etcd rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### KUBERNETES-API-SERVER-AND-ETCD-JN-10

- [ ] **Code:** **Question:** What does `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` help you verify for Kubernetes API server and etcd?
> **Covered in:** [Kubernetes API server and etcd — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant Kubernetes API server and etcd concept is API priority/fairness: API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Kubernetes API server and etcd rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### KUBERNETES-API-SERVER-AND-ETCD-JP-01

- [ ] **Code:** **Question:** A basic API groups/versions check fails. What would you do first using the CLI?
> **Covered in:** [Kubernetes API server and etcd — API groups/versions](README.md#api-groupsversions)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get --raw '/readyz?verbose'`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-02

- [ ] **Question:** A basic Request path check fails. What would you do first?
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get --raw '/metrics' | rg 'apiserver|etcd'`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-03

- [ ] **Question:** A basic Watch check fails. What would you do first?
> **Covered in:** [Kubernetes API server and etcd — Watch](README.md#watch)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl api-resources`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-04

- [ ] **Code:** **Question:** A basic ResourceVersion check fails. What would you do first using the CLI?
> **Covered in:** [Kubernetes API server and etcd — ResourceVersion](README.md#resourceversion)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-05

- [ ] **Question:** A basic Server-side apply check fails. What would you do first?
> **Covered in:** [Kubernetes API server and etcd — Server-side apply](README.md#server-side-apply)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get --raw '/readyz?verbose'`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-06

- [ ] **Question:** A basic etcd quorum check fails. What would you do first?
> **Covered in:** [Kubernetes API server and etcd — etcd quorum](README.md#etcd-quorum)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get --raw '/metrics' | rg 'apiserver|etcd'`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-07

- [ ] **Code:** **Question:** A basic Compaction check fails. What would you do first using the CLI?
> **Covered in:** [Kubernetes API server and etcd — Compaction](README.md#compaction)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl api-resources`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-08

- [ ] **Question:** A basic Defragmentation check fails. What would you do first?
> **Covered in:** [Kubernetes API server and etcd — Defragmentation](README.md#defragmentation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-09

- [ ] **Question:** A basic Encryption at rest check fails. What would you do first?
> **Covered in:** [Kubernetes API server and etcd — Encryption at rest](README.md#encryption-at-rest)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get --raw '/readyz?verbose'`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### KUBERNETES-API-SERVER-AND-ETCD-JP-10

- [ ] **Code:** **Question:** A basic API priority/fairness check fails. What would you do first using the CLI?
> **Covered in:** [Kubernetes API server and etcd — API priority/fairness](README.md#api-priorityfairness)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get --raw '/metrics' | rg 'apiserver|etcd'`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Kubernetes API server and etcd client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### KUBERNETES-API-SERVER-AND-ETCD-MN-01

- [ ] **Question:** Compare API groups/versions with Request path. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Definitions.** API groups/versions: Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. Request path: The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-02

- [ ] **Question:** Compare Request path with Watch. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Definitions.** Request path: The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. Watch: A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-03

- [ ] **Question:** Compare Watch with ResourceVersion. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — ResourceVersion](README.md#resourceversion)

**Answer:** **Definitions.** Watch: A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. ResourceVersion: `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-04

- [ ] **Configuration review:** **Question:** Compare ResourceVersion with Server-side apply. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — Server-side apply](README.md#server-side-apply)

**Answer:** **Definitions.** ResourceVersion: `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. Server-side apply: Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-05

- [ ] **Question:** Compare Server-side apply with etcd quorum. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — Server-side apply](README.md#server-side-apply)

**Answer:** **Definitions.** Server-side apply: Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. etcd quorum: An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-06

- [ ] **Question:** Compare etcd quorum with Compaction. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — etcd quorum](README.md#etcd-quorum)

**Answer:** **Definitions.** etcd quorum: An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. Compaction: etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-07

- [ ] **Configuration review:** **Question:** Compare Compaction with Defragmentation. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — Defragmentation](README.md#defragmentation)

**Answer:** **Definitions.** Compaction: etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. Defragmentation: etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-08

- [ ] **Question:** Compare Defragmentation with Encryption at rest. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — Encryption at rest](README.md#encryption-at-rest)

**Answer:** **Definitions.** Defragmentation: etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. Encryption at rest: Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-09

- [ ] **Question:** Compare Encryption at rest with API priority/fairness. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — Encryption at rest](README.md#encryption-at-rest)

**Answer:** **Definitions.** Encryption at rest: Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. API priority/fairness: API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### KUBERNETES-API-SERVER-AND-ETCD-MN-10

- [ ] **Configuration review:** **Question:** Compare API priority/fairness with API groups/versions. When would each change your design?
> **Covered in:** [Kubernetes API server and etcd — API groups/versions](README.md#api-groupsversions)

**Answer:** **Definitions.** API priority/fairness: API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. API groups/versions: Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. **Mechanism.** In Kubernetes API server and etcd, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Kubernetes API server and etcd design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### KUBERNETES-API-SERVER-AND-ETCD-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around API groups/versions; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — API groups/versions](README.md#api-groupsversions)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get --raw '/readyz?verbose'` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-02

- [ ] **Question:** Production is degraded around Request path; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Watch; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — Watch](README.md#watch)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl api-resources` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-04

- [ ] **Question:** Production is degraded around ResourceVersion; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — ResourceVersion](README.md#resourceversion)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Server-side apply; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — Server-side apply](README.md#server-side-apply)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get --raw '/readyz?verbose'` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-06

- [ ] **Question:** Production is degraded around etcd quorum; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — etcd quorum](README.md#etcd-quorum)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Compaction; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — Compaction](README.md#compaction)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl api-resources` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-08

- [ ] **Question:** Production is degraded around Defragmentation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — Defragmentation](README.md#defragmentation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Encryption at rest; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — Encryption at rest](README.md#encryption-at-rest)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get --raw '/readyz?verbose'` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### KUBERNETES-API-SERVER-AND-ETCD-MP-10

- [ ] **Question:** Production is degraded around API priority/fairness; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes API server and etcd — API priority/fairness](README.md#api-priorityfairness)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Kubernetes API server and etcd source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### KUBERNETES-API-SERVER-AND-ETCD-SN-01

- [ ] **Question:** Design a production Kubernetes API server and etcd capability where API groups/versions, ResourceVersion and Compaction are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — API groups/versions](README.md#api-groupsversions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** API groups/versions: Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. ResourceVersion: `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. Compaction: etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Kubernetes API server and etcd capability where Request path, Server-side apply and Defragmentation are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Request path: The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. Server-side apply: Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. Defragmentation: etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-03

- [ ] **Question:** Design a production Kubernetes API server and etcd capability where Watch, etcd quorum and Encryption at rest are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — Encryption at rest](README.md#encryption-at-rest)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Watch: A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. etcd quorum: An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. Encryption at rest: Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Kubernetes API server and etcd capability where ResourceVersion, Compaction and API priority/fairness are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — API priority/fairness](README.md#api-priorityfairness)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** ResourceVersion: `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. Compaction: etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. API priority/fairness: API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-05

- [ ] **Question:** Design a production Kubernetes API server and etcd capability where Server-side apply, Defragmentation and API groups/versions are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — Server-side apply](README.md#server-side-apply)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Server-side apply: Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. Defragmentation: etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. API groups/versions: Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Kubernetes API server and etcd capability where etcd quorum, Encryption at rest and Request path are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** etcd quorum: An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. Encryption at rest: Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. Request path: The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-07

- [ ] **Question:** Design a production Kubernetes API server and etcd capability where Compaction, API priority/fairness and Watch are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — Watch](README.md#watch)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Compaction: etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. API priority/fairness: API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. Watch: A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Kubernetes API server and etcd capability where Defragmentation, API groups/versions and ResourceVersion are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — API groups/versions](README.md#api-groupsversions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Defragmentation: etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. API groups/versions: Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. ResourceVersion: `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-09

- [ ] **Question:** Design a production Kubernetes API server and etcd capability where Encryption at rest, Request path and Server-side apply are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Encryption at rest: Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. Request path: The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. Server-side apply: Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### KUBERNETES-API-SERVER-AND-ETCD-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Kubernetes API server and etcd capability where API priority/fairness, Watch and etcd quorum are first-class requirements.
> **Covered in:** [Kubernetes API server and etcd — API priority/fairness](README.md#api-priorityfairness)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** API priority/fairness: API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. Watch: A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. etcd quorum: An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Kubernetes API server and etcd options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### KUBERNETES-API-SERVER-AND-ETCD-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving API groups/versions. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — API groups/versions](README.md#api-groupsversions)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get --raw '/readyz?verbose'` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Kubernetes API groups organize related resource kinds and versions define the schema contract served to clients. A served version can differ from the storage version used in etcd, with conversion translating between them. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Request path. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — Request path](README.md#request-path)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The Kubernetes API request path is the ordered chain that turns an HTTPS request into an authorized, admitted and persisted object or a precise rejection. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Watch. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — Watch](README.md#watch)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl api-resources` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Kubernetes watch is a long-lived API stream of resource changes after a known revision. Controllers use list-then-watch to maintain a local cache without repeatedly listing every object. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving ResourceVersion. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — ResourceVersion](README.md#resourceversion)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** `resourceVersion` is Kubernetes' opaque concurrency and change-stream token for an observed object or collection state. It is not a wall-clock timestamp, generation number or value applications should numerically interpret. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Server-side apply. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — Server-side apply](README.md#server-side-apply)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get --raw '/readyz?verbose'` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Server-side apply is Kubernetes' declarative merge mechanism that records which field manager owns each field and reports conflicts when another manager tries to change owned intent. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving etcd quorum. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — etcd quorum](README.md#etcd-quorum)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** An etcd quorum is the majority of voting members required for Raft to elect a leader and commit writes. In a three-member cluster, two members form quorum; losing two preserves data on disk but stops consistent progress. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Compaction. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — Compaction](README.md#compaction)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl api-resources` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** etcd compaction discards old MVCC history before a selected revision while retaining current key values. It bounds historical revision growth but means a watch cannot resume from a compacted revision. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Defragmentation. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — Defragmentation](README.md#defragmentation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `ETCDCTL_API=3 etcdctl endpoint status --cluster -w table` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** etcd defragmentation rewrites the backend database to release free pages left after deletes and compaction. Compaction removes logical history; defragmentation is the separate physical space-reclamation step. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Encryption at rest. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — Encryption at rest](README.md#encryption-at-rest)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get --raw '/readyz?verbose'` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Kubernetes encryption at rest transforms selected API resource values before the API server stores them in etcd. It protects raw etcd data or snapshots from directly revealing those values but does not prevent an authorized API read. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### KUBERNETES-API-SERVER-AND-ETCD-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving API priority/fairness. How do you lead it end to end?
> **Covered in:** [Kubernetes API server and etcd — API priority/fairness](README.md#api-priorityfairness)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get --raw '/metrics' | rg 'apiserver|etcd'` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** API Priority and Fairness classifies Kubernetes API requests into priority levels and flow queues so a noisy client cannot consume every API-server concurrency slot. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Kubernetes API server and etcd from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Architecture](../README.md) · [Study note](README.md) · [Next: Scheduler, controllers and kubelet →](../02-scheduler-controllers-kubelet/README.md)

<!-- reading-navigation:end -->
