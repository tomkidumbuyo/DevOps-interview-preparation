# CSI, snapshots and Kubernetes backup — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-01

- [ ] **Question:** What is CSI controller, and why does it matter in CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI controller](README.md#csi-controller)

**Answer:** **Definition.** External components provision/delete/attach/snapshot/resize volumes. **Mechanism.** The mechanism receives configuration or runtime input, validates preconditions, changes or derives state, and exposes status to the next component. Synchronous parts return a direct outcome; asynchronous parts need durable status, reconciliation and idempotency because the original caller may disappear. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence shows the expected state transition and its owner, timestamp and reason. Invalid input, stale state, conflicting writers, hidden limits and dependencies that partially complete are the most useful failure categories to distinguish. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which CSI controller matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original CSI, snapshots and Kubernetes backup journey rather than an administrative green status alone.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-02

- [ ] **Question:** What is CSI node plugin, and why does it matter in CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI node plugin](README.md#csi-node-plugin)

**Answer:** **Definition.** Stages/publishes/mounts volumes on each node. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which CSI node plugin matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original CSI, snapshots and Kubernetes backup journey rather than an administrative green status alone.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-03

- [ ] **Question:** What is CSIDriver/CSINode, and why does it matter in CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSIDriver/CSINode](README.md#csidrivercsinode)

**Answer:** **Definition.** Advertise driver lifecycle and node topology/capability. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which CSIDriver/CSINode matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original CSI, snapshots and Kubernetes backup journey rather than an administrative green status alone.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-04

- [ ] **Question:** What is VolumeAttachment, and why does it matter in CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeAttachment](README.md#volumeattachment)

**Answer:** **Definition.** Records controller attach intent/status for attachable storage. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which VolumeAttachment matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original CSI, snapshots and Kubernetes backup journey rather than an administrative green status alone.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-05

- [ ] **Question:** What is VolumeSnapshotClass, and why does it matter in CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshotClass](README.md#volumesnapshotclass)

**Answer:** **Definition.** Selects snapshot driver, deletion policy and parameters. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which VolumeSnapshotClass matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original CSI, snapshots and Kubernetes backup journey rather than an administrative green status alone.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-06

- [ ] **Question:** What is VolumeSnapshot/content, and why does it matter in CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshot/content](README.md#volumesnapshotcontent)

**Answer:** **Definition.** Namespaced request and cluster snapshot handle mirror PVC/PV model. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which VolumeSnapshot/content matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original CSI, snapshots and Kubernetes backup journey rather than an administrative green status alone.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-07

- [ ] **Question:** What is Crash consistency, and why does it matter in CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Definition.** Block snapshot alone may not capture application transaction consistency. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Crash consistency matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original CSI, snapshots and Kubernetes backup journey rather than an administrative green status alone.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-08

- [ ] **Code:** **Question:** What does `velero backup get && velero restore get` help you verify for CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `velero backup get && velero restore get` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant CSI, snapshots and Kubernetes backup concept is Velero/resource backup: Captures API objects and integrates volume backup methods under plugin limits. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a CSI, snapshots and Kubernetes backup rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-09

- [ ] **Code:** **Question:** What does `kubectl get csidriver,csinode,volumeattachment` help you verify for CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeAttachment](README.md#volumeattachment)

**Answer:** **Purpose.** `kubectl get csidriver,csinode,volumeattachment` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant CSI, snapshots and Kubernetes backup concept is Restore ordering: CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a CSI, snapshots and Kubernetes backup rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JN-10

- [ ] **Code:** **Question:** What does `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` help you verify for CSI, snapshots and Kubernetes backup?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshotClass](README.md#volumesnapshotclass)

**Answer:** **Purpose.** `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant CSI, snapshots and Kubernetes backup concept is Restore validation: Application queries/checksums and measured RPO/RTO prove recovery. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a CSI, snapshots and Kubernetes backup rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-01

- [ ] **Code:** **Question:** A basic CSI controller check fails. What would you do first using the CLI?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI controller](README.md#csi-controller)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get csidriver,csinode,volumeattachment`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** External components provision/delete/attach/snapshot/resize volumes. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-02

- [ ] **Question:** A basic CSI node plugin check fails. What would you do first?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI node plugin](README.md#csi-node-plugin)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Stages/publishes/mounts volumes on each node. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-03

- [ ] **Question:** A basic CSIDriver/CSINode check fails. What would you do first?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSIDriver/CSINode](README.md#csidrivercsinode)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n kube-system -l app=csi-controller --all-containers`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Advertise driver lifecycle and node topology/capability. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-04

- [ ] **Code:** **Question:** A basic VolumeAttachment check fails. What would you do first using the CLI?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeAttachment](README.md#volumeattachment)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `velero backup get && velero restore get`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Records controller attach intent/status for attachable storage. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-05

- [ ] **Question:** A basic VolumeSnapshotClass check fails. What would you do first?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshotClass](README.md#volumesnapshotclass)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get csidriver,csinode,volumeattachment`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Selects snapshot driver, deletion policy and parameters. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-06

- [ ] **Question:** A basic VolumeSnapshot/content check fails. What would you do first?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshot/content](README.md#volumesnapshotcontent)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Namespaced request and cluster snapshot handle mirror PVC/PV model. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-07

- [ ] **Code:** **Question:** A basic Crash consistency check fails. What would you do first using the CLI?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n kube-system -l app=csi-controller --all-containers`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Block snapshot alone may not capture application transaction consistency. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-08

- [ ] **Question:** A basic Velero/resource backup check fails. What would you do first?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Velero/resource backup](README.md#veleroresource-backup)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `velero backup get && velero restore get`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Captures API objects and integrates volume backup methods under plugin limits. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-09

- [ ] **Question:** A basic Restore ordering check fails. What would you do first?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore ordering](README.md#restore-ordering)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get csidriver,csinode,volumeattachment`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-JP-10

- [ ] **Code:** **Question:** A basic Restore validation check fails. What would you do first using the CLI?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore validation](README.md#restore-validation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Application queries/checksums and measured RPO/RTO prove recovery. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original CSI, snapshots and Kubernetes backup client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-01

- [ ] **Question:** Compare CSI controller with CSI node plugin. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI node plugin](README.md#csi-node-plugin)

**Answer:** **Definitions.** CSI controller: External components provision/delete/attach/snapshot/resize volumes. CSI node plugin: Stages/publishes/mounts volumes on each node. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-02

- [ ] **Question:** Compare CSI node plugin with CSIDriver/CSINode. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI node plugin](README.md#csi-node-plugin)

**Answer:** **Definitions.** CSI node plugin: Stages/publishes/mounts volumes on each node. CSIDriver/CSINode: Advertise driver lifecycle and node topology/capability. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-03

- [ ] **Question:** Compare CSIDriver/CSINode with VolumeAttachment. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSIDriver/CSINode](README.md#csidrivercsinode)

**Answer:** **Definitions.** CSIDriver/CSINode: Advertise driver lifecycle and node topology/capability. VolumeAttachment: Records controller attach intent/status for attachable storage. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-04

- [ ] **Configuration review:** **Question:** Compare VolumeAttachment with VolumeSnapshotClass. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeAttachment](README.md#volumeattachment)

**Answer:** **Definitions.** VolumeAttachment: Records controller attach intent/status for attachable storage. VolumeSnapshotClass: Selects snapshot driver, deletion policy and parameters. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-05

- [ ] **Question:** Compare VolumeSnapshotClass with VolumeSnapshot/content. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshotClass](README.md#volumesnapshotclass)

**Answer:** **Definitions.** VolumeSnapshotClass: Selects snapshot driver, deletion policy and parameters. VolumeSnapshot/content: Namespaced request and cluster snapshot handle mirror PVC/PV model. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-06

- [ ] **Question:** Compare VolumeSnapshot/content with Crash consistency. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Definitions.** VolumeSnapshot/content: Namespaced request and cluster snapshot handle mirror PVC/PV model. Crash consistency: Block snapshot alone may not capture application transaction consistency. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-07

- [ ] **Configuration review:** **Question:** Compare Crash consistency with Velero/resource backup. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Velero/resource backup](README.md#veleroresource-backup)

**Answer:** **Definitions.** Crash consistency: Block snapshot alone may not capture application transaction consistency. Velero/resource backup: Captures API objects and integrates volume backup methods under plugin limits. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-08

- [ ] **Question:** Compare Velero/resource backup with Restore ordering. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Velero/resource backup](README.md#veleroresource-backup)

**Answer:** **Definitions.** Velero/resource backup: Captures API objects and integrates volume backup methods under plugin limits. Restore ordering: CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-09

- [ ] **Question:** Compare Restore ordering with Restore validation. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore ordering](README.md#restore-ordering)

**Answer:** **Definitions.** Restore ordering: CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. Restore validation: Application queries/checksums and measured RPO/RTO prove recovery. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MN-10

- [ ] **Configuration review:** **Question:** Compare Restore validation with CSI controller. When would each change your design?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore validation](README.md#restore-validation)

**Answer:** **Definitions.** Restore validation: Application queries/checksums and measured RPO/RTO prove recovery. CSI controller: External components provision/delete/attach/snapshot/resize volumes. **Mechanism.** In CSI, snapshots and Kubernetes backup, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production CSI, snapshots and Kubernetes backup design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around CSI controller; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI controller](README.md#csi-controller)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get csidriver,csinode,volumeattachment` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** External components provision/delete/attach/snapshot/resize volumes. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-02

- [ ] **Question:** Production is degraded around CSI node plugin; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI node plugin](README.md#csi-node-plugin)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Stages/publishes/mounts volumes on each node. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around CSIDriver/CSINode; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSIDriver/CSINode](README.md#csidrivercsinode)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n kube-system -l app=csi-controller --all-containers` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Advertise driver lifecycle and node topology/capability. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-04

- [ ] **Question:** Production is degraded around VolumeAttachment; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeAttachment](README.md#volumeattachment)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `velero backup get && velero restore get` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Records controller attach intent/status for attachable storage. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around VolumeSnapshotClass; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshotClass](README.md#volumesnapshotclass)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get csidriver,csinode,volumeattachment` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Selects snapshot driver, deletion policy and parameters. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-06

- [ ] **Question:** Production is degraded around VolumeSnapshot/content; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshot/content](README.md#volumesnapshotcontent)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Namespaced request and cluster snapshot handle mirror PVC/PV model. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Crash consistency; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n kube-system -l app=csi-controller --all-containers` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Block snapshot alone may not capture application transaction consistency. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-08

- [ ] **Question:** Production is degraded around Velero/resource backup; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Velero/resource backup](README.md#veleroresource-backup)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `velero backup get && velero restore get` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Captures API objects and integrates volume backup methods under plugin limits. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Restore ordering; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore ordering](README.md#restore-ordering)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get csidriver,csinode,volumeattachment` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-MP-10

- [ ] **Question:** Production is degraded around Restore validation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore validation](README.md#restore-validation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Application queries/checksums and measured RPO/RTO prove recovery. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the CSI, snapshots and Kubernetes backup source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-01

- [ ] **Question:** Design a production CSI, snapshots and Kubernetes backup capability where CSI controller, VolumeAttachment and Crash consistency are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** CSI controller: External components provision/delete/attach/snapshot/resize volumes. VolumeAttachment: Records controller attach intent/status for attachable storage. Crash consistency: Block snapshot alone may not capture application transaction consistency. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production CSI, snapshots and Kubernetes backup capability where CSI node plugin, VolumeSnapshotClass and Velero/resource backup are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI node plugin](README.md#csi-node-plugin)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** CSI node plugin: Stages/publishes/mounts volumes on each node. VolumeSnapshotClass: Selects snapshot driver, deletion policy and parameters. Velero/resource backup: Captures API objects and integrates volume backup methods under plugin limits. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-03

- [ ] **Question:** Design a production CSI, snapshots and Kubernetes backup capability where CSIDriver/CSINode, VolumeSnapshot/content and Restore ordering are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore ordering](README.md#restore-ordering)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** CSIDriver/CSINode: Advertise driver lifecycle and node topology/capability. VolumeSnapshot/content: Namespaced request and cluster snapshot handle mirror PVC/PV model. Restore ordering: CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production CSI, snapshots and Kubernetes backup capability where VolumeAttachment, Crash consistency and Restore validation are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** VolumeAttachment: Records controller attach intent/status for attachable storage. Crash consistency: Block snapshot alone may not capture application transaction consistency. Restore validation: Application queries/checksums and measured RPO/RTO prove recovery. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-05

- [ ] **Question:** Design a production CSI, snapshots and Kubernetes backup capability where VolumeSnapshotClass, Velero/resource backup and CSI controller are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Velero/resource backup](README.md#veleroresource-backup)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** VolumeSnapshotClass: Selects snapshot driver, deletion policy and parameters. Velero/resource backup: Captures API objects and integrates volume backup methods under plugin limits. CSI controller: External components provision/delete/attach/snapshot/resize volumes. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production CSI, snapshots and Kubernetes backup capability where VolumeSnapshot/content, Restore ordering and CSI node plugin are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore ordering](README.md#restore-ordering)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** VolumeSnapshot/content: Namespaced request and cluster snapshot handle mirror PVC/PV model. Restore ordering: CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. CSI node plugin: Stages/publishes/mounts volumes on each node. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-07

- [ ] **Question:** Design a production CSI, snapshots and Kubernetes backup capability where Crash consistency, Restore validation and CSIDriver/CSINode are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Crash consistency: Block snapshot alone may not capture application transaction consistency. Restore validation: Application queries/checksums and measured RPO/RTO prove recovery. CSIDriver/CSINode: Advertise driver lifecycle and node topology/capability. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production CSI, snapshots and Kubernetes backup capability where Velero/resource backup, CSI controller and VolumeAttachment are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Velero/resource backup](README.md#veleroresource-backup)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Velero/resource backup: Captures API objects and integrates volume backup methods under plugin limits. CSI controller: External components provision/delete/attach/snapshot/resize volumes. VolumeAttachment: Records controller attach intent/status for attachable storage. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-09

- [ ] **Question:** Design a production CSI, snapshots and Kubernetes backup capability where Restore ordering, CSI node plugin and VolumeSnapshotClass are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore ordering](README.md#restore-ordering)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Restore ordering: CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. CSI node plugin: Stages/publishes/mounts volumes on each node. VolumeSnapshotClass: Selects snapshot driver, deletion policy and parameters. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production CSI, snapshots and Kubernetes backup capability where Restore validation, CSIDriver/CSINode and VolumeSnapshot/content are first-class requirements.
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore validation](README.md#restore-validation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Restore validation: Application queries/checksums and measured RPO/RTO prove recovery. CSIDriver/CSINode: Advertise driver lifecycle and node topology/capability. VolumeSnapshot/content: Namespaced request and cluster snapshot handle mirror PVC/PV model. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated CSI, snapshots and Kubernetes backup options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving CSI controller. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI controller](README.md#csi-controller)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get csidriver,csinode,volumeattachment` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** External components provision/delete/attach/snapshot/resize volumes. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving CSI node plugin. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSI node plugin](README.md#csi-node-plugin)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Stages/publishes/mounts volumes on each node. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving CSIDriver/CSINode. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — CSIDriver/CSINode](README.md#csidrivercsinode)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n kube-system -l app=csi-controller --all-containers` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Advertise driver lifecycle and node topology/capability. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving VolumeAttachment. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeAttachment](README.md#volumeattachment)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `velero backup get && velero restore get` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Records controller attach intent/status for attachable storage. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving VolumeSnapshotClass. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshotClass](README.md#volumesnapshotclass)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get csidriver,csinode,volumeattachment` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Selects snapshot driver, deletion policy and parameters. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving VolumeSnapshot/content. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — VolumeSnapshot/content](README.md#volumesnapshotcontent)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Namespaced request and cluster snapshot handle mirror PVC/PV model. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Crash consistency. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Crash consistency](README.md#crash-consistency)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n kube-system -l app=csi-controller --all-containers` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Block snapshot alone may not capture application transaction consistency. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Velero/resource backup. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Velero/resource backup](README.md#veleroresource-backup)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `velero backup get && velero restore get` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Captures API objects and integrates volume backup methods under plugin limits. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Restore ordering. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore ordering](README.md#restore-ordering)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get csidriver,csinode,volumeattachment` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** CRDs/operators/secrets/classes/data/workloads and identities must be reconstructed coherently. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CSI-SNAPSHOTS-AND-KUBERNETES-BACKUP-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Restore validation. How do you lead it end to end?
> **Covered in:** [CSI, snapshots and Kubernetes backup — Restore validation](README.md#restore-validation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get volumesnapshotclass,volumesnapshot,volumesnapshotcontent -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Application queries/checksums and measured RPO/RTO prove recovery. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify CSI, snapshots and Kubernetes backup from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: PersistentVolumes, PVCs and StorageClasses](../01-pv-pvc-storageclass/README.md) · [Study note](README.md) · [Next: Security →](../../05-security/README.md)

<!-- reading-navigation:end -->
