# Observability fundamentals — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### OBSERVABILITY-FUNDAMENTALS-JN-01

- [ ] **Question:** What is Metrics, and why does it matter in Observability fundamentals?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Definition.** Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Metrics matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Observability fundamentals journey rather than an administrative green status alone.

### OBSERVABILITY-FUNDAMENTALS-JN-02

- [ ] **Question:** What is Logs, and why does it matter in Observability fundamentals?
> **Covered in:** [Observability fundamentals — Logs](README.md#logs)

**Answer:** **Definition.** Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Logs matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Observability fundamentals journey rather than an administrative green status alone.

### OBSERVABILITY-FUNDAMENTALS-JN-03

- [ ] **Question:** What is Traces, and why does it matter in Observability fundamentals?
> **Covered in:** [Observability fundamentals — Traces](README.md#traces)

**Answer:** **Definition.** A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Traces matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Observability fundamentals journey rather than an administrative green status alone.

### OBSERVABILITY-FUNDAMENTALS-JN-04

- [ ] **Question:** What is Events, and why does it matter in Observability fundamentals?
> **Covered in:** [Observability fundamentals — Events](README.md#events)

**Answer:** **Definition.** Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Events matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Observability fundamentals journey rather than an administrative green status alone.

### OBSERVABILITY-FUNDAMENTALS-JN-05

- [ ] **Question:** What is Profiles, and why does it matter in Observability fundamentals?
> **Covered in:** [Observability fundamentals — Profiles](README.md#profiles)

**Answer:** **Definition.** Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Profiles matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Observability fundamentals journey rather than an administrative green status alone.

### OBSERVABILITY-FUNDAMENTALS-JN-06

- [ ] **Question:** What is Correlation, and why does it matter in Observability fundamentals?
> **Covered in:** [Observability fundamentals — Correlation](README.md#correlation)

**Answer:** **Definition.** Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Correlation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Observability fundamentals journey rather than an administrative green status alone.

### OBSERVABILITY-FUNDAMENTALS-JN-07

- [ ] **Question:** What is Context propagation, and why does it matter in Observability fundamentals?
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Definition.** Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Context propagation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Observability fundamentals journey rather than an administrative green status alone.

### OBSERVABILITY-FUNDAMENTALS-JN-08

- [ ] **Code:** **Question:** What does `trivy fs .` help you verify for Observability fundamentals?
> **Covered in:** [Observability fundamentals — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `trivy fs .` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Observability fundamentals concept is Structured telemetry: Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Observability fundamentals rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### OBSERVABILITY-FUNDAMENTALS-JN-09

- [ ] **Code:** **Question:** What does `curl -s http://SERVICE/metrics` help you verify for Observability fundamentals?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Purpose.** `curl -s http://SERVICE/metrics` tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. **Mechanism.** The relevant Observability fundamentals concept is Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Observability fundamentals rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### OBSERVABILITY-FUNDAMENTALS-JN-10

- [ ] **Code:** **Question:** What does `promtool check rules rules.yml` help you verify for Observability fundamentals?
> **Covered in:** [Observability fundamentals — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `promtool check rules rules.yml` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Observability fundamentals concept is Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Observability fundamentals rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### OBSERVABILITY-FUNDAMENTALS-JP-01

- [ ] **Code:** **Question:** A basic Metrics check fails. What would you do first using the CLI?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://SERVICE/metrics`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-02

- [ ] **Question:** A basic Logs check fails. What would you do first?
> **Covered in:** [Observability fundamentals — Logs](README.md#logs)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `promtool check rules rules.yml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-03

- [ ] **Question:** A basic Traces check fails. What would you do first?
> **Covered in:** [Observability fundamentals — Traces](README.md#traces)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get events -A --sort-by=.lastTimestamp`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-04

- [ ] **Code:** **Question:** A basic Events check fails. What would you do first using the CLI?
> **Covered in:** [Observability fundamentals — Events](README.md#events)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `trivy fs .`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-05

- [ ] **Question:** A basic Profiles check fails. What would you do first?
> **Covered in:** [Observability fundamentals — Profiles](README.md#profiles)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://SERVICE/metrics`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-06

- [ ] **Question:** A basic Correlation check fails. What would you do first?
> **Covered in:** [Observability fundamentals — Correlation](README.md#correlation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `promtool check rules rules.yml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-07

- [ ] **Code:** **Question:** A basic Context propagation check fails. What would you do first using the CLI?
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get events -A --sort-by=.lastTimestamp`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-08

- [ ] **Question:** A basic Structured telemetry check fails. What would you do first?
> **Covered in:** [Observability fundamentals — Structured telemetry](README.md#structured-telemetry)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `trivy fs .`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-09

- [ ] **Question:** A basic Metrics check fails. What would you do first?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://SERVICE/metrics`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### OBSERVABILITY-FUNDAMENTALS-JP-10

- [ ] **Code:** **Question:** A basic Logs check fails. What would you do first using the CLI?
> **Covered in:** [Observability fundamentals — Logs](README.md#logs)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `promtool check rules rules.yml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Observability fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### OBSERVABILITY-FUNDAMENTALS-MN-01

- [ ] **Question:** Compare Metrics with Logs. When would each change your design?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Definitions.** Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-02

- [ ] **Question:** Compare Logs with Traces. When would each change your design?
> **Covered in:** [Observability fundamentals — Traces](README.md#traces)

**Answer:** **Definitions.** Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Traces: A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-03

- [ ] **Question:** Compare Traces with Events. When would each change your design?
> **Covered in:** [Observability fundamentals — Traces](README.md#traces)

**Answer:** **Definitions.** Traces: A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. Events: Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-04

- [ ] **Configuration review:** **Question:** Compare Events with Profiles. When would each change your design?
> **Covered in:** [Observability fundamentals — Events](README.md#events)

**Answer:** **Definitions.** Events: Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. Profiles: Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-05

- [ ] **Question:** Compare Profiles with Correlation. When would each change your design?
> **Covered in:** [Observability fundamentals — Profiles](README.md#profiles)

**Answer:** **Definitions.** Profiles: Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. Correlation: Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-06

- [ ] **Question:** Compare Correlation with Context propagation. When would each change your design?
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Definitions.** Correlation: Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. Context propagation: Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-07

- [ ] **Configuration review:** **Question:** Compare Context propagation with Structured telemetry. When would each change your design?
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Definitions.** Context propagation: Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. Structured telemetry: Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-08

- [ ] **Question:** Compare Structured telemetry with Metrics. When would each change your design?
> **Covered in:** [Observability fundamentals — Structured telemetry](README.md#structured-telemetry)

**Answer:** **Definitions.** Structured telemetry: Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-09

- [ ] **Question:** Compare Metrics with Logs. When would each change your design?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Definitions.** Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### OBSERVABILITY-FUNDAMENTALS-MN-10

- [ ] **Configuration review:** **Question:** Compare Logs with Metrics. When would each change your design?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Definitions.** Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. **Mechanism.** In Observability fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Observability fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### OBSERVABILITY-FUNDAMENTALS-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Metrics; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://SERVICE/metrics` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-02

- [ ] **Question:** Production is degraded around Logs; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Logs](README.md#logs)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `promtool check rules rules.yml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Traces; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Traces](README.md#traces)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get events -A --sort-by=.lastTimestamp` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-04

- [ ] **Question:** Production is degraded around Events; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Events](README.md#events)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `trivy fs .` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Profiles; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Profiles](README.md#profiles)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://SERVICE/metrics` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-06

- [ ] **Question:** Production is degraded around Correlation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Correlation](README.md#correlation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `promtool check rules rules.yml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Context propagation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get events -A --sort-by=.lastTimestamp` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-08

- [ ] **Question:** Production is degraded around Structured telemetry; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Structured telemetry](README.md#structured-telemetry)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `trivy fs .` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Metrics; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://SERVICE/metrics` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### OBSERVABILITY-FUNDAMENTALS-MP-10

- [ ] **Question:** Production is degraded around Logs; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Observability fundamentals — Logs](README.md#logs)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `promtool check rules rules.yml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Observability fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### OBSERVABILITY-FUNDAMENTALS-SN-01

- [ ] **Question:** Design a production Observability fundamentals capability where Metrics, Events and Context propagation are first-class requirements.
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Events: Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. Context propagation: Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Observability fundamentals capability where Logs, Profiles and Structured telemetry are first-class requirements.
> **Covered in:** [Observability fundamentals — Structured telemetry](README.md#structured-telemetry)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Profiles: Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. Structured telemetry: Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-03

- [ ] **Question:** Design a production Observability fundamentals capability where Traces, Correlation and Metrics are first-class requirements.
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Traces: A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. Correlation: Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Observability fundamentals capability where Events, Context propagation and Logs are first-class requirements.
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Events: Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. Context propagation: Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-05

- [ ] **Question:** Design a production Observability fundamentals capability where Profiles, Structured telemetry and Metrics are first-class requirements.
> **Covered in:** [Observability fundamentals — Structured telemetry](README.md#structured-telemetry)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Profiles: Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. Structured telemetry: Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Observability fundamentals capability where Correlation, Metrics and Logs are first-class requirements.
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Correlation: Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-07

- [ ] **Question:** Design a production Observability fundamentals capability where Context propagation, Logs and Traces are first-class requirements.
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Context propagation: Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Traces: A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Observability fundamentals capability where Structured telemetry, Metrics and Events are first-class requirements.
> **Covered in:** [Observability fundamentals — Structured telemetry](README.md#structured-telemetry)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Structured telemetry: Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Events: Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-09

- [ ] **Question:** Design a production Observability fundamentals capability where Metrics, Logs and Profiles are first-class requirements.
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Metrics: Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Profiles: Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### OBSERVABILITY-FUNDAMENTALS-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Observability fundamentals capability where Logs, Traces and Correlation are first-class requirements.
> **Covered in:** [Observability fundamentals — Traces](README.md#traces)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Logs: Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Traces: A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. Correlation: Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Observability fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### OBSERVABILITY-FUNDAMENTALS-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Metrics. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://SERVICE/metrics` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Logs. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Logs](README.md#logs)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `promtool check rules rules.yml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Traces. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Traces](README.md#traces)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get events -A --sort-by=.lastTimestamp` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A distributed trace represents one request or unit of work as causally related spans across services. Each span records timing, status, attributes and parent/child context for one operation boundary. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Events. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Events](README.md#events)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `trivy fs .` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Operational events are discrete records that something happened, such as a deployment, scheduler decision or resource state change. They add change context but may be best-effort and short-lived rather than a durable audit history. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Profiles. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Profiles](README.md#profiles)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://SERVICE/metrics` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Profiles aggregate where a program spends CPU time, allocations, lock wait or other sampled resources. They attribute cost inside a process when request-level metrics or traces show that a service is slow but not which code consumes the resource. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Correlation. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Correlation](README.md#correlation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `promtool check rules rules.yml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Correlation connects signals that describe the same request, revision, tenant or incident window. It narrows investigation but does not by itself prove causation; time, propagation and dependency evidence still matter. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Context propagation. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Context propagation](README.md#context-propagation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get events -A --sort-by=.lastTimestamp` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Context propagation carries trace, correlation and selected baggage fields across process and asynchronous boundaries so downstream telemetry can be joined to the originating work without relying only on timestamps. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Structured telemetry. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Structured telemetry](README.md#structured-telemetry)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `trivy fs .` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Structured telemetry encodes fields according to a stable schema instead of embedding all meaning in free text. Consistent names, units and semantic conventions make queries and cross-service correlation reliable. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Metrics. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Metrics](README.md#metrics)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://SERVICE/metrics` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Metrics are timestamped numeric measurements, usually organized as time series with bounded labels. Counters, gauges and histogram-like distributions summarize rates, levels and latency without preserving every individual event. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### OBSERVABILITY-FUNDAMENTALS-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Logs. How do you lead it end to end?
> **Covered in:** [Observability fundamentals — Logs](README.md#logs)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `promtool check rules rules.yml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Logs are discrete timestamped records of events or state transitions. Structured logs make stable fields queryable, while message text and high-cardinality identifiers preserve detail that would be unsafe or expensive as metric labels. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Observability fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Observability](../README.md) · [Study note](README.md) · [Next: Metrics →](../02-metrics/README.md)

<!-- reading-navigation:end -->
