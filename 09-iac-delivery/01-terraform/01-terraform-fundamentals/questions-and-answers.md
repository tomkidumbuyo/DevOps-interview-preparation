# Terraform fundamentals — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### TERRAFORM-FUNDAMENTALS-JN-01

- [ ] **Question:** What is HCL, and why does it matter in Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — HCL](README.md#hcl)

**Answer:** **Definition.** HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which HCL matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Terraform fundamentals journey rather than an administrative green status alone.

### TERRAFORM-FUNDAMENTALS-JN-02

- [ ] **Question:** What is Providers, and why does it matter in Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Providers](README.md#providers)

**Answer:** **Definition.** A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Providers matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Terraform fundamentals journey rather than an administrative green status alone.

### TERRAFORM-FUNDAMENTALS-JN-03

- [ ] **Question:** What is Resources, and why does it matter in Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Resources](README.md#resources)

**Answer:** **Definition.** A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Resources matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Terraform fundamentals journey rather than an administrative green status alone.

### TERRAFORM-FUNDAMENTALS-JN-04

- [ ] **Question:** What is Data sources, and why does it matter in Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Definition.** A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Data sources matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Terraform fundamentals journey rather than an administrative green status alone.

### TERRAFORM-FUNDAMENTALS-JN-05

- [ ] **Question:** What is Variables, and why does it matter in Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Variables](README.md#variables)

**Answer:** **Definition.** Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Variables matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Terraform fundamentals journey rather than an administrative green status alone.

### TERRAFORM-FUNDAMENTALS-JN-06

- [ ] **Question:** What is Local values, and why does it matter in Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Definition.** Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Local values matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Terraform fundamentals journey rather than an administrative green status alone.

### TERRAFORM-FUNDAMENTALS-JN-07

- [ ] **Question:** What is Outputs, and why does it matter in Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Outputs](README.md#outputs)

**Answer:** **Definition.** Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Outputs matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Terraform fundamentals journey rather than an administrative green status alone.

### TERRAFORM-FUNDAMENTALS-JN-08

- [ ] **Code:** **Question:** What does `git diff --check` help you verify for Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `git diff --check` inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. **Mechanism.** The relevant Terraform fundamentals concept is Expressions: Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Terraform fundamentals rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### TERRAFORM-FUNDAMENTALS-JN-09

- [ ] **Code:** **Question:** What does `terraform fmt -check -recursive` help you verify for Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `terraform fmt -check -recursive` checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. **Mechanism.** The relevant Terraform fundamentals concept is Functions: Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Terraform fundamentals rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### TERRAFORM-FUNDAMENTALS-JN-10

- [ ] **Code:** **Question:** What does `terraform validate; terraform plan` help you verify for Terraform fundamentals?
> **Covered in:** [Terraform fundamentals — Outputs](README.md#outputs)

**Answer:** **Purpose.** `terraform validate; terraform plan` checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. **Mechanism.** The relevant Terraform fundamentals concept is Dependency graph: Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Terraform fundamentals rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### TERRAFORM-FUNDAMENTALS-JP-01

- [ ] **Code:** **Question:** A basic HCL check fails. What would you do first using the CLI?
> **Covered in:** [Terraform fundamentals — HCL](README.md#hcl)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform fmt -check -recursive`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-02

- [ ] **Question:** A basic Providers check fails. What would you do first?
> **Covered in:** [Terraform fundamentals — Providers](README.md#providers)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform validate; terraform plan`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-03

- [ ] **Question:** A basic Resources check fails. What would you do first?
> **Covered in:** [Terraform fundamentals — Resources](README.md#resources)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `pulumi preview --diff`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-04

- [ ] **Code:** **Question:** A basic Data sources check fails. What would you do first using the CLI?
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git diff --check`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-05

- [ ] **Question:** A basic Variables check fails. What would you do first?
> **Covered in:** [Terraform fundamentals — Variables](README.md#variables)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform fmt -check -recursive`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-06

- [ ] **Question:** A basic Local values check fails. What would you do first?
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform validate; terraform plan`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-07

- [ ] **Code:** **Question:** A basic Outputs check fails. What would you do first using the CLI?
> **Covered in:** [Terraform fundamentals — Outputs](README.md#outputs)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `pulumi preview --diff`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-08

- [ ] **Question:** A basic Expressions check fails. What would you do first?
> **Covered in:** [Terraform fundamentals — Expressions](README.md#expressions)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git diff --check`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-09

- [ ] **Question:** A basic Functions check fails. What would you do first?
> **Covered in:** [Terraform fundamentals — Functions](README.md#functions)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform fmt -check -recursive`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### TERRAFORM-FUNDAMENTALS-JP-10

- [ ] **Code:** **Question:** A basic Dependency graph check fails. What would you do first using the CLI?
> **Covered in:** [Terraform fundamentals — Dependency graph](README.md#dependency-graph)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform validate; terraform plan`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Terraform fundamentals client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### TERRAFORM-FUNDAMENTALS-MN-01

- [ ] **Question:** Compare HCL with Providers. When would each change your design?
> **Covered in:** [Terraform fundamentals — HCL](README.md#hcl)

**Answer:** **Definitions.** HCL: HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. Providers: A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-02

- [ ] **Question:** Compare Providers with Resources. When would each change your design?
> **Covered in:** [Terraform fundamentals — Providers](README.md#providers)

**Answer:** **Definitions.** Providers: A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. Resources: A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-03

- [ ] **Question:** Compare Resources with Data sources. When would each change your design?
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Definitions.** Resources: A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. Data sources: A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-04

- [ ] **Configuration review:** **Question:** Compare Data sources with Variables. When would each change your design?
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Definitions.** Data sources: A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. Variables: Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-05

- [ ] **Question:** Compare Variables with Local values. When would each change your design?
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Definitions.** Variables: Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. Local values: Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-06

- [ ] **Question:** Compare Local values with Outputs. When would each change your design?
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Definitions.** Local values: Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. Outputs: Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-07

- [ ] **Configuration review:** **Question:** Compare Outputs with Expressions. When would each change your design?
> **Covered in:** [Terraform fundamentals — Outputs](README.md#outputs)

**Answer:** **Definitions.** Outputs: Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. Expressions: Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-08

- [ ] **Question:** Compare Expressions with Functions. When would each change your design?
> **Covered in:** [Terraform fundamentals — Functions](README.md#functions)

**Answer:** **Definitions.** Expressions: Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. Functions: Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-09

- [ ] **Question:** Compare Functions with Dependency graph. When would each change your design?
> **Covered in:** [Terraform fundamentals — Dependency graph](README.md#dependency-graph)

**Answer:** **Definitions.** Functions: Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. Dependency graph: Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### TERRAFORM-FUNDAMENTALS-MN-10

- [ ] **Configuration review:** **Question:** Compare Dependency graph with HCL. When would each change your design?
> **Covered in:** [Terraform fundamentals — Dependency graph](README.md#dependency-graph)

**Answer:** **Definitions.** Dependency graph: Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. HCL: HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. **Mechanism.** In Terraform fundamentals, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Terraform fundamentals design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### TERRAFORM-FUNDAMENTALS-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around HCL; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — HCL](README.md#hcl)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform fmt -check -recursive` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-02

- [ ] **Question:** Production is degraded around Providers; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Providers](README.md#providers)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform validate; terraform plan` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Resources; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Resources](README.md#resources)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `pulumi preview --diff` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-04

- [ ] **Question:** Production is degraded around Data sources; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git diff --check` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Variables; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Variables](README.md#variables)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform fmt -check -recursive` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-06

- [ ] **Question:** Production is degraded around Local values; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform validate; terraform plan` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Outputs; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Outputs](README.md#outputs)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `pulumi preview --diff` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-08

- [ ] **Question:** Production is degraded around Expressions; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Expressions](README.md#expressions)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git diff --check` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Functions; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Functions](README.md#functions)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform fmt -check -recursive` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### TERRAFORM-FUNDAMENTALS-MP-10

- [ ] **Question:** Production is degraded around Dependency graph; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Terraform fundamentals — Dependency graph](README.md#dependency-graph)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform validate; terraform plan` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Terraform fundamentals source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### TERRAFORM-FUNDAMENTALS-SN-01

- [ ] **Question:** Design a production Terraform fundamentals capability where HCL, Data sources and Outputs are first-class requirements.
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** HCL: HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. Data sources: A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. Outputs: Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Terraform fundamentals capability where Providers, Variables and Expressions are first-class requirements.
> **Covered in:** [Terraform fundamentals — Variables](README.md#variables)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Providers: A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. Variables: Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. Expressions: Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-03

- [ ] **Question:** Design a production Terraform fundamentals capability where Resources, Local values and Functions are first-class requirements.
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Resources: A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. Local values: Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. Functions: Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Terraform fundamentals capability where Data sources, Outputs and Dependency graph are first-class requirements.
> **Covered in:** [Terraform fundamentals — Dependency graph](README.md#dependency-graph)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Data sources: A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. Outputs: Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. Dependency graph: Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-05

- [ ] **Question:** Design a production Terraform fundamentals capability where Variables, Expressions and HCL are first-class requirements.
> **Covered in:** [Terraform fundamentals — HCL](README.md#hcl)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Variables: Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. Expressions: Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. HCL: HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Terraform fundamentals capability where Local values, Functions and Providers are first-class requirements.
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Local values: Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. Functions: Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. Providers: A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-07

- [ ] **Question:** Design a production Terraform fundamentals capability where Outputs, Dependency graph and Resources are first-class requirements.
> **Covered in:** [Terraform fundamentals — Dependency graph](README.md#dependency-graph)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Outputs: Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. Dependency graph: Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. Resources: A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Terraform fundamentals capability where Expressions, HCL and Data sources are first-class requirements.
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Expressions: Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. HCL: HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. Data sources: A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-09

- [ ] **Question:** Design a production Terraform fundamentals capability where Functions, Providers and Variables are first-class requirements.
> **Covered in:** [Terraform fundamentals — Functions](README.md#functions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Functions: Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. Providers: A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. Variables: Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### TERRAFORM-FUNDAMENTALS-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Terraform fundamentals capability where Dependency graph, Resources and Local values are first-class requirements.
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Dependency graph: Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. Resources: A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. Local values: Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Terraform fundamentals options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### TERRAFORM-FUNDAMENTALS-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving HCL. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — HCL](README.md#hcl)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform fmt -check -recursive` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** HashiCorp Configuration Language is Terraform's declarative configuration syntax. Blocks describe objects and relationships, while expressions compute values; HCL describes desired configuration rather than an ordered shell script. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Providers. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Providers](README.md#providers)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform validate; terraform plan` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Terraform provider is a versioned plugin that defines resource and data-source schemas and translates Terraform operations into remote API calls. Provider version, configuration and credentials are part of the execution contract. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Resources. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Resources](README.md#resources)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `pulumi preview --diff` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Terraform resource block declares a managed object's desired arguments. Its resource address is associated through state with a remote identity so Terraform can plan create, read, update or delete operations. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Data sources. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Data sources](README.md#data-sources)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git diff --check` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Terraform data source reads information managed outside the current resource address. It supplies values to the graph but does not make Terraform the lifecycle owner of the remote object. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Variables. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Variables](README.md#variables)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform fmt -check -recursive` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Terraform input variables are the typed external parameters of a module. Defaults, validation, sensitivity and nullable behavior define the module's caller contract. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Local values. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Local values](README.md#local-values)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform validate; terraform plan` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Terraform local values give names to expressions inside a module. They reduce repetition and centralize transformation but are not external inputs and do not create independently stored resources. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Outputs. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Outputs](README.md#outputs)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `pulumi preview --diff` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Terraform outputs expose selected root-module results or child-module return values. An output can depend on values unknown until apply and marking it sensitive only redacts normal display—it may still exist in state. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Expressions. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Expressions](README.md#expressions)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git diff --check` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Terraform expressions combine literals, references, operators, conditionals and collection transformations to compute typed values. References also let Terraform infer dependency edges. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Functions. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Functions](README.md#functions)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform fmt -check -recursive` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Terraform functions are built-in pure transformations such as encoding, string, collection and filesystem helpers. They return values during configuration evaluation and do not call providers or create resources. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### TERRAFORM-FUNDAMENTALS-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Dependency graph. How do you lead it end to end?
> **Covered in:** [Terraform fundamentals — Dependency graph](README.md#dependency-graph)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform validate; terraform plan` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Terraform builds a directed dependency graph from references and explicit `depends_on` relationships. It walks independent vertices concurrently and orders operations whose correctness depends on another object. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Terraform fundamentals from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Terraform](../README.md) · [Study note](README.md) · [Next: Terraform planning →](../02-terraform-planning/README.md)

<!-- reading-navigation:end -->
