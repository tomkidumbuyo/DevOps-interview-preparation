# 150 cross-domain procedural scenarios with answers

> This is the requested post-topic mega scenario bank. Mark rehearsal progress by changing `- [ ]` to `- [x]`. Give your answer before revealing the model answer.

Use **stabilize → scope → inspect → hypothesize → test → mitigate → verify → prevent**. Senior answers must also cover incident/change leadership, communication, security and tenant boundaries, SLO/RPO/RTO, cost, source-of-truth repair and ownership.

## Linux and host incidents

### HOST-01

- [ ] **Question:** A production API host has load average 80 but CPU utilization is below 20%. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-02

- [ ] **Question:** A service is OOM-killed inside its container while the node still shows free memory. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-03

- [ ] **Question:** The root filesystem reports 100% full, but visible files account for only half the space. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-04

- [ ] **Question:** A host has free disk blocks but applications fail with `No space left on device`. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-05

- [ ] **Question:** A systemd service enters a rapid restart loop immediately after a configuration rollout. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-06

- [ ] **Question:** SSH login latency jumps to 40 seconds although established sessions are healthy. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-07

- [ ] **Question:** One NUMA-heavy inference process is slow only on a subset of otherwise identical hosts. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-08

- [ ] **Question:** New outbound connections fail intermittently while existing connections remain healthy. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-09

- [ ] **Question:** Disk latency spikes after a database change although average IOPS is below the advertised limit. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### HOST-10

- [ ] **Question:** A kernel upgrade leaves several GPU nodes unable to boot into the normal target. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; uptime; systemctl --failed; vmstat 1; iostat -xz 1; journalctl -b -p warning`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this linux and host incidents case, test process/cgroup limits, PSI, open-deleted files, inodes, service lifecycle, kernel/device evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Networking, DNS, TLS and load balancing

### NET-01

- [ ] **Question:** A hostname resolves correctly on your laptop but not inside one production subnet. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-02

- [ ] **Question:** TLS works by IP with verification disabled but fails by the public hostname. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-03

- [ ] **Question:** An ALB reports healthy targets while users receive intermittent 502 responses. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-04

- [ ] **Question:** Only large HTTP responses stall after a new VPN or overlay-network rollout. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-05

- [ ] **Question:** TCP connections complete in one direction but return traffic takes a different firewall path. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-06

- [ ] **Question:** A DNS migration causes sporadic traffic to continue reaching the old environment for hours. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-07

- [ ] **Question:** A reverse proxy retries streaming LLM requests and some users receive duplicated output. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-08

- [ ] **Question:** A NAT gateway is healthy, but one busy node cannot create more outbound connections. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-09

- [ ] **Question:** A NetworkPolicy rollout breaks DNS for selected namespaces while direct IP connections work. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### NET-10

- [ ] **Question:** Certificate rotation succeeds on the load balancer but several clients reject the new chain. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `getent ahosts NAME; ip route get IP; nc -vz HOST PORT; curl -vk URL; openssl s_client -connect HOST:443 -servername HOST </dev/null`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this networking, dns, tls and load balancing case, test resolver path, route/policy/NAT, MTU, TCP state, SNI/trust chain, proxy/LB versus target evidence. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Containers and software supply chain

### CTR-01

- [ ] **Question:** A container works locally but exits immediately in Kubernetes with no useful application log. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-02

- [ ] **Question:** An image tag was overwritten and two replicas run different bytes under the same release name. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-03

- [ ] **Question:** A build cache accidentally includes a cloud credential in an image layer. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-04

- [ ] **Question:** A non-root container cannot write to a mounted volume after security hardening. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-05

- [ ] **Question:** A container consumes all host PIDs even though its CPU and memory are within limits. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-06

- [ ] **Question:** A critical base-image CVE has no fixed package version and production release is tomorrow. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-07

- [ ] **Question:** A multi-stage Docker build produces an image three times larger than expected. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-08

- [ ] **Question:** A registry outage blocks deployment even though the image ran on the node yesterday. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-09

- [ ] **Question:** A privileged debugging container is left running on a GPU node after an incident. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### CTR-10

- [ ] **Question:** A signed image verifies, but the deployment still contains unreviewed vulnerable code. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `docker image inspect IMAGE; docker history --no-trunc IMAGE; docker inspect CONTAINER; docker events --since 30m`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this containers and software supply chain case, test digest/provenance, entrypoint/PID 1, UID/mounts, namespaces/cgroups/capabilities, cache/layers, registry and policy. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Kubernetes control plane and cluster lifecycle

### KCP-01

- [ ] **Question:** The Kubernetes API is intermittently slow while workloads continue serving traffic. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-02

- [ ] **Question:** etcd disk usage grows rapidly and API writes begin timing out. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-03

- [ ] **Question:** A validating admission webhook outage blocks emergency production changes. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-04

- [ ] **Question:** A controller repeatedly updates the same object and creates an API-server hot loop. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-05

- [ ] **Question:** A cluster upgrade reveals an API version removed from the new control plane. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-06

- [ ] **Question:** All new Pods remain Pending after a scheduler configuration change. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-07

- [ ] **Question:** A kubelet certificate expires on a subset of nodes. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-08

- [ ] **Question:** An operator upgrade corrupts status reconciliation but desired custom resources remain intact. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-09

- [ ] **Question:** A failed control-plane restore returns the API but not the expected workload state. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KCP-10

- [ ] **Question:** A noisy tenant consumes API request capacity and delays critical controllers. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get --raw '/readyz?verbose'; kubectl get events -A --sort-by=.lastTimestamp; kubectl get --raw /metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes control plane and cluster lifecycle case, test API request path, admission, APF, watch/reconcile loops, etcd quorum/storage, version skew, backup/restore validation. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Kubernetes workloads, network and storage

### KWS-01

- [ ] **Question:** A Deployment is stuck with old and new ReplicaSets both consuming full capacity. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-02

- [ ] **Question:** Pods are Ready but receive no Service traffic after a label change. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-03

- [ ] **Question:** A StatefulSet cannot reschedule because its volume is attached in another zone. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-04

- [ ] **Question:** A readiness probe causes a cascading outage during downstream latency. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-05

- [ ] **Question:** A node drain hangs on a workload with a strict PodDisruptionBudget. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-06

- [ ] **Question:** A DaemonSet update removes the CNI agent from too many nodes simultaneously. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-07

- [ ] **Question:** A CronJob runs twice around a controller restart and duplicates a billing operation. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-08

- [ ] **Question:** A namespace default-deny policy blocks an undocumented external dependency. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-09

- [ ] **Question:** Pods restart with ephemeral-storage eviction while the persistent volume has space. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### KWS-10

- [ ] **Question:** CoreDNS latency rises only for names with many search-domain expansions. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get pod,deploy,rs,svc,endpointslice,pvc -A -o wide; kubectl describe pod POD -n NS; kubectl get events -n NS --sort-by=.lastTimestamp`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this kubernetes workloads, network and storage case, test desired/observed rollout, readiness/endpoints, placement/topology, PDB/eviction, CNI/DNS/policy, CSI attachment and idempotency. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## AWS production operations

### AWS-01

- [ ] **Question:** Private-subnet instances lose S3 access after an egress-cost optimization. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-02

- [ ] **Question:** An EC2 Auto Scaling group adds instances, but the ALB never routes to them. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-03

- [ ] **Question:** An S3 cross-account application receives AccessDenied despite an apparent IAM allow. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-04

- [ ] **Question:** A DynamoDB table throttles one customer although aggregate capacity looks low. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-05

- [ ] **Question:** Lambda concurrency spikes and overloads an RDS database. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-06

- [ ] **Question:** An EKS node group cannot launch GPU instances despite available account quota. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-07

- [ ] **Question:** CloudTrail evidence is missing from one newly created AWS account. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-08

- [ ] **Question:** A KMS key-policy change makes encrypted snapshots unusable in the DR account. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-09

- [ ] **Question:** Route 53 failover sends traffic to a healthy endpoint whose database is stale. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AWS-10

- [ ] **Question:** A NAT Gateway bill triples after an EKS topology change. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `aws sts get-caller-identity; aws configure list; aws cloudtrail lookup-events --max-results 20; aws service-quotas list-service-quotas --service-code SERVICE`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this aws production operations case, test identity/SCP/resource/key/endpoint policy, Region/account, route and endpoint, target health, partition-key/concurrency, capacity and cost path. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## GCP and multi-cloud operations

### GCP-01

- [ ] **Question:** A GKE workload can call Google APIs in one project but receives 403 in another. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-02

- [ ] **Question:** Shared VPC routes exist, but a service project cannot reach a private managed endpoint. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-03

- [ ] **Question:** A global external load balancer serves an old backend after a regional failover. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-04

- [ ] **Question:** GPU Pods remain Pending even though the GCP console shows quota in the region. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-05

- [ ] **Question:** A Vertex AI endpoint rollout increases latency only for one model version. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-06

- [ ] **Question:** Cloud Run scales rapidly and exhausts a private database connection limit. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-07

- [ ] **Question:** Pub/Sub redeliveries cause duplicate writes after subscriber latency increases. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-08

- [ ] **Question:** A dual-cloud failover violates the promised data-residency boundary. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-09

- [ ] **Question:** Cloud Logging cost doubles after enabling detailed AI request telemetry. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GCP-10

- [ ] **Question:** Workload Identity Federation stops working after an external issuer rotates keys. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `gcloud auth list; gcloud config list; gcloud projects describe PROJECT; gcloud logging read 'severity>=ERROR' --limit=20`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gcp and multi-cloud operations case, test resource hierarchy/IAM, project/quota/zone, Shared VPC and private access, backend health, workload identity, model revision and cross-cloud contract. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Terraform, Pulumi, CI/CD and GitOps

### IAC-01

- [ ] **Question:** Terraform proposes replacing a production database after a module refactor. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-02

- [ ] **Question:** A state-lock holder disappeared, but the remote backend still reports the lock. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-03

- [ ] **Question:** A `for_each` key change plans destructive recreation of many stable resources. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-04

- [ ] **Question:** Pulumi preview shows unknown Outputs that a developer tried to convert to plain strings. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-05

- [ ] **Question:** A CI runner from an untrusted pull request can request cloud credentials. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-06

- [ ] **Question:** A GitOps controller continually reverts an approved emergency mitigation. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-07

- [ ] **Question:** A canary deployment passes infrastructure health but fails a business-quality metric. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-08

- [ ] **Question:** A database migration succeeds in staging but locks the production table. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-09

- [ ] **Question:** A provider upgrade changes defaults across hundreds of stacks. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### IAC-10

- [ ] **Question:** A secrets scanner finds a token in old Git history after it was deleted from HEAD. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git diff --check; terraform fmt -check -recursive; terraform validate; terraform plan -out=tfplan; pulumi preview --diff`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this terraform, pulumi, ci/cd and gitops case, test identity/state/address mapping, plan evidence, refactor/import, secret Outputs, runner trust, reconciliation, compatibility, rollout and rollback. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Observability, SRE and disaster recovery

### SRE-01

- [ ] **Question:** The dashboard is green while customers report high tail latency. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-02

- [ ] **Question:** A high-cardinality label causes the Prometheus backend to run out of memory. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-03

- [ ] **Question:** Trace sampling drops nearly every request from a rare but severe error path. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-04

- [ ] **Question:** An alert fires every morning but never requires action. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-05

- [ ] **Question:** A service burns its monthly error budget in 45 minutes. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-06

- [ ] **Question:** A dependency outage triggers synchronized retries across the platform. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-07

- [ ] **Question:** A backup job is green, but the restored application fails integrity checks. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-08

- [ ] **Question:** A regional failover meets infrastructure RTO but clients cache old DNS. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-09

- [ ] **Question:** An OpenTelemetry Collector outage silently drops AI cost telemetry. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SRE-10

- [ ] **Question:** A postmortem has many action items, but the same incident recurs three months later. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -s http://SERVICE/metrics; kubectl get events -A --sort-by=.lastTimestamp; promtool check rules rules.yml`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this observability, sre and disaster recovery case, test user SLI and distributions, telemetry pipeline, cardinality/sampling, burn rate, retry/backpressure, application restore and verified ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Security and incident response

### SEC-01

- [ ] **Question:** A long-lived cloud access key is exposed in a public repository. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-02

- [ ] **Question:** A Kubernetes ServiceAccount token appears in application logs. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-03

- [ ] **Question:** An attacker used an overly broad CI role to create a new administrator identity. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-04

- [ ] **Question:** A compromised container contacts an unfamiliar external IP. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-05

- [ ] **Question:** A secret rotation breaks half the replicas because connections are pooled. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-06

- [ ] **Question:** A customer asks for proof that one tenant cannot retrieve another tenant's data. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-07

- [ ] **Question:** A vulnerability scanner flags a critical library that is unreachable at runtime. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-08

- [ ] **Question:** A WAF rule blocks legitimate streaming prompts after a managed-rule update. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-09

- [ ] **Question:** Audit logs contain sensitive prompts that policy says must not be retained. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SEC-10

- [ ] **Question:** A BYOD contractor laptop is lost while it still has active platform sessions. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `date -u; whoami; git log --since='24 hours ago'; kubectl auth can-i --list; aws sts get-caller-identity`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this security and incident response case, test containment without evidence loss, revoke/rotate/session scope, identity and audit timeline, tenant boundary, exfiltration, recovery and control verification. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## GPU infrastructure

### GPU-01

- [ ] **Question:** A GPU is visible on the host but Kubernetes advertises zero allocatable GPUs. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-02

- [ ] **Question:** CUDA reports a driver/runtime compatibility error after a node-image update. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-03

- [ ] **Question:** NCCL collectives hang only for multi-node training jobs. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-04

- [ ] **Question:** GPU utilization is near 100%, but inference token throughput falls sharply. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-05

- [ ] **Question:** MIG reconfiguration leaves scheduled Pods unable to start. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-06

- [ ] **Question:** Time-sliced GPU tenants interfere and violate latency SLOs. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-07

- [ ] **Question:** Karpenter cannot satisfy a pending GPU Pod despite broad instance-family selection. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-08

- [ ] **Question:** Spot interruption repeatedly loses a long training job's progress. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-09

- [ ] **Question:** Model downloads saturate network and delay every GPU node cold start. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GPU-10

- [ ] **Question:** DCGM metrics disappear from one node pool while inference continues. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `nvidia-smi; nvidia-smi topo -m; kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu; kubectl describe pod POD -n NS`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this gpu infrastructure case, test hardware→driver→runtime→plugin/DRA→framework chain, topology, memory/ECC/Xid, scheduling resource names, capacity/caching and SLO. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Model serving and inference

### SERVE-01

- [ ] **Question:** A new model fits on disk but crashes with GPU OOM during warmup. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-02

- [ ] **Question:** Time to first token rises while inter-token latency remains stable. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-03

- [ ] **Question:** Continuous batching increases throughput but breaches one tenant's latency objective. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-04

- [ ] **Question:** A canary has healthy HTTP probes but produces lower-quality answers. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-05

- [ ] **Question:** A vLLM rollout fails because tokenizer and model revisions do not match. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-06

- [ ] **Question:** KServe scales to zero successfully but misses the first-request SLO. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-07

- [ ] **Question:** A multi-model server thrashes its model cache under a changed traffic mix. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-08

- [ ] **Question:** Quantization reduces memory but changes output quality for one language. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-09

- [ ] **Question:** A streaming client disconnect leaves expensive generation running. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### SERVE-10

- [ ] **Question:** An inference autoscaler oscillates between too many and too few replicas. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `kubectl get deployment,inferenceservice,pod -n inference -o wide; kubectl logs POD -n inference --since=30m; curl -s http://MODEL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this model serving and inference case, test weights/tokenizer/template/runtime/hardware lineage, queue/prefill/decode/KV memory, cancellation, batching, cold path, quality gate and scaling stability. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## LLM gateway and provider operations

### GW-01

- [ ] **Question:** One tenant exhausts provider quota and degrades every other tenant. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-02

- [ ] **Question:** A provider returns 429, but gateway retries make the incident worse. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-03

- [ ] **Question:** A fallback model is available but cannot satisfy the required data residency. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-04

- [ ] **Question:** Streaming responses are billed twice after a mid-stream connection failure. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-05

- [ ] **Question:** A semantic cache returns an answer generated under another tenant's policy. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-06

- [ ] **Question:** A model alias changes underneath an application without a release record. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-07

- [ ] **Question:** Gateway token estimates consistently undercount requests with images or tools. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-08

- [ ] **Question:** A prompt policy blocks a critical customer workflow after a rules update. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-09

- [ ] **Question:** Provider latency is healthy but end-to-end gateway latency doubles. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### GW-10

- [ ] **Question:** Gateway logs help debugging but expose prompts and credentials. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `curl -N -v GATEWAY_URL/v1/chat/completions -H 'Authorization: Bearer TEST_TOKEN' -d @request.json; curl -s GATEWAY_URL/metrics`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this llm gateway and provider operations case, test tenant identity/policy/quota, normalized semantics, retry/fallback after streaming, token/concurrency work, privacy-safe tracing, metering and release ownership. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## RAG, evaluation, agents and AI governance

### AIG-01

- [ ] **Question:** A RAG system returns documents the requesting user cannot open at the source. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-02

- [ ] **Question:** Deleted source content continues appearing in answers from an old vector index. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-03

- [ ] **Question:** Retrieval recall improves while answer faithfulness gets worse. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-04

- [ ] **Question:** A model-as-judge approves its own model family much more often than humans do. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-05

- [ ] **Question:** An evaluation release gate is stable but does not predict production complaints. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-06

- [ ] **Question:** A retrieved document instructs an agent to exfiltrate secrets through a tool. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-07

- [ ] **Question:** An agent repeats a costly write tool after a timeout with unknown outcome. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-08

- [ ] **Question:** A prompt change reaches production without model, dataset or evaluator lineage. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-09

- [ ] **Question:** A customer requests all AI assets and data flows involving its records. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### AIG-10

- [ ] **Question:** A high-risk AI feature must be paused while preserving audit and appeal evidence. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `python -m pytest -q; python -m evals.run --manifest release.yaml --dataset golden.jsonl; curl -s VECTOR_DB/health`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this rag, evaluation, agents and ai governance case, test source ACL before retrieval, ingestion/delete lineage, retrieval versus generation metrics, evaluator calibration, deterministic tool authorization/idempotency, inventory and governance workflow. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

## Platform ownership, deployment models and FinOps

### PLAT-01

- [ ] **Question:** You inherit an undocumented AI platform and the Head of Engineering needs immediate operational relief. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-02

- [ ] **Question:** A SaaS control plane must manage customer-cloud data planes without permanent administrator access. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-03

- [ ] **Question:** An on-prem customer cannot provide the GPU model assumed by the release. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-04

- [ ] **Question:** An air-gapped deployment requires upgrades and vulnerability evidence without registry access. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-05

- [ ] **Question:** A multi-cloud design promises portability but depends deeply on one provider's identity and queue semantics. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-06

- [ ] **Question:** GPU reservation cost is high while customers still see capacity-related errors. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-07

- [ ] **Question:** Teams bypass the platform golden path because onboarding takes two weeks. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-08

- [ ] **Question:** A cost-cutting request demands 30% savings without changing the production SLO. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-09

- [ ] **Question:** A platform migration has unclear decision rights across engineering, security and a customer team. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.

### PLAT-10

- [ ] **Question:** You must explain an AI incident, trade-offs and recovery plan to executives and affected engineers. Walk through immediate safety, evidence, hypotheses, commands/queries, reversible mitigation, user-facing verification, durable repair and prevention.

**Answer:** **1. Stabilize and lead.** State the user/tenant impact, start time, SLO or safety risk, incident owner and communications cadence; pause risky changes and protect data. **2. Scope safely.** Confirm identity, environment, Region/cluster/host and recent changes. Start read-only with `git log --since='90 days ago' --stat; kubectl get events -A --sort-by=.lastTimestamp; terraform plan; cloud-cost-export-query`. Preserve timestamps, request/change IDs and exact errors. **3. Trace the mechanism.** For this platform ownership, deployment models and finops case, test inventory/ownership, control/data trust, packaging/compatibility/upgrades/support, demand and unit economics, golden-path product metrics, ADR and communication. Compare healthy and failing cohorts and use one discriminating hypothesis at a time. **4. Mitigate.** Stop amplification, shed or reroute only to an approved compatible path, and prefer a reversible configuration or rollback. Do not widen access, delete evidence, or assume a timeout means no side effect. **5. Verify.** Re-run the original user/workload path and verify SLO, correctness/quality, tenant/security boundary, data integrity and billing—not merely resource health. **6. Repair and prevent.** Fix desired state in Git/IaC, add a regression test and leading alert, document the runbook/decision, assign an owner/deadline and rehearse the corrected failure. In an interview, state which result would disprove your first hypothesis and when you would escalate or roll back.
