# Container security — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### CONTAINER-SECURITY-JN-01

- [ ] **Question:** What is Non-root containers, and why does it matter in Container security?
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Definition.** The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Non-root containers matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Container security journey rather than an administrative green status alone.

### CONTAINER-SECURITY-JN-02

- [ ] **Question:** What is Read-only root filesystems, and why does it matter in Container security?
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Definition.** The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Read-only root filesystems matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Container security journey rather than an administrative green status alone.

### CONTAINER-SECURITY-JN-03

- [ ] **Question:** What is Dropping capabilities, and why does it matter in Container security?
> **Covered in:** [Container security — Dropping capabilities](README.md#dropping-capabilities)

**Answer:** **Definition.** The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Dropping capabilities matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Container security journey rather than an administrative green status alone.

### CONTAINER-SECURITY-JN-04

- [ ] **Question:** What is Seccomp profiles, and why does it matter in Container security?
> **Covered in:** [Container security — Seccomp profiles](README.md#seccomp-profiles)

**Answer:** **Definition.** The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Seccomp profiles matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Container security journey rather than an administrative green status alone.

### CONTAINER-SECURITY-JN-05

- [ ] **Question:** What is Minimal images, and why does it matter in Container security?
> **Covered in:** [Container security — Minimal images](README.md#minimal-images)

**Answer:** **Definition.** The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Minimal images matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Container security journey rather than an administrative green status alone.

### CONTAINER-SECURITY-JN-06

- [ ] **Question:** What is Distroless images, and why does it matter in Container security?
> **Covered in:** [Container security — Distroless images](README.md#distroless-images)

**Answer:** **Definition.** The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Distroless images matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Container security journey rather than an administrative green status alone.

### CONTAINER-SECURITY-JN-07

- [ ] **Question:** What is SBOMs, and why does it matter in Container security?
> **Covered in:** [Container security — SBOMs](README.md#sboms)

**Answer:** **Definition.** The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which SBOMs matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Container security journey rather than an administrative green status alone.

### CONTAINER-SECURITY-JN-08

- [ ] **Code:** **Question:** What does `docker events --since 30m` help you verify for Container security?
> **Covered in:** [Container security — Security, reliability and production ownership](README.md#security-reliability-and-production-ownership)

**Answer:** **Purpose.** `docker events --since 30m` shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. **Mechanism.** The relevant Container security concept is Image provenance: The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Container security rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### CONTAINER-SECURITY-JN-09

- [ ] **Code:** **Question:** What does `docker image inspect IMAGE` help you verify for Container security?
> **Covered in:** [Container security — Image provenance](README.md#image-provenance)

**Answer:** **Purpose.** `docker image inspect IMAGE` shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. **Mechanism.** The relevant Container security concept is Runtime security: Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Container security rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### CONTAINER-SECURITY-JN-10

- [ ] **Code:** **Question:** What does `docker inspect CONTAINER` help you verify for Container security?
> **Covered in:** [Container security — Security, reliability and production ownership](README.md#security-reliability-and-production-ownership)

**Answer:** **Purpose.** `docker inspect CONTAINER` shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. **Mechanism.** The relevant Container security concept is Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Container security rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### CONTAINER-SECURITY-JP-01

- [ ] **Code:** **Question:** A basic Non-root containers check fails. What would you do first using the CLI?
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker image inspect IMAGE`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-02

- [ ] **Question:** A basic Read-only root filesystems check fails. What would you do first?
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker inspect CONTAINER`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-03

- [ ] **Question:** A basic Dropping capabilities check fails. What would you do first?
> **Covered in:** [Container security — Dropping capabilities](README.md#dropping-capabilities)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker stats --no-stream`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-04

- [ ] **Code:** **Question:** A basic Seccomp profiles check fails. What would you do first using the CLI?
> **Covered in:** [Container security — Seccomp profiles](README.md#seccomp-profiles)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker events --since 30m`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-05

- [ ] **Question:** A basic Minimal images check fails. What would you do first?
> **Covered in:** [Container security — Minimal images](README.md#minimal-images)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker image inspect IMAGE`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-06

- [ ] **Question:** A basic Distroless images check fails. What would you do first?
> **Covered in:** [Container security — Distroless images](README.md#distroless-images)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker inspect CONTAINER`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-07

- [ ] **Code:** **Question:** A basic SBOMs check fails. What would you do first using the CLI?
> **Covered in:** [Container security — SBOMs](README.md#sboms)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker stats --no-stream`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-08

- [ ] **Question:** A basic Image provenance check fails. What would you do first?
> **Covered in:** [Container security — Image provenance](README.md#image-provenance)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker events --since 30m`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-09

- [ ] **Question:** A basic Runtime security check fails. What would you do first?
> **Covered in:** [Container security — Runtime security](README.md#runtime-security)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker image inspect IMAGE`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### CONTAINER-SECURITY-JP-10

- [ ] **Code:** **Question:** A basic Non-root containers check fails. What would you do first using the CLI?
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker inspect CONTAINER`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Container security client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### CONTAINER-SECURITY-MN-01

- [ ] **Question:** Compare Non-root containers with Read-only root filesystems. When would each change your design?
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Definitions.** Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Read-only root filesystems: The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-02

- [ ] **Question:** Compare Read-only root filesystems with Dropping capabilities. When would each change your design?
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Definitions.** Read-only root filesystems: The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. Dropping capabilities: The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-03

- [ ] **Question:** Compare Dropping capabilities with Seccomp profiles. When would each change your design?
> **Covered in:** [Container security — Dropping capabilities](README.md#dropping-capabilities)

**Answer:** **Definitions.** Dropping capabilities: The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Seccomp profiles: The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-04

- [ ] **Configuration review:** **Question:** Compare Seccomp profiles with Minimal images. When would each change your design?
> **Covered in:** [Container security — Minimal images](README.md#minimal-images)

**Answer:** **Definitions.** Seccomp profiles: The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. Minimal images: The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-05

- [ ] **Question:** Compare Minimal images with Distroless images. When would each change your design?
> **Covered in:** [Container security — Minimal images](README.md#minimal-images)

**Answer:** **Definitions.** Minimal images: The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Distroless images: The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-06

- [ ] **Question:** Compare Distroless images with SBOMs. When would each change your design?
> **Covered in:** [Container security — Distroless images](README.md#distroless-images)

**Answer:** **Definitions.** Distroless images: The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. SBOMs: The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-07

- [ ] **Configuration review:** **Question:** Compare SBOMs with Image provenance. When would each change your design?
> **Covered in:** [Container security — Image provenance](README.md#image-provenance)

**Answer:** **Definitions.** SBOMs: The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Image provenance: The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-08

- [ ] **Question:** Compare Image provenance with Runtime security. When would each change your design?
> **Covered in:** [Container security — Image provenance](README.md#image-provenance)

**Answer:** **Definitions.** Image provenance: The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Runtime security: Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-09

- [ ] **Question:** Compare Runtime security with Non-root containers. When would each change your design?
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Definitions.** Runtime security: Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### CONTAINER-SECURITY-MN-10

- [ ] **Configuration review:** **Question:** Compare Non-root containers with Non-root containers. When would each change your design?
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Definitions.** Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. **Mechanism.** In Container security, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Container security design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### CONTAINER-SECURITY-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Non-root containers; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker image inspect IMAGE` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-02

- [ ] **Question:** Production is degraded around Read-only root filesystems; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker inspect CONTAINER` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Dropping capabilities; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Dropping capabilities](README.md#dropping-capabilities)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker stats --no-stream` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-04

- [ ] **Question:** Production is degraded around Seccomp profiles; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Seccomp profiles](README.md#seccomp-profiles)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker events --since 30m` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Minimal images; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Minimal images](README.md#minimal-images)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker image inspect IMAGE` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-06

- [ ] **Question:** Production is degraded around Distroless images; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Distroless images](README.md#distroless-images)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker inspect CONTAINER` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around SBOMs; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — SBOMs](README.md#sboms)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker stats --no-stream` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-08

- [ ] **Question:** Production is degraded around Image provenance; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Image provenance](README.md#image-provenance)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker events --since 30m` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Runtime security; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Runtime security](README.md#runtime-security)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker image inspect IMAGE` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### CONTAINER-SECURITY-MP-10

- [ ] **Question:** Production is degraded around Non-root containers; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker inspect CONTAINER` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Container security source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### CONTAINER-SECURITY-SN-01

- [ ] **Question:** Design a production Container security capability where Non-root containers, Seccomp profiles and SBOMs are first-class requirements.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Seccomp profiles: The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. SBOMs: The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Container security capability where Read-only root filesystems, Minimal images and Image provenance are first-class requirements.
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Read-only root filesystems: The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. Minimal images: The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Image provenance: The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-03

- [ ] **Question:** Design a production Container security capability where Dropping capabilities, Distroless images and Runtime security are first-class requirements.
> **Covered in:** [Container security — Dropping capabilities](README.md#dropping-capabilities)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Dropping capabilities: The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Distroless images: The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. Runtime security: Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Container security capability where Seccomp profiles, SBOMs and Non-root containers are first-class requirements.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Seccomp profiles: The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. SBOMs: The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-05

- [ ] **Question:** Design a production Container security capability where Minimal images, Image provenance and Non-root containers are first-class requirements.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Minimal images: The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Image provenance: The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Container security capability where Distroless images, Runtime security and Read-only root filesystems are first-class requirements.
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Distroless images: The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. Runtime security: Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. Read-only root filesystems: The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-07

- [ ] **Question:** Design a production Container security capability where SBOMs, Non-root containers and Dropping capabilities are first-class requirements.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** SBOMs: The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Dropping capabilities: The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Container security capability where Image provenance, Non-root containers and Seccomp profiles are first-class requirements.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Image provenance: The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Seccomp profiles: The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-09

- [ ] **Question:** Design a production Container security capability where Runtime security, Read-only root filesystems and Minimal images are first-class requirements.
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Runtime security: Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. Read-only root filesystems: The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. Minimal images: The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### CONTAINER-SECURITY-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Container security capability where Non-root containers, Dropping capabilities and Distroless images are first-class requirements.
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Non-root containers: The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Dropping capabilities: The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Distroless images: The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Container security options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### CONTAINER-SECURITY-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Non-root containers. How do you lead it end to end?
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker image inspect IMAGE` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Read-only root filesystems. How do you lead it end to end?
> **Covered in:** [Container security — Read-only root filesystems](README.md#read-only-root-filesystems)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker inspect CONTAINER` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Read-only root filesystems holds state beyond one operation or process lifetime and therefore must define durability, consistency, concurrency, replication, backup, restore and deletion semantics within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Dropping capabilities. How do you lead it end to end?
> **Covered in:** [Container security — Dropping capabilities](README.md#dropping-capabilities)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker stats --no-stream` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Dropping capabilities refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Seccomp profiles. How do you lead it end to end?
> **Covered in:** [Container security — Seccomp profiles](README.md#seccomp-profiles)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker events --since 30m` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Seccomp profiles is an observability signal that records a defined aspect of runtime behavior; its schema, context, sampling, retention and query semantics determine which hypotheses it can distinguish within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Minimal images. How do you lead it end to end?
> **Covered in:** [Container security — Minimal images](README.md#minimal-images)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker image inspect IMAGE` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Minimal images refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Distroless images. How do you lead it end to end?
> **Covered in:** [Container security — Distroless images](README.md#distroless-images)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker inspect CONTAINER` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Distroless images is a trust-control mechanism that identifies an actor or protected asset and enforces which action is allowed under explicit context, with audit, rotation, revocation and recovery behavior within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving SBOMs. How do you lead it end to end?
> **Covered in:** [Container security — SBOMs](README.md#sboms)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker stats --no-stream` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term SBOMs refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Image provenance. How do you lead it end to end?
> **Covered in:** [Container security — Image provenance](README.md#image-provenance)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker events --since 30m` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Image provenance refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Runtime security. How do you lead it end to end?
> **Covered in:** [Container security — Runtime security](README.md#runtime-security)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker image inspect IMAGE` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Defines a trust/control boundary: identify actor, protected asset, decision/enforcement point, least privilege, bypass path, audit evidence, rotation/revocation and recovery. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### CONTAINER-SECURITY-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Non-root containers. How do you lead it end to end?
> **Covered in:** [Container security — Non-root containers](README.md#non-root-containers)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker inspect CONTAINER` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Non-root containers refers to an image, runtime or kernel-isolation mechanism that changes the filesystem, resource or security view of a process within Container security. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Container security from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Registries](../06-registries/README.md) · [Study note](README.md) · [Next: Kubernetes core platform →](../../06-kubernetes/README.md)

<!-- reading-navigation:end -->
