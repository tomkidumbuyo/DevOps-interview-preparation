# Model registry, artifacts and lineage — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-01

- [ ] **Question:** What is Registered model, and why does it matter in Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Definition.** Logical product/capability groups versions without conflating mutable alias with byte identity. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Registered model matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Model registry, artifacts and lineage journey rather than an administrative green status alone.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-02

- [ ] **Question:** What is Model version, and why does it matter in Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Definition.** Immutable digest plus source run/data/code/config defines one candidate. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Model version matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Model registry, artifacts and lineage journey rather than an administrative green status alone.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-03

- [ ] **Question:** What is Alias, and why does it matter in Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Alias](README.md#alias)

**Answer:** **Definition.** Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Alias matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Model registry, artifacts and lineage journey rather than an administrative green status alone.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-04

- [ ] **Question:** What is Artifact format, and why does it matter in Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Artifact format](README.md#artifact-format)

**Answer:** **Definition.** Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Artifact format matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Model registry, artifacts and lineage journey rather than an administrative green status alone.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-05

- [ ] **Question:** What is Signature/schema, and why does it matter in Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Signature/schema](README.md#signatureschema)

**Answer:** **Definition.** Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. **Mechanism.** A real request crosses several independent states: name resolution returns an address, the source selects a route and source address, link or overlay forwarding reaches the next hop, stateful or stateless policy evaluates the flow, transport establishes communication, and TLS/application protocols negotiate their own contract. The return path must also work. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence progresses layer by layer: correct name/address, expected route, permitted flow, listening endpoint, successful handshake and valid application response. Timeouts, refusals, resets and protocol errors mean different layers; packet loss, MTU, asymmetric paths, connection-state exhaustion and proxy timeout mismatch are common production failures. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Signature/schema matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Model registry, artifacts and lineage journey rather than an administrative green status alone.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-06

- [ ] **Question:** What is Lineage, and why does it matter in Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Definition.** Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Lineage matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Model registry, artifacts and lineage journey rather than an administrative green status alone.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-07

- [ ] **Question:** What is Model card, and why does it matter in Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Model card](README.md#model-card)

**Answer:** **Definition.** Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Model card matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Model registry, artifacts and lineage journey rather than an administrative green status alone.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-08

- [ ] **Code:** **Question:** What does `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST` help you verify for Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Purpose.** `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST` tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. **Mechanism.** The relevant Model registry, artifacts and lineage concept is Storage: Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Model registry, artifacts and lineage rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-09

- [ ] **Code:** **Question:** What does `mlflow models search -f 'name = MODEL_NAME'` help you verify for Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Purpose.** `mlflow models search -f 'name = MODEL_NAME'` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Model registry, artifacts and lineage concept is Access: Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Model registry, artifacts and lineage rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JN-10

- [ ] **Code:** **Question:** What does `mlflow artifacts download --run-id RUN_ID --artifact-path model` help you verify for Model registry, artifacts and lineage?
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Purpose.** `mlflow artifacts download --run-id RUN_ID --artifact-path model` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Model registry, artifacts and lineage concept is Deployment binding: Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Model registry, artifacts and lineage rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-01

- [ ] **Code:** **Question:** A basic Registered model check fails. What would you do first using the CLI?
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow models search -f 'name = MODEL_NAME'`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Logical product/capability groups versions without conflating mutable alias with byte identity. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-02

- [ ] **Question:** A basic Model version check fails. What would you do first?
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow artifacts download --run-id RUN_ID --artifact-path model`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Immutable digest plus source run/data/code/config defines one candidate. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-03

- [ ] **Question:** A basic Alias check fails. What would you do first?
> **Covered in:** [Model registry, artifacts and lineage — Alias](README.md#alias)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `sha256sum MODEL_ARTIFACT`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-04

- [ ] **Code:** **Question:** A basic Artifact format check fails. What would you do first using the CLI?
> **Covered in:** [Model registry, artifacts and lineage — Artifact format](README.md#artifact-format)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-05

- [ ] **Question:** A basic Signature/schema check fails. What would you do first?
> **Covered in:** [Model registry, artifacts and lineage — Signature/schema](README.md#signatureschema)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow models search -f 'name = MODEL_NAME'`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-06

- [ ] **Question:** A basic Lineage check fails. What would you do first?
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow artifacts download --run-id RUN_ID --artifact-path model`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-07

- [ ] **Code:** **Question:** A basic Model card check fails. What would you do first using the CLI?
> **Covered in:** [Model registry, artifacts and lineage — Model card](README.md#model-card)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `sha256sum MODEL_ARTIFACT`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-08

- [ ] **Question:** A basic Storage check fails. What would you do first?
> **Covered in:** [Model registry, artifacts and lineage — Storage](README.md#storage)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-09

- [ ] **Question:** A basic Access check fails. What would you do first?
> **Covered in:** [Model registry, artifacts and lineage — Access](README.md#access)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow models search -f 'name = MODEL_NAME'`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-JP-10

- [ ] **Code:** **Question:** A basic Deployment binding check fails. What would you do first using the CLI?
> **Covered in:** [Model registry, artifacts and lineage — Deployment binding](README.md#deployment-binding)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow artifacts download --run-id RUN_ID --artifact-path model`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Model registry, artifacts and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-01

- [ ] **Question:** Compare Registered model with Model version. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Definitions.** Registered model: Logical product/capability groups versions without conflating mutable alias with byte identity. Model version: Immutable digest plus source run/data/code/config defines one candidate. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-02

- [ ] **Question:** Compare Model version with Alias. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Definitions.** Model version: Immutable digest plus source run/data/code/config defines one candidate. Alias: Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-03

- [ ] **Question:** Compare Alias with Artifact format. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Artifact format](README.md#artifact-format)

**Answer:** **Definitions.** Alias: Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. Artifact format: Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-04

- [ ] **Configuration review:** **Question:** Compare Artifact format with Signature/schema. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Artifact format](README.md#artifact-format)

**Answer:** **Definitions.** Artifact format: Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. Signature/schema: Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-05

- [ ] **Question:** Compare Signature/schema with Lineage. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Definitions.** Signature/schema: Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. Lineage: Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-06

- [ ] **Question:** Compare Lineage with Model card. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Model card](README.md#model-card)

**Answer:** **Definitions.** Lineage: Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. Model card: Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-07

- [ ] **Configuration review:** **Question:** Compare Model card with Storage. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Model card](README.md#model-card)

**Answer:** **Definitions.** Model card: Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. Storage: Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-08

- [ ] **Question:** Compare Storage with Access. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Storage](README.md#storage)

**Answer:** **Definitions.** Storage: Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. Access: Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-09

- [ ] **Question:** Compare Access with Deployment binding. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Deployment binding](README.md#deployment-binding)

**Answer:** **Definitions.** Access: Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. Deployment binding: Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MN-10

- [ ] **Configuration review:** **Question:** Compare Deployment binding with Registered model. When would each change your design?
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Definitions.** Deployment binding: Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. Registered model: Logical product/capability groups versions without conflating mutable alias with byte identity. **Mechanism.** In Model registry, artifacts and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Model registry, artifacts and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Registered model; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow models search -f 'name = MODEL_NAME'` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Logical product/capability groups versions without conflating mutable alias with byte identity. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-02

- [ ] **Question:** Production is degraded around Model version; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow artifacts download --run-id RUN_ID --artifact-path model` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Immutable digest plus source run/data/code/config defines one candidate. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Alias; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Alias](README.md#alias)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `sha256sum MODEL_ARTIFACT` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-04

- [ ] **Question:** Production is degraded around Artifact format; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Artifact format](README.md#artifact-format)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Signature/schema; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Signature/schema](README.md#signatureschema)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow models search -f 'name = MODEL_NAME'` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-06

- [ ] **Question:** Production is degraded around Lineage; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow artifacts download --run-id RUN_ID --artifact-path model` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Model card; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Model card](README.md#model-card)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `sha256sum MODEL_ARTIFACT` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-08

- [ ] **Question:** Production is degraded around Storage; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Storage](README.md#storage)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Access; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Access](README.md#access)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow models search -f 'name = MODEL_NAME'` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-MP-10

- [ ] **Question:** Production is degraded around Deployment binding; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Model registry, artifacts and lineage — Deployment binding](README.md#deployment-binding)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow artifacts download --run-id RUN_ID --artifact-path model` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Model registry, artifacts and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-01

- [ ] **Question:** Design a production Model registry, artifacts and lineage capability where Registered model, Artifact format and Model card are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Registered model: Logical product/capability groups versions without conflating mutable alias with byte identity. Artifact format: Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. Model card: Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Model registry, artifacts and lineage capability where Model version, Signature/schema and Storage are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Model version: Immutable digest plus source run/data/code/config defines one candidate. Signature/schema: Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. Storage: Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-03

- [ ] **Question:** Design a production Model registry, artifacts and lineage capability where Alias, Lineage and Access are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Alias](README.md#alias)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Alias: Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. Lineage: Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. Access: Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Model registry, artifacts and lineage capability where Artifact format, Model card and Deployment binding are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Deployment binding](README.md#deployment-binding)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Artifact format: Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. Model card: Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. Deployment binding: Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-05

- [ ] **Question:** Design a production Model registry, artifacts and lineage capability where Signature/schema, Storage and Registered model are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Signature/schema: Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. Storage: Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. Registered model: Logical product/capability groups versions without conflating mutable alias with byte identity. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Model registry, artifacts and lineage capability where Lineage, Access and Model version are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Lineage: Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. Access: Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. Model version: Immutable digest plus source run/data/code/config defines one candidate. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-07

- [ ] **Question:** Design a production Model registry, artifacts and lineage capability where Model card, Deployment binding and Alias are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Deployment binding](README.md#deployment-binding)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Model card: Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. Deployment binding: Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. Alias: Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Model registry, artifacts and lineage capability where Storage, Registered model and Artifact format are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Storage: Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. Registered model: Logical product/capability groups versions without conflating mutable alias with byte identity. Artifact format: Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-09

- [ ] **Question:** Design a production Model registry, artifacts and lineage capability where Access, Model version and Signature/schema are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Access: Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. Model version: Immutable digest plus source run/data/code/config defines one candidate. Signature/schema: Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Model registry, artifacts and lineage capability where Deployment binding, Alias and Lineage are first-class requirements.
> **Covered in:** [Model registry, artifacts and lineage — Deployment binding](README.md#deployment-binding)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Deployment binding: Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. Alias: Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. Lineage: Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Model registry, artifacts and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Registered model. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Registered model](README.md#registered-model)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow models search -f 'name = MODEL_NAME'` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Logical product/capability groups versions without conflating mutable alias with byte identity. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Model version. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Model version](README.md#model-version)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow artifacts download --run-id RUN_ID --artifact-path model` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Immutable digest plus source run/data/code/config defines one candidate. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Alias. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Alias](README.md#alias)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `sha256sum MODEL_ARTIFACT` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Champion/Challenger/production is a mutable pointer changed through authorized auditable promotion. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Artifact format. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Artifact format](README.md#artifact-format)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Framework-native, safetensors, ONNX or MLflow flavor determines loader/runtime compatibility and attack surface. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Signature/schema. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Signature/schema](README.md#signatureschema)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow models search -f 'name = MODEL_NAME'` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Input/output names/types/shapes and preprocessing contract prevent silent integration mismatch. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Lineage. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Lineage](README.md#lineage)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow artifacts download --run-id RUN_ID --artifact-path model` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Dataset/run/checkpoint/evaluation/license/approval links support reproduction and impact analysis. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Model card. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Model card](README.md#model-card)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `sha256sum MODEL_ARTIFACT` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Intended use, limitations, training/evaluation data summary and risk evidence inform consumers. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Storage. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Storage](README.md#storage)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `oras manifest fetch REGISTRY/REPOSITORY@sha256:DIGEST` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Encryption, immutable retention, replication, malware/unsafe-deserialization controls and restore tests protect artifacts. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Access. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Access](README.md#access)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow models search -f 'name = MODEL_NAME'` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Separate discover/download/promote/delete privileges and audit sensitive model exfiltration. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MODEL-REGISTRY-ARTIFACTS-AND-LINEAGE-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Deployment binding. How do you lead it end to end?
> **Covered in:** [Model registry, artifacts and lineage — Deployment binding](README.md#deployment-binding)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow artifacts download --run-id RUN_ID --artifact-path model` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Deployed endpoint records exact model/runtime/prompt/index/policy revision rather than only an alias. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Model registry, artifacts and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Reproducibility and environment capture](../09-reproducibility-environments/README.md) · [Study note](README.md) · [Next: LLM release manifests and compatibility →](../11-llm-release-manifest/README.md)

<!-- reading-navigation:end -->
