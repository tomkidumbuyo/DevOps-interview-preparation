# Data versioning, quality and lineage — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-01

- [ ] **Question:** What is Logical dataset version, and why does it matter in Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Logical dataset version](README.md#logical-dataset-version)

**Answer:** **Definition.** Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Logical dataset version matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Data versioning, quality and lineage journey rather than an administrative green status alone.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-02

- [ ] **Question:** What is Content digest, and why does it matter in Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Content digest](README.md#content-digest)

**Answer:** **Definition.** Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Content digest matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Data versioning, quality and lineage journey rather than an administrative green status alone.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-03

- [ ] **Question:** What is Schema contract, and why does it matter in Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Schema contract](README.md#schema-contract)

**Answer:** **Definition.** Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Schema contract matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Data versioning, quality and lineage journey rather than an administrative green status alone.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-04

- [ ] **Question:** What is Statistical validation, and why does it matter in Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Statistical validation](README.md#statistical-validation)

**Answer:** **Definition.** Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Statistical validation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Data versioning, quality and lineage journey rather than an administrative green status alone.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-05

- [ ] **Question:** What is Point-in-time correctness, and why does it matter in Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Point-in-time correctness](README.md#point-in-time-correctness)

**Answer:** **Definition.** Feature joins must use only information available at prediction time to prevent future leakage. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Point-in-time correctness matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Data versioning, quality and lineage journey rather than an administrative green status alone.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-06

- [ ] **Question:** What is Lineage graph, and why does it matter in Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Lineage graph](README.md#lineage-graph)

**Answer:** **Definition.** Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Lineage graph matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Data versioning, quality and lineage journey rather than an administrative green status alone.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-07

- [ ] **Question:** What is Sensitive data, and why does it matter in Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Sensitive data](README.md#sensitive-data)

**Answer:** **Definition.** Classification, purpose, consent, residency, minimization and access policy follow derived datasets. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Sensitive data matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Data versioning, quality and lineage journey rather than an administrative green status alone.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-08

- [ ] **Code:** **Question:** What does `python scripts/verify_dataset_manifest.py data/manifest.json` help you verify for Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `python scripts/verify_dataset_manifest.py data/manifest.json` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Data versioning, quality and lineage concept is Deletion propagation: Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Data versioning, quality and lineage rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-09

- [ ] **Code:** **Question:** What does `dvc status; dvc dag; dvc repro` help you verify for Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `dvc status; dvc dag; dvc repro` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Data versioning, quality and lineage concept is Data drift: Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Data versioning, quality and lineage rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JN-10

- [ ] **Code:** **Question:** What does `dvc data status --json` help you verify for Data versioning, quality and lineage?
> **Covered in:** [Data versioning, quality and lineage — Lineage graph](README.md#lineage-graph)

**Answer:** **Purpose.** `dvc data status --json` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Data versioning, quality and lineage concept is Reproducible split: Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Data versioning, quality and lineage rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-01

- [ ] **Code:** **Question:** A basic Logical dataset version check fails. What would you do first using the CLI?
> **Covered in:** [Data versioning, quality and lineage — Logical dataset version](README.md#logical-dataset-version)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `dvc status; dvc dag; dvc repro`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-02

- [ ] **Question:** A basic Content digest check fails. What would you do first?
> **Covered in:** [Data versioning, quality and lineage — Content digest](README.md#content-digest)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `dvc data status --json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-03

- [ ] **Question:** A basic Schema contract check fails. What would you do first?
> **Covered in:** [Data versioning, quality and lineage — Schema contract](README.md#schema-contract)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `great_expectations checkpoint run CHECKPOINT`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-04

- [ ] **Code:** **Question:** A basic Statistical validation check fails. What would you do first using the CLI?
> **Covered in:** [Data versioning, quality and lineage — Statistical validation](README.md#statistical-validation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python scripts/verify_dataset_manifest.py data/manifest.json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-05

- [ ] **Question:** A basic Point-in-time correctness check fails. What would you do first?
> **Covered in:** [Data versioning, quality and lineage — Point-in-time correctness](README.md#point-in-time-correctness)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `dvc status; dvc dag; dvc repro`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Feature joins must use only information available at prediction time to prevent future leakage. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-06

- [ ] **Question:** A basic Lineage graph check fails. What would you do first?
> **Covered in:** [Data versioning, quality and lineage — Lineage graph](README.md#lineage-graph)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `dvc data status --json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-07

- [ ] **Code:** **Question:** A basic Sensitive data check fails. What would you do first using the CLI?
> **Covered in:** [Data versioning, quality and lineage — Sensitive data](README.md#sensitive-data)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `great_expectations checkpoint run CHECKPOINT`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Classification, purpose, consent, residency, minimization and access policy follow derived datasets. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-08

- [ ] **Question:** A basic Deletion propagation check fails. What would you do first?
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python scripts/verify_dataset_manifest.py data/manifest.json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-09

- [ ] **Question:** A basic Data drift check fails. What would you do first?
> **Covered in:** [Data versioning, quality and lineage — Data drift](README.md#data-drift)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `dvc status; dvc dag; dvc repro`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-JP-10

- [ ] **Code:** **Question:** A basic Reproducible split check fails. What would you do first using the CLI?
> **Covered in:** [Data versioning, quality and lineage — Reproducible split](README.md#reproducible-split)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `dvc data status --json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Data versioning, quality and lineage client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-01

- [ ] **Question:** Compare Logical dataset version with Content digest. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Logical dataset version](README.md#logical-dataset-version)

**Answer:** **Definitions.** Logical dataset version: Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. Content digest: Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-02

- [ ] **Question:** Compare Content digest with Schema contract. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Content digest](README.md#content-digest)

**Answer:** **Definitions.** Content digest: Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. Schema contract: Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-03

- [ ] **Question:** Compare Schema contract with Statistical validation. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Statistical validation](README.md#statistical-validation)

**Answer:** **Definitions.** Schema contract: Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. Statistical validation: Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-04

- [ ] **Configuration review:** **Question:** Compare Statistical validation with Point-in-time correctness. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Statistical validation](README.md#statistical-validation)

**Answer:** **Definitions.** Statistical validation: Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. Point-in-time correctness: Feature joins must use only information available at prediction time to prevent future leakage. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-05

- [ ] **Question:** Compare Point-in-time correctness with Lineage graph. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Point-in-time correctness](README.md#point-in-time-correctness)

**Answer:** **Definitions.** Point-in-time correctness: Feature joins must use only information available at prediction time to prevent future leakage. Lineage graph: Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-06

- [ ] **Question:** Compare Lineage graph with Sensitive data. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Lineage graph](README.md#lineage-graph)

**Answer:** **Definitions.** Lineage graph: Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. Sensitive data: Classification, purpose, consent, residency, minimization and access policy follow derived datasets. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-07

- [ ] **Configuration review:** **Question:** Compare Sensitive data with Deletion propagation. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Definitions.** Sensitive data: Classification, purpose, consent, residency, minimization and access policy follow derived datasets. Deletion propagation: Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-08

- [ ] **Question:** Compare Deletion propagation with Data drift. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Definitions.** Deletion propagation: Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. Data drift: Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-09

- [ ] **Question:** Compare Data drift with Reproducible split. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Reproducible split](README.md#reproducible-split)

**Answer:** **Definitions.** Data drift: Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. Reproducible split: Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MN-10

- [ ] **Configuration review:** **Question:** Compare Reproducible split with Logical dataset version. When would each change your design?
> **Covered in:** [Data versioning, quality and lineage — Logical dataset version](README.md#logical-dataset-version)

**Answer:** **Definitions.** Reproducible split: Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. Logical dataset version: Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. **Mechanism.** In Data versioning, quality and lineage, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Data versioning, quality and lineage design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Logical dataset version; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Logical dataset version](README.md#logical-dataset-version)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `dvc status; dvc dag; dvc repro` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-02

- [ ] **Question:** Production is degraded around Content digest; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Content digest](README.md#content-digest)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `dvc data status --json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Schema contract; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Schema contract](README.md#schema-contract)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `great_expectations checkpoint run CHECKPOINT` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-04

- [ ] **Question:** Production is degraded around Statistical validation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Statistical validation](README.md#statistical-validation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python scripts/verify_dataset_manifest.py data/manifest.json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Point-in-time correctness; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Point-in-time correctness](README.md#point-in-time-correctness)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `dvc status; dvc dag; dvc repro` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Feature joins must use only information available at prediction time to prevent future leakage. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-06

- [ ] **Question:** Production is degraded around Lineage graph; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Lineage graph](README.md#lineage-graph)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `dvc data status --json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Sensitive data; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Sensitive data](README.md#sensitive-data)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `great_expectations checkpoint run CHECKPOINT` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Classification, purpose, consent, residency, minimization and access policy follow derived datasets. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-08

- [ ] **Question:** Production is degraded around Deletion propagation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python scripts/verify_dataset_manifest.py data/manifest.json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Data drift; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Data drift](README.md#data-drift)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `dvc status; dvc dag; dvc repro` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-MP-10

- [ ] **Question:** Production is degraded around Reproducible split; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Data versioning, quality and lineage — Reproducible split](README.md#reproducible-split)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `dvc data status --json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Data versioning, quality and lineage source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-01

- [ ] **Question:** Design a production Data versioning, quality and lineage capability where Logical dataset version, Statistical validation and Sensitive data are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Logical dataset version](README.md#logical-dataset-version)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Logical dataset version: Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. Statistical validation: Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. Sensitive data: Classification, purpose, consent, residency, minimization and access policy follow derived datasets. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Data versioning, quality and lineage capability where Content digest, Point-in-time correctness and Deletion propagation are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Content digest: Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. Point-in-time correctness: Feature joins must use only information available at prediction time to prevent future leakage. Deletion propagation: Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-03

- [ ] **Question:** Design a production Data versioning, quality and lineage capability where Schema contract, Lineage graph and Data drift are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Schema contract](README.md#schema-contract)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Schema contract: Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. Lineage graph: Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. Data drift: Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Data versioning, quality and lineage capability where Statistical validation, Sensitive data and Reproducible split are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Statistical validation](README.md#statistical-validation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Statistical validation: Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. Sensitive data: Classification, purpose, consent, residency, minimization and access policy follow derived datasets. Reproducible split: Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-05

- [ ] **Question:** Design a production Data versioning, quality and lineage capability where Point-in-time correctness, Deletion propagation and Logical dataset version are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Point-in-time correctness: Feature joins must use only information available at prediction time to prevent future leakage. Deletion propagation: Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. Logical dataset version: Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Data versioning, quality and lineage capability where Lineage graph, Data drift and Content digest are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Content digest](README.md#content-digest)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Lineage graph: Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. Data drift: Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. Content digest: Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-07

- [ ] **Question:** Design a production Data versioning, quality and lineage capability where Sensitive data, Reproducible split and Schema contract are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Schema contract](README.md#schema-contract)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Sensitive data: Classification, purpose, consent, residency, minimization and access policy follow derived datasets. Reproducible split: Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. Schema contract: Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Data versioning, quality and lineage capability where Deletion propagation, Logical dataset version and Statistical validation are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Deletion propagation: Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. Logical dataset version: Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. Statistical validation: Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-09

- [ ] **Question:** Design a production Data versioning, quality and lineage capability where Data drift, Content digest and Point-in-time correctness are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Content digest](README.md#content-digest)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Data drift: Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. Content digest: Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. Point-in-time correctness: Feature joins must use only information available at prediction time to prevent future leakage. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Data versioning, quality and lineage capability where Reproducible split, Schema contract and Lineage graph are first-class requirements.
> **Covered in:** [Data versioning, quality and lineage — Schema contract](README.md#schema-contract)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Reproducible split: Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. Schema contract: Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. Lineage graph: Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Data versioning, quality and lineage options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Logical dataset version. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Logical dataset version](README.md#logical-dataset-version)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `dvc status; dvc dag; dvc repro` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Immutable manifest identifies source snapshot/partitions/files and schema rather than relying on a mutable path. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Content digest. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Content digest](README.md#content-digest)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `dvc data status --json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Hashes prove byte identity but require canonical manifests and cannot prove authorization or semantic quality. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Schema contract. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Schema contract](README.md#schema-contract)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `great_expectations checkpoint run CHECKPOINT` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Names/types/nullability/ranges/keys and evolution compatibility fail early before expensive training. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Statistical validation. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Statistical validation](README.md#statistical-validation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python scripts/verify_dataset_manifest.py data/manifest.json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Distribution, missingness, duplicates, label balance and leakage tests compare against owned baselines. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Point-in-time correctness. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Point-in-time correctness](README.md#point-in-time-correctness)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `dvc status; dvc dag; dvc repro` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Feature joins must use only information available at prediction time to prevent future leakage. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Lineage graph. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Lineage graph](README.md#lineage-graph)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `dvc data status --json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Source→transform code/config→dataset→run→model→release supports impact and deletion analysis. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Sensitive data. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Sensitive data](README.md#sensitive-data)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `great_expectations checkpoint run CHECKPOINT` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Classification, purpose, consent, residency, minimization and access policy follow derived datasets. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Deletion propagation. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Deletion propagation](README.md#deletion-propagation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python scripts/verify_dataset_manifest.py data/manifest.json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Source deletion must reconcile caches, snapshots, embeddings, indexes and retained models under policy. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Data drift. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Data drift](README.md#data-drift)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `dvc status; dvc dag; dvc repro` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Changed production input is a signal for investigation/evaluation, not automatic proof that retraining helps. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DATA-VERSIONING-QUALITY-AND-LINEAGE-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Reproducible split. How do you lead it end to end?
> **Covered in:** [Data versioning, quality and lineage — Reproducible split](README.md#reproducible-split)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `dvc data status --json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Versioned entity/time-aware train/validation/test assignment avoids overlap and evaluation contamination. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Data versioning, quality and lineage from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Experiment tracking and comparison](../02-experiment-tracking/README.md) · [Study note](README.md) · [Next: Feature stores and training-serving consistency →](../04-feature-store-training-serving/README.md)

<!-- reading-navigation:end -->
