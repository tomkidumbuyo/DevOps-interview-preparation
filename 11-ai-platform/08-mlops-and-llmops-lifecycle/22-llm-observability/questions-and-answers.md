# LLM observability and quality telemetry — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-01

- [ ] **Question:** What is End-to-end trace, and why does it matter in LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Definition.** Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which End-to-end trace matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM observability and quality telemetry journey rather than an administrative green status alone.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-02

- [ ] **Question:** What is TTFT, and why does it matter in LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — TTFT](README.md#ttft)

**Answer:** **Definition.** Time to first token separates admission/queue/prefill from ongoing decode latency. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which TTFT matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM observability and quality telemetry journey rather than an administrative green status alone.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-03

- [ ] **Question:** What is Inter-token latency, and why does it matter in LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Definition.** Streaming cadence reveals decode/runtime/network behavior hidden by total duration. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Inter-token latency matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM observability and quality telemetry journey rather than an administrative green status alone.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-04

- [ ] **Question:** What is Token work, and why does it matter in LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — Token work](README.md#token-work)

**Answer:** **Definition.** Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Token work matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM observability and quality telemetry journey rather than an administrative green status alone.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-05

- [ ] **Question:** What is Goodput, and why does it matter in LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — Goodput](README.md#goodput)

**Answer:** **Definition.** Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Goodput matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM observability and quality telemetry journey rather than an administrative green status alone.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-06

- [ ] **Question:** What is Quality signal, and why does it matter in LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Definition.** Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Quality signal matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM observability and quality telemetry journey rather than an administrative green status alone.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-07

- [ ] **Question:** What is Privacy, and why does it matter in LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — Privacy](README.md#privacy)

**Answer:** **Definition.** Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Privacy matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM observability and quality telemetry journey rather than an administrative green status alone.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-08

- [ ] **Code:** **Question:** What does `python scripts/trace_release.py --release RELEASE_ID` help you verify for LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `python scripts/trace_release.py --release RELEASE_ID` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant LLM observability and quality telemetry concept is Cardinality: Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM observability and quality telemetry rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-09

- [ ] **Code:** **Question:** What does `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` help you verify for LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — TTFT](README.md#ttft)

**Answer:** **Purpose.** `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. **Mechanism.** The relevant LLM observability and quality telemetry concept is Cost: Provider charges and allocated accelerator time join tenant/project/release without exposing content. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM observability and quality telemetry rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JN-10

- [ ] **Code:** **Question:** What does `otelcol validate --config otel-collector.yaml` help you verify for LLM observability and quality telemetry?
> **Covered in:** [LLM observability and quality telemetry — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `otelcol validate --config otel-collector.yaml` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant LLM observability and quality telemetry concept is Telemetry reliability: Collector/exporter backlog, drops, sampling and schema/version changes are monitored. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM observability and quality telemetry rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-01

- [ ] **Code:** **Question:** A basic End-to-end trace check fails. What would you do first using the CLI?
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-02

- [ ] **Question:** A basic TTFT check fails. What would you do first?
> **Covered in:** [LLM observability and quality telemetry — TTFT](README.md#ttft)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `otelcol validate --config otel-collector.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Time to first token separates admission/queue/prefill from ongoing decode latency. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-03

- [ ] **Question:** A basic Inter-token latency check fails. What would you do first?
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n observability deploy/otel-collector --since=30m`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Streaming cadence reveals decode/runtime/network behavior hidden by total duration. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-04

- [ ] **Code:** **Question:** A basic Token work check fails. What would you do first using the CLI?
> **Covered in:** [LLM observability and quality telemetry — Token work](README.md#token-work)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python scripts/trace_release.py --release RELEASE_ID`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-05

- [ ] **Question:** A basic Goodput check fails. What would you do first?
> **Covered in:** [LLM observability and quality telemetry — Goodput](README.md#goodput)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-06

- [ ] **Question:** A basic Quality signal check fails. What would you do first?
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `otelcol validate --config otel-collector.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-07

- [ ] **Code:** **Question:** A basic Privacy check fails. What would you do first using the CLI?
> **Covered in:** [LLM observability and quality telemetry — Privacy](README.md#privacy)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n observability deploy/otel-collector --since=30m`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-08

- [ ] **Question:** A basic Cardinality check fails. What would you do first?
> **Covered in:** [LLM observability and quality telemetry — Cardinality](README.md#cardinality)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python scripts/trace_release.py --release RELEASE_ID`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-09

- [ ] **Question:** A basic Cost check fails. What would you do first?
> **Covered in:** [LLM observability and quality telemetry — Cost](README.md#cost)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Provider charges and allocated accelerator time join tenant/project/release without exposing content. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-JP-10

- [ ] **Code:** **Question:** A basic Telemetry reliability check fails. What would you do first using the CLI?
> **Covered in:** [LLM observability and quality telemetry — Telemetry reliability](README.md#telemetry-reliability)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `otelcol validate --config otel-collector.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Collector/exporter backlog, drops, sampling and schema/version changes are monitored. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM observability and quality telemetry client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-01

- [ ] **Question:** Compare End-to-end trace with TTFT. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Definitions.** End-to-end trace: Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. TTFT: Time to first token separates admission/queue/prefill from ongoing decode latency. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-02

- [ ] **Question:** Compare TTFT with Inter-token latency. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Definitions.** TTFT: Time to first token separates admission/queue/prefill from ongoing decode latency. Inter-token latency: Streaming cadence reveals decode/runtime/network behavior hidden by total duration. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-03

- [ ] **Question:** Compare Inter-token latency with Token work. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Definitions.** Inter-token latency: Streaming cadence reveals decode/runtime/network behavior hidden by total duration. Token work: Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-04

- [ ] **Configuration review:** **Question:** Compare Token work with Goodput. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Token work](README.md#token-work)

**Answer:** **Definitions.** Token work: Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. Goodput: Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-05

- [ ] **Question:** Compare Goodput with Quality signal. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Definitions.** Goodput: Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. Quality signal: Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-06

- [ ] **Question:** Compare Quality signal with Privacy. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Definitions.** Quality signal: Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. Privacy: Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-07

- [ ] **Configuration review:** **Question:** Compare Privacy with Cardinality. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Privacy](README.md#privacy)

**Answer:** **Definitions.** Privacy: Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. Cardinality: Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-08

- [ ] **Question:** Compare Cardinality with Cost. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Cardinality](README.md#cardinality)

**Answer:** **Definitions.** Cardinality: Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. Cost: Provider charges and allocated accelerator time join tenant/project/release without exposing content. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-09

- [ ] **Question:** Compare Cost with Telemetry reliability. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — Telemetry reliability](README.md#telemetry-reliability)

**Answer:** **Definitions.** Cost: Provider charges and allocated accelerator time join tenant/project/release without exposing content. Telemetry reliability: Collector/exporter backlog, drops, sampling and schema/version changes are monitored. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MN-10

- [ ] **Configuration review:** **Question:** Compare Telemetry reliability with End-to-end trace. When would each change your design?
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Definitions.** Telemetry reliability: Collector/exporter backlog, drops, sampling and schema/version changes are monitored. End-to-end trace: Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. **Mechanism.** In LLM observability and quality telemetry, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM observability and quality telemetry design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around End-to-end trace; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-02

- [ ] **Question:** Production is degraded around TTFT; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — TTFT](README.md#ttft)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `otelcol validate --config otel-collector.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Time to first token separates admission/queue/prefill from ongoing decode latency. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Inter-token latency; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n observability deploy/otel-collector --since=30m` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Streaming cadence reveals decode/runtime/network behavior hidden by total duration. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-04

- [ ] **Question:** Production is degraded around Token work; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Token work](README.md#token-work)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python scripts/trace_release.py --release RELEASE_ID` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Goodput; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Goodput](README.md#goodput)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-06

- [ ] **Question:** Production is degraded around Quality signal; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `otelcol validate --config otel-collector.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Privacy; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Privacy](README.md#privacy)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n observability deploy/otel-collector --since=30m` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-08

- [ ] **Question:** Production is degraded around Cardinality; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Cardinality](README.md#cardinality)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python scripts/trace_release.py --release RELEASE_ID` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Cost; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Cost](README.md#cost)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Provider charges and allocated accelerator time join tenant/project/release without exposing content. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-MP-10

- [ ] **Question:** Production is degraded around Telemetry reliability; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM observability and quality telemetry — Telemetry reliability](README.md#telemetry-reliability)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `otelcol validate --config otel-collector.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Collector/exporter backlog, drops, sampling and schema/version changes are monitored. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM observability and quality telemetry source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-01

- [ ] **Question:** Design a production LLM observability and quality telemetry capability where End-to-end trace, Token work and Privacy are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** End-to-end trace: Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. Token work: Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. Privacy: Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production LLM observability and quality telemetry capability where TTFT, Goodput and Cardinality are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — Goodput](README.md#goodput)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** TTFT: Time to first token separates admission/queue/prefill from ongoing decode latency. Goodput: Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. Cardinality: Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-03

- [ ] **Question:** Design a production LLM observability and quality telemetry capability where Inter-token latency, Quality signal and Cost are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Inter-token latency: Streaming cadence reveals decode/runtime/network behavior hidden by total duration. Quality signal: Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. Cost: Provider charges and allocated accelerator time join tenant/project/release without exposing content. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production LLM observability and quality telemetry capability where Token work, Privacy and Telemetry reliability are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — Telemetry reliability](README.md#telemetry-reliability)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Token work: Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. Privacy: Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. Telemetry reliability: Collector/exporter backlog, drops, sampling and schema/version changes are monitored. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-05

- [ ] **Question:** Design a production LLM observability and quality telemetry capability where Goodput, Cardinality and End-to-end trace are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Goodput: Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. Cardinality: Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. End-to-end trace: Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production LLM observability and quality telemetry capability where Quality signal, Cost and TTFT are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Quality signal: Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. Cost: Provider charges and allocated accelerator time join tenant/project/release without exposing content. TTFT: Time to first token separates admission/queue/prefill from ongoing decode latency. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-07

- [ ] **Question:** Design a production LLM observability and quality telemetry capability where Privacy, Telemetry reliability and Inter-token latency are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Privacy: Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. Telemetry reliability: Collector/exporter backlog, drops, sampling and schema/version changes are monitored. Inter-token latency: Streaming cadence reveals decode/runtime/network behavior hidden by total duration. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production LLM observability and quality telemetry capability where Cardinality, End-to-end trace and Token work are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Cardinality: Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. End-to-end trace: Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. Token work: Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-09

- [ ] **Question:** Design a production LLM observability and quality telemetry capability where Cost, TTFT and Goodput are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — Goodput](README.md#goodput)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Cost: Provider charges and allocated accelerator time join tenant/project/release without exposing content. TTFT: Time to first token separates admission/queue/prefill from ongoing decode latency. Goodput: Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production LLM observability and quality telemetry capability where Telemetry reliability, Inter-token latency and Quality signal are first-class requirements.
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Telemetry reliability: Collector/exporter backlog, drops, sampling and schema/version changes are monitored. Inter-token latency: Streaming cadence reveals decode/runtime/network behavior hidden by total duration. Quality signal: Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM observability and quality telemetry options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving End-to-end trace. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — End-to-end trace](README.md#end-to-end-trace)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Gateway→retrieval→rerank→model→tool spans share safe request/release/tenant identifiers. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving TTFT. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — TTFT](README.md#ttft)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `otelcol validate --config otel-collector.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Time to first token separates admission/queue/prefill from ongoing decode latency. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Inter-token latency. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Inter-token latency](README.md#inter-token-latency)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n observability deploy/otel-collector --since=30m` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Streaming cadence reveals decode/runtime/network behavior hidden by total duration. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Token work. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Token work](README.md#token-work)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python scripts/trace_release.py --release RELEASE_ID` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Input/output/cache/tool/image work supports quota, capacity and cost attribution under tokenizer uncertainty. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Goodput. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Goodput](README.md#goodput)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Requests/tokens satisfying latency, quality and safety are more meaningful than raw GPU utilization. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Quality signal. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Quality signal](README.md#quality-signal)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `otelcol validate --config otel-collector.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Online deterministic checks, feedback and sampled evaluations connect infrastructure to user outcome. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Privacy. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Privacy](README.md#privacy)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n observability deploy/otel-collector --since=30m` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Prompts/responses/retrieved text/tool arguments default to excluded or redacted with controlled sampling/access/retention. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Cardinality. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Cardinality](README.md#cardinality)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python scripts/trace_release.py --release RELEASE_ID` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Model/release/route/status are bounded metric labels; request/user/document IDs belong in traces/logs. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Cost. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Cost](README.md#cost)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://LLM_GATEWAY/metrics | rg 'token|ttft|latency|cost|error'` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Provider charges and allocated accelerator time join tenant/project/release without exposing content. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-OBSERVABILITY-AND-QUALITY-TELEMETRY-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Telemetry reliability. How do you lead it end to end?
> **Covered in:** [LLM observability and quality telemetry — Telemetry reliability](README.md#telemetry-reliability)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `otelcol validate --config otel-collector.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Collector/exporter backlog, drops, sampling and schema/version changes are monitored. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM observability and quality telemetry from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: AgentOps and tool-using system operations](../21-agentops/README.md) · [Study note](README.md) · [Next: LLM safety, red teaming and guardrail operations →](../23-safety-redteam-guardrails/README.md)

<!-- reading-navigation:end -->
