# LLM fine-tuning, PEFT and adapter operations — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-01

- [ ] **Question:** What is Objective, and why does it matter in LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Objective](README.md#objective)

**Answer:** **Definition.** Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Objective matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM fine-tuning, PEFT and adapter operations journey rather than an administrative green status alone.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-02

- [ ] **Question:** What is Tokenizer/template, and why does it matter in LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Tokenizer/template](README.md#tokenizertemplate)

**Answer:** **Definition.** Chat format and special tokens must match training and serving or quality silently collapses. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Tokenizer/template matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM fine-tuning, PEFT and adapter operations journey rather than an administrative green status alone.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-03

- [ ] **Question:** What is PEFT/LoRA, and why does it matter in LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Definition.** Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which PEFT/LoRA matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM fine-tuning, PEFT and adapter operations journey rather than an administrative green status alone.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-04

- [ ] **Question:** What is QLoRA, and why does it matter in LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — QLoRA](README.md#qlora)

**Answer:** **Definition.** Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which QLoRA matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM fine-tuning, PEFT and adapter operations journey rather than an administrative green status alone.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-05

- [ ] **Question:** What is Dataset formatting, and why does it matter in LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Dataset formatting](README.md#dataset-formatting)

**Answer:** **Definition.** Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Dataset formatting matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM fine-tuning, PEFT and adapter operations journey rather than an administrative green status alone.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-06

- [ ] **Question:** What is Packing, and why does it matter in LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Packing](README.md#packing)

**Answer:** **Definition.** Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Packing matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM fine-tuning, PEFT and adapter operations journey rather than an administrative green status alone.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-07

- [ ] **Question:** What is Checkpoint, and why does it matter in LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Checkpoint](README.md#checkpoint)

**Answer:** **Definition.** Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Checkpoint matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM fine-tuning, PEFT and adapter operations journey rather than an administrative green status alone.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-08

- [ ] **Code:** **Question:** What does `python eval_adapter.py --base BASE_MODEL --adapter output/adapter` help you verify for LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `python eval_adapter.py --base BASE_MODEL --adapter output/adapter` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant LLM fine-tuning, PEFT and adapter operations concept is Merge versus dynamic adapter: Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM fine-tuning, PEFT and adapter operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-09

- [ ] **Code:** **Question:** What does `python -m pip show peft transformers accelerate` help you verify for LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `python -m pip show peft transformers accelerate` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant LLM fine-tuning, PEFT and adapter operations concept is Evaluation: Task/safety/memorization and base-capability regression compare adapter with base/champion. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM fine-tuning, PEFT and adapter operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JN-10

- [ ] **Code:** **Question:** What does `accelerate config` help you verify for LLM fine-tuning, PEFT and adapter operations?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `accelerate config` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant LLM fine-tuning, PEFT and adapter operations concept is Lineage/license: Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM fine-tuning, PEFT and adapter operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-01

- [ ] **Code:** **Question:** A basic Objective check fails. What would you do first using the CLI?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Objective](README.md#objective)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pip show peft transformers accelerate`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-02

- [ ] **Question:** A basic Tokenizer/template check fails. What would you do first?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Tokenizer/template](README.md#tokenizertemplate)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `accelerate config`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Chat format and special tokens must match training and serving or quality silently collapses. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-03

- [ ] **Question:** A basic PEFT/LoRA check fails. What would you do first?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python train_lora.py --config configs/smoke.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-04

- [ ] **Code:** **Question:** A basic QLoRA check fails. What would you do first using the CLI?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — QLoRA](README.md#qlora)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python eval_adapter.py --base BASE_MODEL --adapter output/adapter`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-05

- [ ] **Question:** A basic Dataset formatting check fails. What would you do first?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Dataset formatting](README.md#dataset-formatting)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pip show peft transformers accelerate`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-06

- [ ] **Question:** A basic Packing check fails. What would you do first?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Packing](README.md#packing)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `accelerate config`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-07

- [ ] **Code:** **Question:** A basic Checkpoint check fails. What would you do first using the CLI?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Checkpoint](README.md#checkpoint)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python train_lora.py --config configs/smoke.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-08

- [ ] **Question:** A basic Merge versus dynamic adapter check fails. What would you do first?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python eval_adapter.py --base BASE_MODEL --adapter output/adapter`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-09

- [ ] **Question:** A basic Evaluation check fails. What would you do first?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Evaluation](README.md#evaluation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pip show peft transformers accelerate`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Task/safety/memorization and base-capability regression compare adapter with base/champion. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-JP-10

- [ ] **Code:** **Question:** A basic Lineage/license check fails. What would you do first using the CLI?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Lineage/license](README.md#lineagelicense)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `accelerate config`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM fine-tuning, PEFT and adapter operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-01

- [ ] **Question:** Compare Objective with Tokenizer/template. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Objective](README.md#objective)

**Answer:** **Definitions.** Objective: Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. Tokenizer/template: Chat format and special tokens must match training and serving or quality silently collapses. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-02

- [ ] **Question:** Compare Tokenizer/template with PEFT/LoRA. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Tokenizer/template](README.md#tokenizertemplate)

**Answer:** **Definitions.** Tokenizer/template: Chat format and special tokens must match training and serving or quality silently collapses. PEFT/LoRA: Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-03

- [ ] **Question:** Compare PEFT/LoRA with QLoRA. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Definitions.** PEFT/LoRA: Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. QLoRA: Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-04

- [ ] **Configuration review:** **Question:** Compare QLoRA with Dataset formatting. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Dataset formatting](README.md#dataset-formatting)

**Answer:** **Definitions.** QLoRA: Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. Dataset formatting: Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-05

- [ ] **Question:** Compare Dataset formatting with Packing. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Dataset formatting](README.md#dataset-formatting)

**Answer:** **Definitions.** Dataset formatting: Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. Packing: Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-06

- [ ] **Question:** Compare Packing with Checkpoint. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Packing](README.md#packing)

**Answer:** **Definitions.** Packing: Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. Checkpoint: Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-07

- [ ] **Configuration review:** **Question:** Compare Checkpoint with Merge versus dynamic adapter. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Definitions.** Checkpoint: Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. Merge versus dynamic adapter: Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-08

- [ ] **Question:** Compare Merge versus dynamic adapter with Evaluation. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Definitions.** Merge versus dynamic adapter: Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. Evaluation: Task/safety/memorization and base-capability regression compare adapter with base/champion. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-09

- [ ] **Question:** Compare Evaluation with Lineage/license. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Evaluation](README.md#evaluation)

**Answer:** **Definitions.** Evaluation: Task/safety/memorization and base-capability regression compare adapter with base/champion. Lineage/license: Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MN-10

- [ ] **Configuration review:** **Question:** Compare Lineage/license with Objective. When would each change your design?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Objective](README.md#objective)

**Answer:** **Definitions.** Lineage/license: Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. Objective: Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. **Mechanism.** In LLM fine-tuning, PEFT and adapter operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM fine-tuning, PEFT and adapter operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Objective; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Objective](README.md#objective)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pip show peft transformers accelerate` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-02

- [ ] **Question:** Production is degraded around Tokenizer/template; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Tokenizer/template](README.md#tokenizertemplate)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `accelerate config` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Chat format and special tokens must match training and serving or quality silently collapses. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around PEFT/LoRA; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python train_lora.py --config configs/smoke.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-04

- [ ] **Question:** Production is degraded around QLoRA; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — QLoRA](README.md#qlora)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python eval_adapter.py --base BASE_MODEL --adapter output/adapter` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Dataset formatting; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Dataset formatting](README.md#dataset-formatting)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pip show peft transformers accelerate` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-06

- [ ] **Question:** Production is degraded around Packing; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Packing](README.md#packing)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `accelerate config` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Checkpoint; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Checkpoint](README.md#checkpoint)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python train_lora.py --config configs/smoke.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-08

- [ ] **Question:** Production is degraded around Merge versus dynamic adapter; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python eval_adapter.py --base BASE_MODEL --adapter output/adapter` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Evaluation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Evaluation](README.md#evaluation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pip show peft transformers accelerate` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Task/safety/memorization and base-capability regression compare adapter with base/champion. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-MP-10

- [ ] **Question:** Production is degraded around Lineage/license; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Lineage/license](README.md#lineagelicense)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `accelerate config` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM fine-tuning, PEFT and adapter operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-01

- [ ] **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Objective, QLoRA and Checkpoint are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Objective](README.md#objective)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Objective: Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. QLoRA: Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. Checkpoint: Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Tokenizer/template, Dataset formatting and Merge versus dynamic adapter are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Tokenizer/template: Chat format and special tokens must match training and serving or quality silently collapses. Dataset formatting: Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. Merge versus dynamic adapter: Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-03

- [ ] **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where PEFT/LoRA, Packing and Evaluation are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** PEFT/LoRA: Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. Packing: Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. Evaluation: Task/safety/memorization and base-capability regression compare adapter with base/champion. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where QLoRA, Checkpoint and Lineage/license are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — QLoRA](README.md#qlora)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** QLoRA: Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. Checkpoint: Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. Lineage/license: Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-05

- [ ] **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Dataset formatting, Merge versus dynamic adapter and Objective are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Dataset formatting: Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. Merge versus dynamic adapter: Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. Objective: Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Packing, Evaluation and Tokenizer/template are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Tokenizer/template](README.md#tokenizertemplate)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Packing: Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. Evaluation: Task/safety/memorization and base-capability regression compare adapter with base/champion. Tokenizer/template: Chat format and special tokens must match training and serving or quality silently collapses. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-07

- [ ] **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Checkpoint, Lineage/license and PEFT/LoRA are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Checkpoint: Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. Lineage/license: Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. PEFT/LoRA: Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Merge versus dynamic adapter, Objective and QLoRA are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Merge versus dynamic adapter: Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. Objective: Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. QLoRA: Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-09

- [ ] **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Evaluation, Tokenizer/template and Dataset formatting are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Dataset formatting](README.md#dataset-formatting)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Evaluation: Task/safety/memorization and base-capability regression compare adapter with base/champion. Tokenizer/template: Chat format and special tokens must match training and serving or quality silently collapses. Dataset formatting: Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production LLM fine-tuning, PEFT and adapter operations capability where Lineage/license, PEFT/LoRA and Packing are first-class requirements.
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Lineage/license: Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. PEFT/LoRA: Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. Packing: Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM fine-tuning, PEFT and adapter operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Objective. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Objective](README.md#objective)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pip show peft transformers accelerate` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Continued pretraining, SFT, reward modeling and preference optimization solve different problems/data contracts. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Tokenizer/template. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Tokenizer/template](README.md#tokenizertemplate)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `accelerate config` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Chat format and special tokens must match training and serving or quality silently collapses. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving PEFT/LoRA. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — PEFT/LoRA](README.md#peftlora)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python train_lora.py --config configs/smoke.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Low-rank adapter parameters reduce trainable state but introduce base-model/rank/target-module compatibility. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving QLoRA. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — QLoRA](README.md#qlora)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python eval_adapter.py --base BASE_MODEL --adapter output/adapter` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Quantized base weights save memory while compute dtype, quantization config and optimizer affect stability. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Dataset formatting. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Dataset formatting](README.md#dataset-formatting)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pip show peft transformers accelerate` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Prompt/completion boundaries, masks, truncation, deduplication and contamination define learning signal. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Packing. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Packing](README.md#packing)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `accelerate config` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Combines short examples efficiently but requires correct loss masks and sequence-boundary handling. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Checkpoint. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Checkpoint](README.md#checkpoint)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python train_lora.py --config configs/smoke.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Adapter, optimizer, scheduler, RNG, trainer/data progress and config support restart/reproduction. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Merge versus dynamic adapter. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Merge versus dynamic adapter](README.md#merge-versus-dynamic-adapter)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python eval_adapter.py --base BASE_MODEL --adapter output/adapter` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Merged weights simplify serving while dynamic adapters improve reuse at isolation/cache complexity. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Evaluation. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Evaluation](README.md#evaluation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pip show peft transformers accelerate` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Task/safety/memorization and base-capability regression compare adapter with base/champion. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-FINE-TUNING-PEFT-AND-ADAPTER-OPERATIONS-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Lineage/license. How do you lead it end to end?
> **Covered in:** [LLM fine-tuning, PEFT and adapter operations — Lineage/license](README.md#lineagelicense)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `accelerate config` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Base model/data/adapter licenses, consent, provenance and intended-use constraints travel with release. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM fine-tuning, PEFT and adapter operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: PromptOps: prompt, template and policy lifecycle](../16-promptops/README.md) · [Study note](README.md) · [Next: RAGOps and index lifecycle →](../18-ragops-index-lifecycle/README.md)

<!-- reading-navigation:end -->
