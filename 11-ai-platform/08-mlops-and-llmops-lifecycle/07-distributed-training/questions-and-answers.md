# Distributed training and checkpoint recovery — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-01

- [ ] **Question:** What is Data parallel, and why does it matter in Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — Data parallel](README.md#data-parallel)

**Answer:** **Definition.** Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Data parallel matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Distributed training and checkpoint recovery journey rather than an administrative green status alone.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-02

- [ ] **Question:** What is DDP, and why does it matter in Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — DDP](README.md#ddp)

**Answer:** **Definition.** One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which DDP matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Distributed training and checkpoint recovery journey rather than an administrative green status alone.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-03

- [ ] **Question:** What is FSDP/ZeRO, and why does it matter in Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — FSDP/ZeRO](README.md#fsdpzero)

**Answer:** **Definition.** Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which FSDP/ZeRO matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Distributed training and checkpoint recovery journey rather than an administrative green status alone.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-04

- [ ] **Question:** What is Tensor parallel, and why does it matter in Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Definition.** Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Tensor parallel matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Distributed training and checkpoint recovery journey rather than an administrative green status alone.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-05

- [ ] **Question:** What is Pipeline parallel, and why does it matter in Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — Pipeline parallel](README.md#pipeline-parallel)

**Answer:** **Definition.** Partitions layers into stages with microbatches, bubbles and failure coordination. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Pipeline parallel matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Distributed training and checkpoint recovery journey rather than an administrative green status alone.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-06

- [ ] **Question:** What is NCCL collectives, and why does it matter in Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Definition.** Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which NCCL collectives matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Distributed training and checkpoint recovery journey rather than an administrative green status alone.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-07

- [ ] **Question:** What is Elasticity, and why does it matter in Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — Elasticity](README.md#elasticity)

**Answer:** **Definition.** Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Elasticity matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Distributed training and checkpoint recovery journey rather than an administrative green status alone.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-08

- [ ] **Code:** **Question:** What does `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix` help you verify for Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant Distributed training and checkpoint recovery concept is Distributed checkpoint: Sharded state needs a committed manifest and compatible reshard/load procedure. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Distributed training and checkpoint recovery rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-09

- [ ] **Code:** **Question:** What does `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` help you verify for Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Distributed training and checkpoint recovery concept is Straggler: One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Distributed training and checkpoint recovery rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JN-10

- [ ] **Code:** **Question:** What does `nvidia-smi topo -m` help you verify for Distributed training and checkpoint recovery?
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Purpose.** `nvidia-smi topo -m` shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. **Mechanism.** The relevant Distributed training and checkpoint recovery concept is Reproducibility: Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Distributed training and checkpoint recovery rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-01

- [ ] **Code:** **Question:** A basic Data parallel check fails. What would you do first using the CLI?
> **Covered in:** [Distributed training and checkpoint recovery — Data parallel](README.md#data-parallel)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `torchrun --standalone --nproc-per-node=2 train.py --smoke-test`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-02

- [ ] **Question:** A basic DDP check fails. What would you do first?
> **Covered in:** [Distributed training and checkpoint recovery — DDP](README.md#ddp)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `nvidia-smi topo -m`. This shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-03

- [ ] **Question:** A basic FSDP/ZeRO check fails. What would you do first?
> **Covered in:** [Distributed training and checkpoint recovery — FSDP/ZeRO](README.md#fsdpzero)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `NCCL_DEBUG=INFO torchrun --nproc-per-node=2 diagnose.py`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-04

- [ ] **Code:** **Question:** A basic Tensor parallel check fails. What would you do first using the CLI?
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-05

- [ ] **Question:** A basic Pipeline parallel check fails. What would you do first?
> **Covered in:** [Distributed training and checkpoint recovery — Pipeline parallel](README.md#pipeline-parallel)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `torchrun --standalone --nproc-per-node=2 train.py --smoke-test`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Partitions layers into stages with microbatches, bubbles and failure coordination. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-06

- [ ] **Question:** A basic NCCL collectives check fails. What would you do first?
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `nvidia-smi topo -m`. This shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-07

- [ ] **Code:** **Question:** A basic Elasticity check fails. What would you do first using the CLI?
> **Covered in:** [Distributed training and checkpoint recovery — Elasticity](README.md#elasticity)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `NCCL_DEBUG=INFO torchrun --nproc-per-node=2 diagnose.py`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-08

- [ ] **Question:** A basic Distributed checkpoint check fails. What would you do first?
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Sharded state needs a committed manifest and compatible reshard/load procedure. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-09

- [ ] **Question:** A basic Straggler check fails. What would you do first?
> **Covered in:** [Distributed training and checkpoint recovery — Straggler](README.md#straggler)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `torchrun --standalone --nproc-per-node=2 train.py --smoke-test`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-JP-10

- [ ] **Code:** **Question:** A basic Reproducibility check fails. What would you do first using the CLI?
> **Covered in:** [Distributed training and checkpoint recovery — Reproducibility](README.md#reproducibility)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `nvidia-smi topo -m`. This shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Distributed training and checkpoint recovery client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-01

- [ ] **Question:** Compare Data parallel with DDP. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — Data parallel](README.md#data-parallel)

**Answer:** **Definitions.** Data parallel: Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. DDP: One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-02

- [ ] **Question:** Compare DDP with FSDP/ZeRO. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — FSDP/ZeRO](README.md#fsdpzero)

**Answer:** **Definitions.** DDP: One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. FSDP/ZeRO: Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-03

- [ ] **Question:** Compare FSDP/ZeRO with Tensor parallel. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Definitions.** FSDP/ZeRO: Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. Tensor parallel: Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-04

- [ ] **Configuration review:** **Question:** Compare Tensor parallel with Pipeline parallel. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Definitions.** Tensor parallel: Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. Pipeline parallel: Partitions layers into stages with microbatches, bubbles and failure coordination. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-05

- [ ] **Question:** Compare Pipeline parallel with NCCL collectives. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Definitions.** Pipeline parallel: Partitions layers into stages with microbatches, bubbles and failure coordination. NCCL collectives: Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-06

- [ ] **Question:** Compare NCCL collectives with Elasticity. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Definitions.** NCCL collectives: Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. Elasticity: Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-07

- [ ] **Configuration review:** **Question:** Compare Elasticity with Distributed checkpoint. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Definitions.** Elasticity: Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. Distributed checkpoint: Sharded state needs a committed manifest and compatible reshard/load procedure. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-08

- [ ] **Question:** Compare Distributed checkpoint with Straggler. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Definitions.** Distributed checkpoint: Sharded state needs a committed manifest and compatible reshard/load procedure. Straggler: One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-09

- [ ] **Question:** Compare Straggler with Reproducibility. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — Straggler](README.md#straggler)

**Answer:** **Definitions.** Straggler: One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. Reproducibility: Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MN-10

- [ ] **Configuration review:** **Question:** Compare Reproducibility with Data parallel. When would each change your design?
> **Covered in:** [Distributed training and checkpoint recovery — Data parallel](README.md#data-parallel)

**Answer:** **Definitions.** Reproducibility: Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. Data parallel: Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. **Mechanism.** In Distributed training and checkpoint recovery, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Distributed training and checkpoint recovery design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Data parallel; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — Data parallel](README.md#data-parallel)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-02

- [ ] **Question:** Production is degraded around DDP; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — DDP](README.md#ddp)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `nvidia-smi topo -m` plus recent changes and the user-facing SLI. The command shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around FSDP/ZeRO; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — FSDP/ZeRO](README.md#fsdpzero)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `NCCL_DEBUG=INFO torchrun --nproc-per-node=2 diagnose.py` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-04

- [ ] **Question:** Production is degraded around Tensor parallel; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Pipeline parallel; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — Pipeline parallel](README.md#pipeline-parallel)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Partitions layers into stages with microbatches, bubbles and failure coordination. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-06

- [ ] **Question:** Production is degraded around NCCL collectives; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `nvidia-smi topo -m` plus recent changes and the user-facing SLI. The command shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Elasticity; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — Elasticity](README.md#elasticity)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `NCCL_DEBUG=INFO torchrun --nproc-per-node=2 diagnose.py` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-08

- [ ] **Question:** Production is degraded around Distributed checkpoint; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Sharded state needs a committed manifest and compatible reshard/load procedure. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Straggler; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — Straggler](README.md#straggler)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-MP-10

- [ ] **Question:** Production is degraded around Reproducibility; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Distributed training and checkpoint recovery — Reproducibility](README.md#reproducibility)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `nvidia-smi topo -m` plus recent changes and the user-facing SLI. The command shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Distributed training and checkpoint recovery source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-01

- [ ] **Question:** Design a production Distributed training and checkpoint recovery capability where Data parallel, Tensor parallel and Elasticity are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Data parallel: Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. Tensor parallel: Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. Elasticity: Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Distributed training and checkpoint recovery capability where DDP, Pipeline parallel and Distributed checkpoint are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** DDP: One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. Pipeline parallel: Partitions layers into stages with microbatches, bubbles and failure coordination. Distributed checkpoint: Sharded state needs a committed manifest and compatible reshard/load procedure. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-03

- [ ] **Question:** Design a production Distributed training and checkpoint recovery capability where FSDP/ZeRO, NCCL collectives and Straggler are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** FSDP/ZeRO: Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. NCCL collectives: Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. Straggler: One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Distributed training and checkpoint recovery capability where Tensor parallel, Elasticity and Reproducibility are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Tensor parallel: Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. Elasticity: Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. Reproducibility: Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-05

- [ ] **Question:** Design a production Distributed training and checkpoint recovery capability where Pipeline parallel, Distributed checkpoint and Data parallel are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Pipeline parallel: Partitions layers into stages with microbatches, bubbles and failure coordination. Distributed checkpoint: Sharded state needs a committed manifest and compatible reshard/load procedure. Data parallel: Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Distributed training and checkpoint recovery capability where NCCL collectives, Straggler and DDP are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** NCCL collectives: Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. Straggler: One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. DDP: One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-07

- [ ] **Question:** Design a production Distributed training and checkpoint recovery capability where Elasticity, Reproducibility and FSDP/ZeRO are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — FSDP/ZeRO](README.md#fsdpzero)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Elasticity: Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. Reproducibility: Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. FSDP/ZeRO: Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Distributed training and checkpoint recovery capability where Distributed checkpoint, Data parallel and Tensor parallel are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Distributed checkpoint: Sharded state needs a committed manifest and compatible reshard/load procedure. Data parallel: Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. Tensor parallel: Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-09

- [ ] **Question:** Design a production Distributed training and checkpoint recovery capability where Straggler, DDP and Pipeline parallel are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — Pipeline parallel](README.md#pipeline-parallel)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Straggler: One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. DDP: One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. Pipeline parallel: Partitions layers into stages with microbatches, bubbles and failure coordination. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Distributed training and checkpoint recovery capability where Reproducibility, FSDP/ZeRO and NCCL collectives are first-class requirements.
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Reproducibility: Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. FSDP/ZeRO: Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. NCCL collectives: Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Distributed training and checkpoint recovery options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Data parallel. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — Data parallel](README.md#data-parallel)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Replicas process different batches and synchronize gradients; global batch and optimizer behavior change with world size. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving DDP. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — DDP](README.md#ddp)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `nvidia-smi topo -m` as one checkpoint because it shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** One process per GPU and collective gradient reduction require rank/world/rendezvous correctness. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving FSDP/ZeRO. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — FSDP/ZeRO](README.md#fsdpzero)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `NCCL_DEBUG=INFO torchrun --nproc-per-node=2 diagnose.py` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Shard parameters, gradients and optimizer state to trade communication/complexity for memory scale. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Tensor parallel. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — Tensor parallel](README.md#tensor-parallel)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Partitions matrix operations within a layer and benefits from high-bandwidth low-latency topology. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Pipeline parallel. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — Pipeline parallel](README.md#pipeline-parallel)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Partitions layers into stages with microbatches, bubbles and failure coordination. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving NCCL collectives. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — NCCL collectives](README.md#nccl-collectives)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `nvidia-smi topo -m` as one checkpoint because it shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Ring/tree algorithms depend on rank, interface, RDMA/EFA, topology and version compatibility. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Elasticity. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — Elasticity](README.md#elasticity)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `NCCL_DEBUG=INFO torchrun --nproc-per-node=2 diagnose.py` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Worker loss/restart changes membership only when algorithm/checkpoint semantics support it. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Distributed checkpoint. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — Distributed checkpoint](README.md#distributed-checkpoint)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n NS -l job-name=TRAINING_JOB --prefix` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Sharded state needs a committed manifest and compatible reshard/load procedure. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Straggler. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — Straggler](README.md#straggler)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `torchrun --standalone --nproc-per-node=2 train.py --smoke-test` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** One slow GPU/network/data worker holds synchronous progress and can hide behind average utilization. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### DISTRIBUTED-TRAINING-AND-CHECKPOINT-RECOVERY-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Reproducibility. How do you lead it end to end?
> **Covered in:** [Distributed training and checkpoint recovery — Reproducibility](README.md#reproducibility)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `nvidia-smi topo -m` as one checkpoint because it shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Seeds alone do not eliminate nondeterministic kernels, asynchronous order or topology differences. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Distributed training and checkpoint recovery from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Training platform operations](../06-training-platform/README.md) · [Study note](README.md) · [Next: Hyperparameter tuning and experiment search →](../08-hyperparameter-tuning/README.md)

<!-- reading-navigation:end -->
