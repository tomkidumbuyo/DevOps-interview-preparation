# AI supply chain, signing and provenance — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-01

- [ ] **Question:** What is Source provenance, and why does it matter in AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Definition.** Repository/revision/review and build identity connect declared source to artifact. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Source provenance matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AI supply chain, signing and provenance journey rather than an administrative green status alone.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-02

- [ ] **Question:** What is Dependency lock/SBOM, and why does it matter in AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Dependency lock/SBOM](README.md#dependency-locksbom)

**Answer:** **Definition.** Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Dependency lock/SBOM matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AI supply chain, signing and provenance journey rather than an administrative green status alone.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-03

- [ ] **Question:** What is Model provenance, and why does it matter in AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Definition.** Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Model provenance matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AI supply chain, signing and provenance journey rather than an administrative green status alone.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-04

- [ ] **Question:** What is Unsafe serialization, and why does it matter in AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Unsafe serialization](README.md#unsafe-serialization)

**Answer:** **Definition.** Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Unsafe serialization matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AI supply chain, signing and provenance journey rather than an administrative green status alone.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-05

- [ ] **Question:** What is OCI artifact, and why does it matter in AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — OCI artifact](README.md#oci-artifact)

**Answer:** **Definition.** Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which OCI artifact matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AI supply chain, signing and provenance journey rather than an administrative green status alone.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-06

- [ ] **Question:** What is Signature, and why does it matter in AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Signature](README.md#signature)

**Answer:** **Definition.** Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. **Mechanism.** A real request crosses several independent states: name resolution returns an address, the source selects a route and source address, link or overlay forwarding reaches the next hop, stateful or stateless policy evaluates the flow, transport establishes communication, and TLS/application protocols negotiate their own contract. The return path must also work. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence progresses layer by layer: correct name/address, expected route, permitted flow, listening endpoint, successful handshake and valid application response. Timeouts, refusals, resets and protocol errors mean different layers; packet loss, MTU, asymmetric paths, connection-state exhaustion and proxy timeout mismatch are common production failures. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Signature matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AI supply chain, signing and provenance journey rather than an administrative green status alone.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-07

- [ ] **Question:** What is Attestation, and why does it matter in AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Attestation](README.md#attestation)

**Answer:** **Definition.** Predicate records build/training/evaluation facts with verifiable producer identity and inputs. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Attestation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AI supply chain, signing and provenance journey rather than an administrative green status alone.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-08

- [ ] **Code:** **Question:** What does `cosign verify-attestation --type slsaprovenance MODEL_REF` help you verify for AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Attestation](README.md#attestation)

**Answer:** **Purpose.** `cosign verify-attestation --type slsaprovenance MODEL_REF` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant AI supply chain, signing and provenance concept is Registry policy: Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a AI supply chain, signing and provenance rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-09

- [ ] **Code:** **Question:** What does `syft IMAGE -o cyclonedx-json` help you verify for AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `syft IMAGE -o cyclonedx-json` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant AI supply chain, signing and provenance concept is Release verification: Admission checks exact digest, signature/attestation, policy and environment compatibility. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a AI supply chain, signing and provenance rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JN-10

- [ ] **Code:** **Question:** What does `oras manifest fetch MODEL_REF` help you verify for AI supply chain, signing and provenance?
> **Covered in:** [AI supply chain, signing and provenance — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `oras manifest fetch MODEL_REF` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant AI supply chain, signing and provenance concept is Revocation/response: Inventory maps compromised dependency/model to runs, releases and deployments for containment. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a AI supply chain, signing and provenance rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-01

- [ ] **Code:** **Question:** A basic Source provenance check fails. What would you do first using the CLI?
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `syft IMAGE -o cyclonedx-json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Repository/revision/review and build identity connect declared source to artifact. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-02

- [ ] **Question:** A basic Dependency lock/SBOM check fails. What would you do first?
> **Covered in:** [AI supply chain, signing and provenance — Dependency lock/SBOM](README.md#dependency-locksbom)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `oras manifest fetch MODEL_REF`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-03

- [ ] **Question:** A basic Model provenance check fails. What would you do first?
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `cosign verify MODEL_REF`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-04

- [ ] **Code:** **Question:** A basic Unsafe serialization check fails. What would you do first using the CLI?
> **Covered in:** [AI supply chain, signing and provenance — Unsafe serialization](README.md#unsafe-serialization)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `cosign verify-attestation --type slsaprovenance MODEL_REF`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-05

- [ ] **Question:** A basic OCI artifact check fails. What would you do first?
> **Covered in:** [AI supply chain, signing and provenance — OCI artifact](README.md#oci-artifact)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `syft IMAGE -o cyclonedx-json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-06

- [ ] **Question:** A basic Signature check fails. What would you do first?
> **Covered in:** [AI supply chain, signing and provenance — Signature](README.md#signature)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `oras manifest fetch MODEL_REF`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-07

- [ ] **Code:** **Question:** A basic Attestation check fails. What would you do first using the CLI?
> **Covered in:** [AI supply chain, signing and provenance — Attestation](README.md#attestation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `cosign verify MODEL_REF`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Predicate records build/training/evaluation facts with verifiable producer identity and inputs. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-08

- [ ] **Question:** A basic Registry policy check fails. What would you do first?
> **Covered in:** [AI supply chain, signing and provenance — Registry policy](README.md#registry-policy)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `cosign verify-attestation --type slsaprovenance MODEL_REF`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-09

- [ ] **Question:** A basic Release verification check fails. What would you do first?
> **Covered in:** [AI supply chain, signing and provenance — Release verification](README.md#release-verification)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `syft IMAGE -o cyclonedx-json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Admission checks exact digest, signature/attestation, policy and environment compatibility. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-JP-10

- [ ] **Code:** **Question:** A basic Revocation/response check fails. What would you do first using the CLI?
> **Covered in:** [AI supply chain, signing and provenance — Revocation/response](README.md#revocationresponse)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `oras manifest fetch MODEL_REF`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Inventory maps compromised dependency/model to runs, releases and deployments for containment. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AI supply chain, signing and provenance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-01

- [ ] **Question:** Compare Source provenance with Dependency lock/SBOM. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Definitions.** Source provenance: Repository/revision/review and build identity connect declared source to artifact. Dependency lock/SBOM: Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-02

- [ ] **Question:** Compare Dependency lock/SBOM with Model provenance. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Dependency lock/SBOM](README.md#dependency-locksbom)

**Answer:** **Definitions.** Dependency lock/SBOM: Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. Model provenance: Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-03

- [ ] **Question:** Compare Model provenance with Unsafe serialization. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Definitions.** Model provenance: Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. Unsafe serialization: Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-04

- [ ] **Configuration review:** **Question:** Compare Unsafe serialization with OCI artifact. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Unsafe serialization](README.md#unsafe-serialization)

**Answer:** **Definitions.** Unsafe serialization: Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. OCI artifact: Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-05

- [ ] **Question:** Compare OCI artifact with Signature. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — OCI artifact](README.md#oci-artifact)

**Answer:** **Definitions.** OCI artifact: Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. Signature: Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-06

- [ ] **Question:** Compare Signature with Attestation. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Attestation](README.md#attestation)

**Answer:** **Definitions.** Signature: Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. Attestation: Predicate records build/training/evaluation facts with verifiable producer identity and inputs. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-07

- [ ] **Configuration review:** **Question:** Compare Attestation with Registry policy. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Registry policy](README.md#registry-policy)

**Answer:** **Definitions.** Attestation: Predicate records build/training/evaluation facts with verifiable producer identity and inputs. Registry policy: Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-08

- [ ] **Question:** Compare Registry policy with Release verification. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Release verification](README.md#release-verification)

**Answer:** **Definitions.** Registry policy: Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. Release verification: Admission checks exact digest, signature/attestation, policy and environment compatibility. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-09

- [ ] **Question:** Compare Release verification with Revocation/response. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Release verification](README.md#release-verification)

**Answer:** **Definitions.** Release verification: Admission checks exact digest, signature/attestation, policy and environment compatibility. Revocation/response: Inventory maps compromised dependency/model to runs, releases and deployments for containment. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MN-10

- [ ] **Configuration review:** **Question:** Compare Revocation/response with Source provenance. When would each change your design?
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Definitions.** Revocation/response: Inventory maps compromised dependency/model to runs, releases and deployments for containment. Source provenance: Repository/revision/review and build identity connect declared source to artifact. **Mechanism.** In AI supply chain, signing and provenance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AI supply chain, signing and provenance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Source provenance; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `syft IMAGE -o cyclonedx-json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Repository/revision/review and build identity connect declared source to artifact. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-02

- [ ] **Question:** Production is degraded around Dependency lock/SBOM; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Dependency lock/SBOM](README.md#dependency-locksbom)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `oras manifest fetch MODEL_REF` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Model provenance; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `cosign verify MODEL_REF` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-04

- [ ] **Question:** Production is degraded around Unsafe serialization; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Unsafe serialization](README.md#unsafe-serialization)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `cosign verify-attestation --type slsaprovenance MODEL_REF` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around OCI artifact; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — OCI artifact](README.md#oci-artifact)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `syft IMAGE -o cyclonedx-json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-06

- [ ] **Question:** Production is degraded around Signature; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Signature](README.md#signature)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `oras manifest fetch MODEL_REF` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Attestation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Attestation](README.md#attestation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `cosign verify MODEL_REF` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Predicate records build/training/evaluation facts with verifiable producer identity and inputs. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-08

- [ ] **Question:** Production is degraded around Registry policy; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Registry policy](README.md#registry-policy)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `cosign verify-attestation --type slsaprovenance MODEL_REF` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Release verification; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Release verification](README.md#release-verification)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `syft IMAGE -o cyclonedx-json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Admission checks exact digest, signature/attestation, policy and environment compatibility. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-MP-10

- [ ] **Question:** Production is degraded around Revocation/response; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI supply chain, signing and provenance — Revocation/response](README.md#revocationresponse)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `oras manifest fetch MODEL_REF` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Inventory maps compromised dependency/model to runs, releases and deployments for containment. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AI supply chain, signing and provenance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-01

- [ ] **Question:** Design a production AI supply chain, signing and provenance capability where Source provenance, Unsafe serialization and Attestation are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Source provenance: Repository/revision/review and build identity connect declared source to artifact. Unsafe serialization: Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. Attestation: Predicate records build/training/evaluation facts with verifiable producer identity and inputs. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production AI supply chain, signing and provenance capability where Dependency lock/SBOM, OCI artifact and Registry policy are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Dependency lock/SBOM](README.md#dependency-locksbom)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Dependency lock/SBOM: Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. OCI artifact: Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. Registry policy: Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-03

- [ ] **Question:** Design a production AI supply chain, signing and provenance capability where Model provenance, Signature and Release verification are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Model provenance: Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. Signature: Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. Release verification: Admission checks exact digest, signature/attestation, policy and environment compatibility. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production AI supply chain, signing and provenance capability where Unsafe serialization, Attestation and Revocation/response are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Unsafe serialization](README.md#unsafe-serialization)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Unsafe serialization: Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. Attestation: Predicate records build/training/evaluation facts with verifiable producer identity and inputs. Revocation/response: Inventory maps compromised dependency/model to runs, releases and deployments for containment. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-05

- [ ] **Question:** Design a production AI supply chain, signing and provenance capability where OCI artifact, Registry policy and Source provenance are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** OCI artifact: Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. Registry policy: Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. Source provenance: Repository/revision/review and build identity connect declared source to artifact. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production AI supply chain, signing and provenance capability where Signature, Release verification and Dependency lock/SBOM are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Dependency lock/SBOM](README.md#dependency-locksbom)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Signature: Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. Release verification: Admission checks exact digest, signature/attestation, policy and environment compatibility. Dependency lock/SBOM: Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-07

- [ ] **Question:** Design a production AI supply chain, signing and provenance capability where Attestation, Revocation/response and Model provenance are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Attestation: Predicate records build/training/evaluation facts with verifiable producer identity and inputs. Revocation/response: Inventory maps compromised dependency/model to runs, releases and deployments for containment. Model provenance: Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production AI supply chain, signing and provenance capability where Registry policy, Source provenance and Unsafe serialization are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Registry policy: Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. Source provenance: Repository/revision/review and build identity connect declared source to artifact. Unsafe serialization: Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-09

- [ ] **Question:** Design a production AI supply chain, signing and provenance capability where Release verification, Dependency lock/SBOM and OCI artifact are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Release verification](README.md#release-verification)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Release verification: Admission checks exact digest, signature/attestation, policy and environment compatibility. Dependency lock/SBOM: Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. OCI artifact: Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production AI supply chain, signing and provenance capability where Revocation/response, Model provenance and Signature are first-class requirements.
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Revocation/response: Inventory maps compromised dependency/model to runs, releases and deployments for containment. Model provenance: Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. Signature: Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AI supply chain, signing and provenance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Source provenance. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Source provenance](README.md#source-provenance)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `syft IMAGE -o cyclonedx-json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Repository/revision/review and build identity connect declared source to artifact. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Dependency lock/SBOM. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Dependency lock/SBOM](README.md#dependency-locksbom)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `oras manifest fetch MODEL_REF` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Software names/versions/hashes/licenses/vulnerabilities cover training and serving environments. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Model provenance. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Model provenance](README.md#model-provenance)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `cosign verify MODEL_REF` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Origin, base, fine-tuning data/process, transforms and digest establish lineage beyond filename. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Unsafe serialization. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Unsafe serialization](README.md#unsafe-serialization)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `cosign verify-attestation --type slsaprovenance MODEL_REF` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Pickle-like formats can execute code; prefer safe formats and isolated inspection for untrusted models. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving OCI artifact. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — OCI artifact](README.md#oci-artifact)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `syft IMAGE -o cyclonedx-json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Media types/manifests/subject/referrers store models, SBOMs, signatures and attestations in registries. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Signature. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Signature](README.md#signature)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `oras manifest fetch MODEL_REF` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Verifies signer/keyless identity and bytes under trust policy, not model quality or authorization alone. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Attestation. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Attestation](README.md#attestation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `cosign verify MODEL_REF` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Predicate records build/training/evaluation facts with verifiable producer identity and inputs. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Registry policy. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Registry policy](README.md#registry-policy)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `cosign verify-attestation --type slsaprovenance MODEL_REF` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Immutability, retention, replication, vulnerability/malware scanning and access logs protect promotion. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Release verification. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Release verification](README.md#release-verification)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `syft IMAGE -o cyclonedx-json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Admission checks exact digest, signature/attestation, policy and environment compatibility. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AI-SUPPLY-CHAIN-SIGNING-AND-PROVENANCE-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Revocation/response. How do you lead it end to end?
> **Covered in:** [AI supply chain, signing and provenance — Revocation/response](README.md#revocationresponse)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `oras manifest fetch MODEL_REF` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Inventory maps compromised dependency/model to runs, releases and deployments for containment. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AI supply chain, signing and provenance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: AI governance, approval and evidence automation](../24-governance-approval-evidence/README.md) · [Study note](README.md) · [Next: MLOps/LLMOps FinOps and capacity →](../26-ai-finops-capacity/README.md)

<!-- reading-navigation:end -->
