# LLM safety, red teaming and guardrail operations — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-01

- [ ] **Question:** What is Threat model, and why does it matter in LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Definition.** Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Threat model matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM safety, red teaming and guardrail operations journey rather than an administrative green status alone.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-02

- [ ] **Question:** What is Policy taxonomy, and why does it matter in LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Policy taxonomy](README.md#policy-taxonomy)

**Answer:** **Definition.** Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Policy taxonomy matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM safety, red teaming and guardrail operations journey rather than an administrative green status alone.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-03

- [ ] **Question:** What is Pre-input control, and why does it matter in LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Definition.** Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Pre-input control matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM safety, red teaming and guardrail operations journey rather than an administrative green status alone.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-04

- [ ] **Question:** What is Prompt injection, and why does it matter in LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Prompt injection](README.md#prompt-injection)

**Answer:** **Definition.** Untrusted user/retrieved/tool content cannot grant system or tool authority. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Prompt injection matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM safety, red teaming and guardrail operations journey rather than an administrative green status alone.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-05

- [ ] **Question:** What is Output control, and why does it matter in LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Output control](README.md#output-control)

**Answer:** **Definition.** Validate structured data, content policy, citations and destinations before display or execution. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Output control matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM safety, red teaming and guardrail operations journey rather than an administrative green status alone.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-06

- [ ] **Question:** What is Tool guardrail, and why does it matter in LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Tool guardrail](README.md#tool-guardrail)

**Answer:** **Definition.** Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Tool guardrail matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM safety, red teaming and guardrail operations journey rather than an administrative green status alone.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-07

- [ ] **Question:** What is Red-team corpus, and why does it matter in LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Red-team corpus](README.md#red-team-corpus)

**Answer:** **Definition.** Versioned attack/edge cases record threat, expected behavior, severity and regression owner. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Red-team corpus matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original LLM safety, red teaming and guardrail operations journey rather than an administrative green status alone.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-08

- [ ] **Code:** **Question:** What does `kubectl logs -n ai-platform deploy/policy-engine --since=30m` help you verify for LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Operations](README.md#operations)

**Answer:** **Purpose.** `kubectl logs -n ai-platform deploy/policy-engine --since=30m` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant LLM safety, red teaming and guardrail operations concept is Adaptive testing: Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM safety, red teaming and guardrail operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-09

- [ ] **Code:** **Question:** What does `python -m pytest tests/guardrails -q` help you verify for LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Operations](README.md#operations)

**Answer:** **Purpose.** `python -m pytest tests/guardrails -q` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant LLM safety, red teaming and guardrail operations concept is Operations: False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM safety, red teaming and guardrail operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JN-10

- [ ] **Code:** **Question:** What does `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` help you verify for LLM safety, red teaming and guardrail operations?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Operations](README.md#operations)

**Answer:** **Purpose.** `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant LLM safety, red teaming and guardrail operations concept is Incident learning: Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a LLM safety, red teaming and guardrail operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-01

- [ ] **Code:** **Question:** A basic Threat model check fails. What would you do first using the CLI?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pytest tests/guardrails -q`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-02

- [ ] **Question:** A basic Policy taxonomy check fails. What would you do first?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Policy taxonomy](README.md#policy-taxonomy)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-03

- [ ] **Question:** A basic Pre-input control check fails. What would you do first?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `opa test policy/`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-04

- [ ] **Code:** **Question:** A basic Prompt injection check fails. What would you do first using the CLI?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Prompt injection](README.md#prompt-injection)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n ai-platform deploy/policy-engine --since=30m`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Untrusted user/retrieved/tool content cannot grant system or tool authority. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-05

- [ ] **Question:** A basic Output control check fails. What would you do first?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Output control](README.md#output-control)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pytest tests/guardrails -q`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Validate structured data, content policy, citations and destinations before display or execution. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-06

- [ ] **Question:** A basic Tool guardrail check fails. What would you do first?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Tool guardrail](README.md#tool-guardrail)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-07

- [ ] **Code:** **Question:** A basic Red-team corpus check fails. What would you do first using the CLI?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Red-team corpus](README.md#red-team-corpus)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `opa test policy/`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Versioned attack/edge cases record threat, expected behavior, severity and regression owner. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-08

- [ ] **Question:** A basic Adaptive testing check fails. What would you do first?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Adaptive testing](README.md#adaptive-testing)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl logs -n ai-platform deploy/policy-engine --since=30m`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-09

- [ ] **Question:** A basic Operations check fails. What would you do first?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Operations](README.md#operations)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pytest tests/guardrails -q`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-JP-10

- [ ] **Code:** **Question:** A basic Incident learning check fails. What would you do first using the CLI?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Incident learning](README.md#incident-learning)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original LLM safety, red teaming and guardrail operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-01

- [ ] **Question:** Compare Threat model with Policy taxonomy. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Definitions.** Threat model: Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. Policy taxonomy: Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-02

- [ ] **Question:** Compare Policy taxonomy with Pre-input control. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Definitions.** Policy taxonomy: Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. Pre-input control: Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-03

- [ ] **Question:** Compare Pre-input control with Prompt injection. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Definitions.** Pre-input control: Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. Prompt injection: Untrusted user/retrieved/tool content cannot grant system or tool authority. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-04

- [ ] **Configuration review:** **Question:** Compare Prompt injection with Output control. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Prompt injection](README.md#prompt-injection)

**Answer:** **Definitions.** Prompt injection: Untrusted user/retrieved/tool content cannot grant system or tool authority. Output control: Validate structured data, content policy, citations and destinations before display or execution. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-05

- [ ] **Question:** Compare Output control with Tool guardrail. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Output control](README.md#output-control)

**Answer:** **Definitions.** Output control: Validate structured data, content policy, citations and destinations before display or execution. Tool guardrail: Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-06

- [ ] **Question:** Compare Tool guardrail with Red-team corpus. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Red-team corpus](README.md#red-team-corpus)

**Answer:** **Definitions.** Tool guardrail: Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. Red-team corpus: Versioned attack/edge cases record threat, expected behavior, severity and regression owner. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-07

- [ ] **Configuration review:** **Question:** Compare Red-team corpus with Adaptive testing. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Red-team corpus](README.md#red-team-corpus)

**Answer:** **Definitions.** Red-team corpus: Versioned attack/edge cases record threat, expected behavior, severity and regression owner. Adaptive testing: Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-08

- [ ] **Question:** Compare Adaptive testing with Operations. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Adaptive testing](README.md#adaptive-testing)

**Answer:** **Definitions.** Adaptive testing: Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. Operations: False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-09

- [ ] **Question:** Compare Operations with Incident learning. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Incident learning](README.md#incident-learning)

**Answer:** **Definitions.** Operations: False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. Incident learning: Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MN-10

- [ ] **Configuration review:** **Question:** Compare Incident learning with Threat model. When would each change your design?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Definitions.** Incident learning: Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. Threat model: Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. **Mechanism.** In LLM safety, red teaming and guardrail operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production LLM safety, red teaming and guardrail operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Threat model; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pytest tests/guardrails -q` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-02

- [ ] **Question:** Production is degraded around Policy taxonomy; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Policy taxonomy](README.md#policy-taxonomy)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Pre-input control; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `opa test policy/` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-04

- [ ] **Question:** Production is degraded around Prompt injection; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Prompt injection](README.md#prompt-injection)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n ai-platform deploy/policy-engine --since=30m` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Untrusted user/retrieved/tool content cannot grant system or tool authority. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Output control; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Output control](README.md#output-control)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pytest tests/guardrails -q` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Validate structured data, content policy, citations and destinations before display or execution. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-06

- [ ] **Question:** Production is degraded around Tool guardrail; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Tool guardrail](README.md#tool-guardrail)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Red-team corpus; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Red-team corpus](README.md#red-team-corpus)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `opa test policy/` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Versioned attack/edge cases record threat, expected behavior, severity and regression owner. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-08

- [ ] **Question:** Production is degraded around Adaptive testing; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Adaptive testing](README.md#adaptive-testing)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl logs -n ai-platform deploy/policy-engine --since=30m` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Operations; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Operations](README.md#operations)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pytest tests/guardrails -q` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-MP-10

- [ ] **Question:** Production is degraded around Incident learning; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Incident learning](README.md#incident-learning)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the LLM safety, red teaming and guardrail operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-01

- [ ] **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Threat model, Prompt injection and Red-team corpus are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Red-team corpus](README.md#red-team-corpus)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Threat model: Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. Prompt injection: Untrusted user/retrieved/tool content cannot grant system or tool authority. Red-team corpus: Versioned attack/edge cases record threat, expected behavior, severity and regression owner. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Policy taxonomy, Output control and Adaptive testing are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Adaptive testing](README.md#adaptive-testing)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Policy taxonomy: Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. Output control: Validate structured data, content policy, citations and destinations before display or execution. Adaptive testing: Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-03

- [ ] **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Pre-input control, Tool guardrail and Operations are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Pre-input control: Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. Tool guardrail: Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. Operations: False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Prompt injection, Red-team corpus and Incident learning are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Prompt injection](README.md#prompt-injection)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Prompt injection: Untrusted user/retrieved/tool content cannot grant system or tool authority. Red-team corpus: Versioned attack/edge cases record threat, expected behavior, severity and regression owner. Incident learning: Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-05

- [ ] **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Output control, Adaptive testing and Threat model are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Output control: Validate structured data, content policy, citations and destinations before display or execution. Adaptive testing: Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. Threat model: Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Tool guardrail, Operations and Policy taxonomy are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Policy taxonomy](README.md#policy-taxonomy)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Tool guardrail: Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. Operations: False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. Policy taxonomy: Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-07

- [ ] **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Red-team corpus, Incident learning and Pre-input control are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Red-team corpus: Versioned attack/edge cases record threat, expected behavior, severity and regression owner. Incident learning: Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. Pre-input control: Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Adaptive testing, Threat model and Prompt injection are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Adaptive testing: Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. Threat model: Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. Prompt injection: Untrusted user/retrieved/tool content cannot grant system or tool authority. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-09

- [ ] **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Operations, Policy taxonomy and Output control are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Policy taxonomy](README.md#policy-taxonomy)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Operations: False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. Policy taxonomy: Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. Output control: Validate structured data, content policy, citations and destinations before display or execution. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production LLM safety, red teaming and guardrail operations capability where Incident learning, Pre-input control and Tool guardrail are first-class requirements.
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Incident learning: Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. Pre-input control: Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. Tool guardrail: Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated LLM safety, red teaming and guardrail operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Threat model. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Threat model](README.md#threat-model)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pytest tests/guardrails -q` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Assets, actors, trust boundaries and abuse cases span prompt, retrieval, model, tool, provider and operators. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Policy taxonomy. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Policy taxonomy](README.md#policy-taxonomy)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Harmful content, privacy, authorization, regulated advice and customer-specific rules need owned definitions. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Pre-input control. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Pre-input control](README.md#pre-input-control)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `opa test policy/` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Size/schema/malware/PII and policy checks bound input but cannot prove downstream safety. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Prompt injection. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Prompt injection](README.md#prompt-injection)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n ai-platform deploy/policy-engine --since=30m` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Untrusted user/retrieved/tool content cannot grant system or tool authority. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Output control. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Output control](README.md#output-control)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pytest tests/guardrails -q` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Validate structured data, content policy, citations and destinations before display or execution. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Tool guardrail. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Tool guardrail](README.md#tool-guardrail)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Deterministic authorization, schema/range checks, idempotency and approval mediate model-proposed actions. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Red-team corpus. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Red-team corpus](README.md#red-team-corpus)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `opa test policy/` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Versioned attack/edge cases record threat, expected behavior, severity and regression owner. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Adaptive testing. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Adaptive testing](README.md#adaptive-testing)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl logs -n ai-platform deploy/policy-engine --since=30m` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Mutation/multiturn/encoding/tool/retrieval attacks explore beyond static known strings. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Operations. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Operations](README.md#operations)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pytest tests/guardrails -q` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** False positive/negative, override/appeal, latency, availability and policy-version metrics have SLO/owners. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### LLM-SAFETY-RED-TEAMING-AND-GUARDRAIL-OPERATIONS-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Incident learning. How do you lead it end to end?
> **Covered in:** [LLM safety, red teaming and guardrail operations — Incident learning](README.md#incident-learning)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python redteam.py --manifest release.yaml --suite suites/prompt-injection.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Preserve safe evidence, contain routes/tools/data, add regression cases and verify control changes. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify LLM safety, red teaming and guardrail operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: LLM observability and quality telemetry](../22-llm-observability/README.md) · [Study note](README.md) · [Next: AI governance, approval and evidence automation →](../24-governance-approval-evidence/README.md)

<!-- reading-navigation:end -->
