# ML/LLM deployment and progressive release — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-01

- [ ] **Question:** What is Release manifest, and why does it matter in ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Definition.** Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Release manifest matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original ML/LLM deployment and progressive release journey rather than an administrative green status alone.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-02

- [ ] **Question:** What is Shadow, and why does it matter in ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Shadow](README.md#shadow)

**Answer:** **Definition.** Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Shadow matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original ML/LLM deployment and progressive release journey rather than an administrative green status alone.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-03

- [ ] **Question:** What is Canary, and why does it matter in ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Canary](README.md#canary)

**Answer:** **Definition.** Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Canary matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original ML/LLM deployment and progressive release journey rather than an administrative green status alone.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-04

- [ ] **Question:** What is Champion/challenger, and why does it matter in ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Champion/challenger](README.md#championchallenger)

**Answer:** **Definition.** Retains incumbent and candidate comparison under explicit promotion authority. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Champion/challenger matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original ML/LLM deployment and progressive release journey rather than an administrative green status alone.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-05

- [ ] **Question:** What is Traffic unit, and why does it matter in ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Traffic unit](README.md#traffic-unit)

**Answer:** **Definition.** Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Traffic unit matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original ML/LLM deployment and progressive release journey rather than an administrative green status alone.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-06

- [ ] **Question:** What is Warmup, and why does it matter in ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Warmup](README.md#warmup)

**Answer:** **Definition.** Model download/load/compile/cache and representative request warmup occur before traffic readiness. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Warmup matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original ML/LLM deployment and progressive release journey rather than an administrative green status alone.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-07

- [ ] **Question:** What is Compatibility, and why does it matter in ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Compatibility](README.md#compatibility)

**Answer:** **Definition.** Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Compatibility matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original ML/LLM deployment and progressive release journey rather than an administrative green status alone.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-08

- [ ] **Code:** **Question:** What does `curl -s http://MODEL/metrics | rg 'request|token|latency|error'` help you verify for ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `curl -s http://MODEL/metrics | rg 'request|token|latency|error'` tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. **Mechanism.** The relevant ML/LLM deployment and progressive release concept is Rollback: Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a ML/LLM deployment and progressive release rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-09

- [ ] **Code:** **Question:** What does `kubectl rollout status deployment/MODEL -n inference` help you verify for ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl rollout status deployment/MODEL -n inference` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant ML/LLM deployment and progressive release concept is Evidence window: Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a ML/LLM deployment and progressive release rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JN-10

- [ ] **Code:** **Question:** What does `kubectl rollout history deployment/MODEL -n inference` help you verify for ML/LLM deployment and progressive release?
> **Covered in:** [ML/LLM deployment and progressive release — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl rollout history deployment/MODEL -n inference` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant ML/LLM deployment and progressive release concept is Audit: Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a ML/LLM deployment and progressive release rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-01

- [ ] **Code:** **Question:** A basic Release manifest check fails. What would you do first using the CLI?
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl rollout status deployment/MODEL -n inference`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-02

- [ ] **Question:** A basic Shadow check fails. What would you do first?
> **Covered in:** [ML/LLM deployment and progressive release — Shadow](README.md#shadow)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl rollout history deployment/MODEL -n inference`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-03

- [ ] **Question:** A basic Canary check fails. What would you do first?
> **Covered in:** [ML/LLM deployment and progressive release — Canary](README.md#canary)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get inferenceservice MODEL -n inference -o yaml`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-04

- [ ] **Code:** **Question:** A basic Champion/challenger check fails. What would you do first using the CLI?
> **Covered in:** [ML/LLM deployment and progressive release — Champion/challenger](README.md#championchallenger)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://MODEL/metrics | rg 'request|token|latency|error'`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Retains incumbent and candidate comparison under explicit promotion authority. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-05

- [ ] **Question:** A basic Traffic unit check fails. What would you do first?
> **Covered in:** [ML/LLM deployment and progressive release — Traffic unit](README.md#traffic-unit)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl rollout status deployment/MODEL -n inference`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-06

- [ ] **Question:** A basic Warmup check fails. What would you do first?
> **Covered in:** [ML/LLM deployment and progressive release — Warmup](README.md#warmup)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl rollout history deployment/MODEL -n inference`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Model download/load/compile/cache and representative request warmup occur before traffic readiness. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-07

- [ ] **Code:** **Question:** A basic Compatibility check fails. What would you do first using the CLI?
> **Covered in:** [ML/LLM deployment and progressive release — Compatibility](README.md#compatibility)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get inferenceservice MODEL -n inference -o yaml`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-08

- [ ] **Question:** A basic Rollback check fails. What would you do first?
> **Covered in:** [ML/LLM deployment and progressive release — Rollback](README.md#rollback)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://MODEL/metrics | rg 'request|token|latency|error'`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-09

- [ ] **Question:** A basic Evidence window check fails. What would you do first?
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl rollout status deployment/MODEL -n inference`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-JP-10

- [ ] **Code:** **Question:** A basic Audit check fails. What would you do first using the CLI?
> **Covered in:** [ML/LLM deployment and progressive release — Audit](README.md#audit)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl rollout history deployment/MODEL -n inference`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original ML/LLM deployment and progressive release client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-01

- [ ] **Question:** Compare Release manifest with Shadow. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Definitions.** Release manifest: Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. Shadow: Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-02

- [ ] **Question:** Compare Shadow with Canary. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Shadow](README.md#shadow)

**Answer:** **Definitions.** Shadow: Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. Canary: Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-03

- [ ] **Question:** Compare Canary with Champion/challenger. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Champion/challenger](README.md#championchallenger)

**Answer:** **Definitions.** Canary: Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. Champion/challenger: Retains incumbent and candidate comparison under explicit promotion authority. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-04

- [ ] **Configuration review:** **Question:** Compare Champion/challenger with Traffic unit. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Traffic unit](README.md#traffic-unit)

**Answer:** **Definitions.** Champion/challenger: Retains incumbent and candidate comparison under explicit promotion authority. Traffic unit: Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-05

- [ ] **Question:** Compare Traffic unit with Warmup. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Traffic unit](README.md#traffic-unit)

**Answer:** **Definitions.** Traffic unit: Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. Warmup: Model download/load/compile/cache and representative request warmup occur before traffic readiness. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-06

- [ ] **Question:** Compare Warmup with Compatibility. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Warmup](README.md#warmup)

**Answer:** **Definitions.** Warmup: Model download/load/compile/cache and representative request warmup occur before traffic readiness. Compatibility: Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-07

- [ ] **Configuration review:** **Question:** Compare Compatibility with Rollback. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Compatibility](README.md#compatibility)

**Answer:** **Definitions.** Compatibility: Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. Rollback: Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-08

- [ ] **Question:** Compare Rollback with Evidence window. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Definitions.** Rollback: Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. Evidence window: Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-09

- [ ] **Question:** Compare Evidence window with Audit. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Definitions.** Evidence window: Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. Audit: Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MN-10

- [ ] **Configuration review:** **Question:** Compare Audit with Release manifest. When would each change your design?
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Definitions.** Audit: Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. Release manifest: Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. **Mechanism.** In ML/LLM deployment and progressive release, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production ML/LLM deployment and progressive release design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Release manifest; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl rollout status deployment/MODEL -n inference` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-02

- [ ] **Question:** Production is degraded around Shadow; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Shadow](README.md#shadow)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl rollout history deployment/MODEL -n inference` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Canary; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Canary](README.md#canary)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get inferenceservice MODEL -n inference -o yaml` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-04

- [ ] **Question:** Production is degraded around Champion/challenger; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Champion/challenger](README.md#championchallenger)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://MODEL/metrics | rg 'request|token|latency|error'` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Retains incumbent and candidate comparison under explicit promotion authority. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Traffic unit; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Traffic unit](README.md#traffic-unit)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl rollout status deployment/MODEL -n inference` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-06

- [ ] **Question:** Production is degraded around Warmup; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Warmup](README.md#warmup)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl rollout history deployment/MODEL -n inference` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Model download/load/compile/cache and representative request warmup occur before traffic readiness. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Compatibility; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Compatibility](README.md#compatibility)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get inferenceservice MODEL -n inference -o yaml` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-08

- [ ] **Question:** Production is degraded around Rollback; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Rollback](README.md#rollback)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://MODEL/metrics | rg 'request|token|latency|error'` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Evidence window; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl rollout status deployment/MODEL -n inference` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-MP-10

- [ ] **Question:** Production is degraded around Audit; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [ML/LLM deployment and progressive release — Audit](README.md#audit)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl rollout history deployment/MODEL -n inference` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the ML/LLM deployment and progressive release source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-01

- [ ] **Question:** Design a production ML/LLM deployment and progressive release capability where Release manifest, Champion/challenger and Compatibility are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Release manifest: Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. Champion/challenger: Retains incumbent and candidate comparison under explicit promotion authority. Compatibility: Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production ML/LLM deployment and progressive release capability where Shadow, Traffic unit and Rollback are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Traffic unit](README.md#traffic-unit)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Shadow: Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. Traffic unit: Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. Rollback: Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-03

- [ ] **Question:** Design a production ML/LLM deployment and progressive release capability where Canary, Warmup and Evidence window are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Canary: Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. Warmup: Model download/load/compile/cache and representative request warmup occur before traffic readiness. Evidence window: Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production ML/LLM deployment and progressive release capability where Champion/challenger, Compatibility and Audit are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Audit](README.md#audit)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Champion/challenger: Retains incumbent and candidate comparison under explicit promotion authority. Compatibility: Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. Audit: Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-05

- [ ] **Question:** Design a production ML/LLM deployment and progressive release capability where Traffic unit, Rollback and Release manifest are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Traffic unit: Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. Rollback: Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. Release manifest: Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production ML/LLM deployment and progressive release capability where Warmup, Evidence window and Shadow are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Warmup: Model download/load/compile/cache and representative request warmup occur before traffic readiness. Evidence window: Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. Shadow: Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-07

- [ ] **Question:** Design a production ML/LLM deployment and progressive release capability where Compatibility, Audit and Canary are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Canary](README.md#canary)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Compatibility: Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. Audit: Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. Canary: Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production ML/LLM deployment and progressive release capability where Rollback, Release manifest and Champion/challenger are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Rollback: Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. Release manifest: Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. Champion/challenger: Retains incumbent and candidate comparison under explicit promotion authority. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-09

- [ ] **Question:** Design a production ML/LLM deployment and progressive release capability where Evidence window, Shadow and Traffic unit are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Evidence window: Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. Shadow: Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. Traffic unit: Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production ML/LLM deployment and progressive release capability where Audit, Canary and Warmup are first-class requirements.
> **Covered in:** [ML/LLM deployment and progressive release — Warmup](README.md#warmup)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Audit: Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. Canary: Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. Warmup: Model download/load/compile/cache and representative request warmup occur before traffic readiness. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated ML/LLM deployment and progressive release options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Release manifest. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Release manifest](README.md#release-manifest)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl rollout status deployment/MODEL -n inference` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Model, tokenizer, prompt, adapter, runtime, hardware, index, policy and evaluator versions form one deployable contract. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Shadow. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Shadow](README.md#shadow)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl rollout history deployment/MODEL -n inference` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Copies production inputs without user-visible output and requires privacy, side-effect and cost controls. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Canary. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Canary](README.md#canary)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get inferenceservice MODEL -n inference -o yaml` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Routes bounded real traffic with automatic infrastructure and task-quality abort thresholds. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Champion/challenger. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Champion/challenger](README.md#championchallenger)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://MODEL/metrics | rg 'request|token|latency|error'` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Retains incumbent and candidate comparison under explicit promotion authority. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Traffic unit. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Traffic unit](README.md#traffic-unit)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl rollout status deployment/MODEL -n inference` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Request/user/session/tenant assignment and stickiness determine unbiased consistent comparison. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Warmup. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Warmup](README.md#warmup)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl rollout history deployment/MODEL -n inference` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Model download/load/compile/cache and representative request warmup occur before traffic readiness. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Compatibility. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Compatibility](README.md#compatibility)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get inferenceservice MODEL -n inference -o yaml` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Schema, client, cache, feature/index and persisted state must work during mixed-version rollout. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Rollback. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Rollback](README.md#rollback)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://MODEL/metrics | rg 'request|token|latency|error'` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Prior bytes/config/index/policy/capacity remain available and correctness is reverified after reversal. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Evidence window. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Evidence window](README.md#evidence-window)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl rollout status deployment/MODEL -n inference` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Rare quality/safety failures may require longer/sample-aware gates than latency/error metrics. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### ML-LLM-DEPLOYMENT-AND-PROGRESSIVE-RELEASE-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Audit. How do you lead it end to end?
> **Covered in:** [ML/LLM deployment and progressive release — Audit](README.md#audit)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl rollout history deployment/MODEL -n inference` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Release actor, evidence, traffic changes, exceptions, incidents and final deployed digest remain linked. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify ML/LLM deployment and progressive release from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Model validation and release gates](../12-model-validation-release-gates/README.md) · [Study note](README.md) · [Next: ML monitoring, drift and data quality →](../14-monitoring-drift/README.md)

<!-- reading-navigation:end -->
