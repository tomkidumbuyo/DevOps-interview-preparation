# PromptOps: prompt, template and policy lifecycle — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-01

- [ ] **Question:** What is Prompt asset, and why does it matter in PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Definition.** System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Prompt asset matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original PromptOps: prompt, template and policy lifecycle journey rather than an administrative green status alone.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-02

- [ ] **Question:** What is Rendering, and why does it matter in PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rendering](README.md#rendering)

**Answer:** **Definition.** Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Rendering matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original PromptOps: prompt, template and policy lifecycle journey rather than an administrative green status alone.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-03

- [ ] **Question:** What is Model coupling, and why does it matter in PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Definition.** A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Model coupling matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original PromptOps: prompt, template and policy lifecycle journey rather than an administrative green status alone.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-04

- [ ] **Question:** What is Prompt dataset, and why does it matter in PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt dataset](README.md#prompt-dataset)

**Answer:** **Definition.** Expected tasks, edge cases, adversarial inputs and owner define regression evidence. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Prompt dataset matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original PromptOps: prompt, template and policy lifecycle journey rather than an administrative green status alone.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-05

- [ ] **Question:** What is Change review, and why does it matter in PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Change review](README.md#change-review)

**Answer:** **Definition.** Semantic diff explains instruction/order/example/policy changes beyond line-level text. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Change review matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original PromptOps: prompt, template and policy lifecycle journey rather than an administrative green status alone.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-06

- [ ] **Question:** What is Release, and why does it matter in PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Release](README.md#release)

**Answer:** **Definition.** Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Release matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original PromptOps: prompt, template and policy lifecycle journey rather than an administrative green status alone.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-07

- [ ] **Question:** What is Caching, and why does it matter in PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Caching](README.md#caching)

**Answer:** **Definition.** Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Caching matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original PromptOps: prompt, template and policy lifecycle journey rather than an administrative green status alone.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-08

- [ ] **Code:** **Question:** What does `sha256sum prompts/support.yaml` help you verify for PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `sha256sum prompts/support.yaml` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant PromptOps: prompt, template and policy lifecycle concept is Observability: Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a PromptOps: prompt, template and policy lifecycle rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-09

- [ ] **Code:** **Question:** What does `git diff -- prompts/` help you verify for PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `git diff -- prompts/` inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. **Mechanism.** The relevant PromptOps: prompt, template and policy lifecycle concept is Rollback: Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a PromptOps: prompt, template and policy lifecycle rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JN-10

- [ ] **Code:** **Question:** What does `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` help you verify for PromptOps: prompt, template and policy lifecycle?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant PromptOps: prompt, template and policy lifecycle concept is Governance: Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a PromptOps: prompt, template and policy lifecycle rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-01

- [ ] **Code:** **Question:** A basic Prompt asset check fails. What would you do first using the CLI?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git diff -- prompts/`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-02

- [ ] **Question:** A basic Rendering check fails. What would you do first?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rendering](README.md#rendering)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-03

- [ ] **Question:** A basic Model coupling check fails. What would you do first?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m evals.run --manifest release.yaml --dataset prompt-regression.jsonl`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-04

- [ ] **Code:** **Question:** A basic Prompt dataset check fails. What would you do first using the CLI?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt dataset](README.md#prompt-dataset)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `sha256sum prompts/support.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Expected tasks, edge cases, adversarial inputs and owner define regression evidence. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-05

- [ ] **Question:** A basic Change review check fails. What would you do first?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Change review](README.md#change-review)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git diff -- prompts/`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Semantic diff explains instruction/order/example/policy changes beyond line-level text. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-06

- [ ] **Question:** A basic Release check fails. What would you do first?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Release](README.md#release)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-07

- [ ] **Code:** **Question:** A basic Caching check fails. What would you do first using the CLI?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Caching](README.md#caching)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m evals.run --manifest release.yaml --dataset prompt-regression.jsonl`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-08

- [ ] **Question:** A basic Observability check fails. What would you do first?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Observability](README.md#observability)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `sha256sum prompts/support.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-09

- [ ] **Question:** A basic Rollback check fails. What would you do first?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rollback](README.md#rollback)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git diff -- prompts/`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-JP-10

- [ ] **Code:** **Question:** A basic Governance check fails. What would you do first using the CLI?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Governance](README.md#governance)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original PromptOps: prompt, template and policy lifecycle client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-01

- [ ] **Question:** Compare Prompt asset with Rendering. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Definitions.** Prompt asset: System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. Rendering: Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-02

- [ ] **Question:** Compare Rendering with Model coupling. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Definitions.** Rendering: Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. Model coupling: A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-03

- [ ] **Question:** Compare Model coupling with Prompt dataset. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Definitions.** Model coupling: A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. Prompt dataset: Expected tasks, edge cases, adversarial inputs and owner define regression evidence. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-04

- [ ] **Configuration review:** **Question:** Compare Prompt dataset with Change review. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt dataset](README.md#prompt-dataset)

**Answer:** **Definitions.** Prompt dataset: Expected tasks, edge cases, adversarial inputs and owner define regression evidence. Change review: Semantic diff explains instruction/order/example/policy changes beyond line-level text. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-05

- [ ] **Question:** Compare Change review with Release. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Change review](README.md#change-review)

**Answer:** **Definitions.** Change review: Semantic diff explains instruction/order/example/policy changes beyond line-level text. Release: Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-06

- [ ] **Question:** Compare Release with Caching. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Caching](README.md#caching)

**Answer:** **Definitions.** Release: Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. Caching: Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-07

- [ ] **Configuration review:** **Question:** Compare Caching with Observability. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Caching](README.md#caching)

**Answer:** **Definitions.** Caching: Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. Observability: Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-08

- [ ] **Question:** Compare Observability with Rollback. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rollback](README.md#rollback)

**Answer:** **Definitions.** Observability: Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. Rollback: Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-09

- [ ] **Question:** Compare Rollback with Governance. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rollback](README.md#rollback)

**Answer:** **Definitions.** Rollback: Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. Governance: Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MN-10

- [ ] **Configuration review:** **Question:** Compare Governance with Prompt asset. When would each change your design?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Definitions.** Governance: Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. Prompt asset: System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. **Mechanism.** In PromptOps: prompt, template and policy lifecycle, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production PromptOps: prompt, template and policy lifecycle design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Prompt asset; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git diff -- prompts/` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-02

- [ ] **Question:** Production is degraded around Rendering; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rendering](README.md#rendering)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Model coupling; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m evals.run --manifest release.yaml --dataset prompt-regression.jsonl` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-04

- [ ] **Question:** Production is degraded around Prompt dataset; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt dataset](README.md#prompt-dataset)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `sha256sum prompts/support.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Expected tasks, edge cases, adversarial inputs and owner define regression evidence. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Change review; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Change review](README.md#change-review)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git diff -- prompts/` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Semantic diff explains instruction/order/example/policy changes beyond line-level text. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-06

- [ ] **Question:** Production is degraded around Release; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Release](README.md#release)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Caching; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Caching](README.md#caching)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m evals.run --manifest release.yaml --dataset prompt-regression.jsonl` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-08

- [ ] **Question:** Production is degraded around Observability; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Observability](README.md#observability)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `sha256sum prompts/support.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Rollback; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rollback](README.md#rollback)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git diff -- prompts/` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-MP-10

- [ ] **Question:** Production is degraded around Governance; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Governance](README.md#governance)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the PromptOps: prompt, template and policy lifecycle source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-01

- [ ] **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Prompt asset, Prompt dataset and Caching are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Prompt asset: System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. Prompt dataset: Expected tasks, edge cases, adversarial inputs and owner define regression evidence. Caching: Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Rendering, Change review and Observability are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Change review](README.md#change-review)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Rendering: Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. Change review: Semantic diff explains instruction/order/example/policy changes beyond line-level text. Observability: Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-03

- [ ] **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Model coupling, Release and Rollback are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Model coupling: A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. Release: Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. Rollback: Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Prompt dataset, Caching and Governance are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt dataset](README.md#prompt-dataset)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Prompt dataset: Expected tasks, edge cases, adversarial inputs and owner define regression evidence. Caching: Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. Governance: Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-05

- [ ] **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Change review, Observability and Prompt asset are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Change review: Semantic diff explains instruction/order/example/policy changes beyond line-level text. Observability: Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. Prompt asset: System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Release, Rollback and Rendering are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Release](README.md#release)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Release: Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. Rollback: Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. Rendering: Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-07

- [ ] **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Caching, Governance and Model coupling are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Caching: Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. Governance: Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. Model coupling: A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Observability, Prompt asset and Prompt dataset are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Observability: Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. Prompt asset: System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. Prompt dataset: Expected tasks, edge cases, adversarial inputs and owner define regression evidence. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-09

- [ ] **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Rollback, Rendering and Change review are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Change review](README.md#change-review)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Rollback: Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. Rendering: Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. Change review: Semantic diff explains instruction/order/example/policy changes beyond line-level text. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production PromptOps: prompt, template and policy lifecycle capability where Governance, Model coupling and Release are first-class requirements.
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Governance: Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. Model coupling: A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. Release: Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated PromptOps: prompt, template and policy lifecycle options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Prompt asset. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt asset](README.md#prompt-asset)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git diff -- prompts/` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** System/developer/user templates, variables, examples and output schema are versioned independently from application code when needed. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Rendering. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rendering](README.md#rendering)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Typed variables, escaping, length limits and deterministic template engine prevent malformed or injected structure. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Model coupling. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Model coupling](README.md#model-coupling)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m evals.run --manifest release.yaml --dataset prompt-regression.jsonl` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A prompt version is qualified against specific model/tokenizer/provider capabilities and settings. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Prompt dataset. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Prompt dataset](README.md#prompt-dataset)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `sha256sum prompts/support.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Expected tasks, edge cases, adversarial inputs and owner define regression evidence. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Change review. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Change review](README.md#change-review)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git diff -- prompts/` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Semantic diff explains instruction/order/example/policy changes beyond line-level text. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Release. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Release](README.md#release)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Immutable prompt digest/alias follows shadow/canary and quality/safety/latency/cost gates. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Caching. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Caching](README.md#caching)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m evals.run --manifest release.yaml --dataset prompt-regression.jsonl` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Cache keys include prompt/model/policy/tenant semantics and never cross authorization boundaries. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Observability. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Observability](README.md#observability)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `sha256sum prompts/support.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Prompt version, safe template ID, tokens, latency, route and evaluation join without logging sensitive rendered content. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Rollback. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Rollback](README.md#rollback)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git diff -- prompts/` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Previous prompt and compatible model/tool schema remain deployable with fast traffic reversal. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### PROMPTOPS-PROMPT-TEMPLATE-AND-POLICY-LIFECYCLE-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Governance. How do you lead it end to end?
> **Covered in:** [PromptOps: prompt, template and policy lifecycle — Governance](README.md#governance)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python scripts/render_prompt.py --template prompts/support.yaml --fixture tests/fixture.json` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Owner, intended use, prohibited data/actions, approval, exception and retirement are auditable. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify PromptOps: prompt, template and policy lifecycle from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Retraining and feedback loops](../15-retraining-feedback-loops/README.md) · [Study note](README.md) · [Next: LLM fine-tuning, PEFT and adapter operations →](../17-llm-finetuning-peft/README.md)

<!-- reading-navigation:end -->
