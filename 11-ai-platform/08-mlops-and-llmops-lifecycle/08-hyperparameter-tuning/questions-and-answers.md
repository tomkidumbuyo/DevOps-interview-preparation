# Hyperparameter tuning and experiment search — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-01

- [ ] **Question:** What is Search space, and why does it matter in Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Definition.** Type/range/scale/conditional relationships encode valid candidate configurations. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Search space matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Hyperparameter tuning and experiment search journey rather than an administrative green status alone.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-02

- [ ] **Question:** What is Random/grid/Bayesian, and why does it matter in Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Random/grid/Bayesian](README.md#randomgridbayesian)

**Answer:** **Definition.** Exploration strategy trades coverage, parallelism and sample efficiency. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Random/grid/Bayesian matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Hyperparameter tuning and experiment search journey rather than an administrative green status alone.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-03

- [ ] **Question:** What is Objective, and why does it matter in Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Objective](README.md#objective)

**Answer:** **Definition.** Metric/dataset/slice/direction/aggregation must represent the production goal. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Objective matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Hyperparameter tuning and experiment search journey rather than an administrative green status alone.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-04

- [ ] **Question:** What is Multi-objective, and why does it matter in Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Multi-objective](README.md#multi-objective)

**Answer:** **Definition.** Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Multi-objective matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Hyperparameter tuning and experiment search journey rather than an administrative green status alone.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-05

- [ ] **Question:** What is Early stopping, and why does it matter in Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Definition.** Prunes poor trials only when partial metrics are comparable and not systematically biased. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Early stopping matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Hyperparameter tuning and experiment search journey rather than an administrative green status alone.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-06

- [ ] **Question:** What is Trial isolation, and why does it matter in Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Definition.** Data split, seed, resources and artifact paths cannot collide across parallel trials. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Trial isolation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Hyperparameter tuning and experiment search journey rather than an administrative green status alone.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-07

- [ ] **Question:** What is Budget, and why does it matter in Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Budget](README.md#budget)

**Answer:** **Definition.** Maximum trials/time/GPU-hours and concurrency prevent search from starving production. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Budget matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Hyperparameter tuning and experiment search journey rather than an administrative green status alone.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-08

- [ ] **Code:** **Question:** What does `mlflow runs list --experiment-id EXPERIMENT_ID` help you verify for Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `mlflow runs list --experiment-id EXPERIMENT_ID` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Hyperparameter tuning and experiment search concept is Scheduler: ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Hyperparameter tuning and experiment search rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-09

- [ ] **Code:** **Question:** What does `kubectl get experiments,trials -A` help you verify for Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl get experiments,trials -A` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant Hyperparameter tuning and experiment search concept is Selection bias: Repeated tuning against one validation set overfits the evaluation process. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Hyperparameter tuning and experiment search rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JN-10

- [ ] **Code:** **Question:** What does `kubectl describe experiment NAME -n NS` help you verify for Hyperparameter tuning and experiment search?
> **Covered in:** [Hyperparameter tuning and experiment search — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl describe experiment NAME -n NS` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant Hyperparameter tuning and experiment search concept is Promotion: Rerun finalist with locked config/seeds and independent evaluation before registry approval. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Hyperparameter tuning and experiment search rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-01

- [ ] **Code:** **Question:** A basic Search space check fails. What would you do first using the CLI?
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get experiments,trials -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Type/range/scale/conditional relationships encode valid candidate configurations. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-02

- [ ] **Question:** A basic Random/grid/Bayesian check fails. What would you do first?
> **Covered in:** [Hyperparameter tuning and experiment search — Random/grid/Bayesian](README.md#randomgridbayesian)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl describe experiment NAME -n NS`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Exploration strategy trades coverage, parallelism and sample efficiency. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-03

- [ ] **Question:** A basic Objective check fails. What would you do first?
> **Covered in:** [Hyperparameter tuning and experiment search — Objective](README.md#objective)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python tune.py --smoke-test --max-trials 4`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Metric/dataset/slice/direction/aggregation must represent the production goal. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-04

- [ ] **Code:** **Question:** A basic Multi-objective check fails. What would you do first using the CLI?
> **Covered in:** [Hyperparameter tuning and experiment search — Multi-objective](README.md#multi-objective)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow runs list --experiment-id EXPERIMENT_ID`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-05

- [ ] **Question:** A basic Early stopping check fails. What would you do first?
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get experiments,trials -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Prunes poor trials only when partial metrics are comparable and not systematically biased. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-06

- [ ] **Question:** A basic Trial isolation check fails. What would you do first?
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl describe experiment NAME -n NS`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Data split, seed, resources and artifact paths cannot collide across parallel trials. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-07

- [ ] **Code:** **Question:** A basic Budget check fails. What would you do first using the CLI?
> **Covered in:** [Hyperparameter tuning and experiment search — Budget](README.md#budget)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python tune.py --smoke-test --max-trials 4`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Maximum trials/time/GPU-hours and concurrency prevent search from starving production. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-08

- [ ] **Question:** A basic Scheduler check fails. What would you do first?
> **Covered in:** [Hyperparameter tuning and experiment search — Scheduler](README.md#scheduler)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow runs list --experiment-id EXPERIMENT_ID`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-09

- [ ] **Question:** A basic Selection bias check fails. What would you do first?
> **Covered in:** [Hyperparameter tuning and experiment search — Selection bias](README.md#selection-bias)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get experiments,trials -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Repeated tuning against one validation set overfits the evaluation process. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-JP-10

- [ ] **Code:** **Question:** A basic Promotion check fails. What would you do first using the CLI?
> **Covered in:** [Hyperparameter tuning and experiment search — Promotion](README.md#promotion)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl describe experiment NAME -n NS`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Rerun finalist with locked config/seeds and independent evaluation before registry approval. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Hyperparameter tuning and experiment search client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-01

- [ ] **Question:** Compare Search space with Random/grid/Bayesian. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Definitions.** Search space: Type/range/scale/conditional relationships encode valid candidate configurations. Random/grid/Bayesian: Exploration strategy trades coverage, parallelism and sample efficiency. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-02

- [ ] **Question:** Compare Random/grid/Bayesian with Objective. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Random/grid/Bayesian](README.md#randomgridbayesian)

**Answer:** **Definitions.** Random/grid/Bayesian: Exploration strategy trades coverage, parallelism and sample efficiency. Objective: Metric/dataset/slice/direction/aggregation must represent the production goal. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-03

- [ ] **Question:** Compare Objective with Multi-objective. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Multi-objective](README.md#multi-objective)

**Answer:** **Definitions.** Objective: Metric/dataset/slice/direction/aggregation must represent the production goal. Multi-objective: Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-04

- [ ] **Configuration review:** **Question:** Compare Multi-objective with Early stopping. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Definitions.** Multi-objective: Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. Early stopping: Prunes poor trials only when partial metrics are comparable and not systematically biased. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-05

- [ ] **Question:** Compare Early stopping with Trial isolation. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Definitions.** Early stopping: Prunes poor trials only when partial metrics are comparable and not systematically biased. Trial isolation: Data split, seed, resources and artifact paths cannot collide across parallel trials. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-06

- [ ] **Question:** Compare Trial isolation with Budget. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Definitions.** Trial isolation: Data split, seed, resources and artifact paths cannot collide across parallel trials. Budget: Maximum trials/time/GPU-hours and concurrency prevent search from starving production. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-07

- [ ] **Configuration review:** **Question:** Compare Budget with Scheduler. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Budget](README.md#budget)

**Answer:** **Definitions.** Budget: Maximum trials/time/GPU-hours and concurrency prevent search from starving production. Scheduler: ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-08

- [ ] **Question:** Compare Scheduler with Selection bias. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Selection bias](README.md#selection-bias)

**Answer:** **Definitions.** Scheduler: ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. Selection bias: Repeated tuning against one validation set overfits the evaluation process. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-09

- [ ] **Question:** Compare Selection bias with Promotion. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Selection bias](README.md#selection-bias)

**Answer:** **Definitions.** Selection bias: Repeated tuning against one validation set overfits the evaluation process. Promotion: Rerun finalist with locked config/seeds and independent evaluation before registry approval. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MN-10

- [ ] **Configuration review:** **Question:** Compare Promotion with Search space. When would each change your design?
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Definitions.** Promotion: Rerun finalist with locked config/seeds and independent evaluation before registry approval. Search space: Type/range/scale/conditional relationships encode valid candidate configurations. **Mechanism.** In Hyperparameter tuning and experiment search, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Hyperparameter tuning and experiment search design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Search space; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get experiments,trials -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Type/range/scale/conditional relationships encode valid candidate configurations. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-02

- [ ] **Question:** Production is degraded around Random/grid/Bayesian; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Random/grid/Bayesian](README.md#randomgridbayesian)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl describe experiment NAME -n NS` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Exploration strategy trades coverage, parallelism and sample efficiency. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Objective; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Objective](README.md#objective)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python tune.py --smoke-test --max-trials 4` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Metric/dataset/slice/direction/aggregation must represent the production goal. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-04

- [ ] **Question:** Production is degraded around Multi-objective; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Multi-objective](README.md#multi-objective)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow runs list --experiment-id EXPERIMENT_ID` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Early stopping; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get experiments,trials -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Prunes poor trials only when partial metrics are comparable and not systematically biased. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-06

- [ ] **Question:** Production is degraded around Trial isolation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl describe experiment NAME -n NS` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Data split, seed, resources and artifact paths cannot collide across parallel trials. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Budget; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Budget](README.md#budget)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python tune.py --smoke-test --max-trials 4` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Maximum trials/time/GPU-hours and concurrency prevent search from starving production. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-08

- [ ] **Question:** Production is degraded around Scheduler; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Scheduler](README.md#scheduler)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow runs list --experiment-id EXPERIMENT_ID` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Selection bias; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Selection bias](README.md#selection-bias)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get experiments,trials -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Repeated tuning against one validation set overfits the evaluation process. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-MP-10

- [ ] **Question:** Production is degraded around Promotion; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Hyperparameter tuning and experiment search — Promotion](README.md#promotion)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl describe experiment NAME -n NS` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Rerun finalist with locked config/seeds and independent evaluation before registry approval. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Hyperparameter tuning and experiment search source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-01

- [ ] **Question:** Design a production Hyperparameter tuning and experiment search capability where Search space, Multi-objective and Budget are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Search space: Type/range/scale/conditional relationships encode valid candidate configurations. Multi-objective: Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. Budget: Maximum trials/time/GPU-hours and concurrency prevent search from starving production. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Hyperparameter tuning and experiment search capability where Random/grid/Bayesian, Early stopping and Scheduler are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Random/grid/Bayesian: Exploration strategy trades coverage, parallelism and sample efficiency. Early stopping: Prunes poor trials only when partial metrics are comparable and not systematically biased. Scheduler: ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-03

- [ ] **Question:** Design a production Hyperparameter tuning and experiment search capability where Objective, Trial isolation and Selection bias are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Objective: Metric/dataset/slice/direction/aggregation must represent the production goal. Trial isolation: Data split, seed, resources and artifact paths cannot collide across parallel trials. Selection bias: Repeated tuning against one validation set overfits the evaluation process. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Hyperparameter tuning and experiment search capability where Multi-objective, Budget and Promotion are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Budget](README.md#budget)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Multi-objective: Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. Budget: Maximum trials/time/GPU-hours and concurrency prevent search from starving production. Promotion: Rerun finalist with locked config/seeds and independent evaluation before registry approval. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-05

- [ ] **Question:** Design a production Hyperparameter tuning and experiment search capability where Early stopping, Scheduler and Search space are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Early stopping: Prunes poor trials only when partial metrics are comparable and not systematically biased. Scheduler: ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. Search space: Type/range/scale/conditional relationships encode valid candidate configurations. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Hyperparameter tuning and experiment search capability where Trial isolation, Selection bias and Random/grid/Bayesian are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Trial isolation: Data split, seed, resources and artifact paths cannot collide across parallel trials. Selection bias: Repeated tuning against one validation set overfits the evaluation process. Random/grid/Bayesian: Exploration strategy trades coverage, parallelism and sample efficiency. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-07

- [ ] **Question:** Design a production Hyperparameter tuning and experiment search capability where Budget, Promotion and Objective are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Budget](README.md#budget)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Budget: Maximum trials/time/GPU-hours and concurrency prevent search from starving production. Promotion: Rerun finalist with locked config/seeds and independent evaluation before registry approval. Objective: Metric/dataset/slice/direction/aggregation must represent the production goal. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Hyperparameter tuning and experiment search capability where Scheduler, Search space and Multi-objective are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Scheduler: ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. Search space: Type/range/scale/conditional relationships encode valid candidate configurations. Multi-objective: Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-09

- [ ] **Question:** Design a production Hyperparameter tuning and experiment search capability where Selection bias, Random/grid/Bayesian and Early stopping are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Selection bias: Repeated tuning against one validation set overfits the evaluation process. Random/grid/Bayesian: Exploration strategy trades coverage, parallelism and sample efficiency. Early stopping: Prunes poor trials only when partial metrics are comparable and not systematically biased. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Hyperparameter tuning and experiment search capability where Promotion, Objective and Trial isolation are first-class requirements.
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Promotion: Rerun finalist with locked config/seeds and independent evaluation before registry approval. Objective: Metric/dataset/slice/direction/aggregation must represent the production goal. Trial isolation: Data split, seed, resources and artifact paths cannot collide across parallel trials. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Hyperparameter tuning and experiment search options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Search space. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Search space](README.md#search-space)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get experiments,trials -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Type/range/scale/conditional relationships encode valid candidate configurations. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Random/grid/Bayesian. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Random/grid/Bayesian](README.md#randomgridbayesian)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl describe experiment NAME -n NS` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Exploration strategy trades coverage, parallelism and sample efficiency. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Objective. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Objective](README.md#objective)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python tune.py --smoke-test --max-trials 4` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Metric/dataset/slice/direction/aggregation must represent the production goal. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Multi-objective. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Multi-objective](README.md#multi-objective)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow runs list --experiment-id EXPERIMENT_ID` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Quality, latency, memory and cost create a Pareto frontier rather than one magic scalar. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Early stopping. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Early stopping](README.md#early-stopping)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get experiments,trials -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Prunes poor trials only when partial metrics are comparable and not systematically biased. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Trial isolation. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Trial isolation](README.md#trial-isolation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl describe experiment NAME -n NS` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Data split, seed, resources and artifact paths cannot collide across parallel trials. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Budget. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Budget](README.md#budget)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python tune.py --smoke-test --max-trials 4` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Maximum trials/time/GPU-hours and concurrency prevent search from starving production. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Scheduler. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Scheduler](README.md#scheduler)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow runs list --experiment-id EXPERIMENT_ID` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** ASHA/HyperBand-style resource allocation needs fair checkpoints and monotonic resource semantics. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Selection bias. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Selection bias](README.md#selection-bias)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get experiments,trials -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Repeated tuning against one validation set overfits the evaluation process. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### HYPERPARAMETER-TUNING-AND-EXPERIMENT-SEARCH-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Promotion. How do you lead it end to end?
> **Covered in:** [Hyperparameter tuning and experiment search — Promotion](README.md#promotion)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl describe experiment NAME -n NS` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Rerun finalist with locked config/seeds and independent evaluation before registry approval. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Hyperparameter tuning and experiment search from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Distributed training and checkpoint recovery](../07-distributed-training/README.md) · [Study note](README.md) · [Next: Reproducibility and environment capture →](../09-reproducibility-environments/README.md)

<!-- reading-navigation:end -->
