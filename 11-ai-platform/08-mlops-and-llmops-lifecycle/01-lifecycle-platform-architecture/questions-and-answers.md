# MLOps/LLMOps platform architecture — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-01

- [ ] **Question:** What is Lifecycle state machine, and why does it matter in MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Definition.** Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Lifecycle state machine matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original MLOps/LLMOps platform architecture journey rather than an administrative green status alone.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-02

- [ ] **Question:** What is Control plane, and why does it matter in MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Definition.** Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Control plane matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original MLOps/LLMOps platform architecture journey rather than an administrative green status alone.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-03

- [ ] **Question:** What is Data plane, and why does it matter in MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Data plane](README.md#data-plane)

**Answer:** **Definition.** Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Data plane matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original MLOps/LLMOps platform architecture journey rather than an administrative green status alone.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-04

- [ ] **Question:** What is Release unit, and why does it matter in MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Release unit](README.md#release-unit)

**Answer:** **Definition.** Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Release unit matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original MLOps/LLMOps platform architecture journey rather than an administrative green status alone.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-05

- [ ] **Question:** What is Metadata spine, and why does it matter in MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Metadata spine](README.md#metadata-spine)

**Answer:** **Definition.** Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Metadata spine matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original MLOps/LLMOps platform architecture journey rather than an administrative green status alone.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-06

- [ ] **Question:** What is Environment promotion, and why does it matter in MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Definition.** Promote immutable evidence/artifacts while configuration and credentials remain target-specific. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Environment promotion matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original MLOps/LLMOps platform architecture journey rather than an administrative green status alone.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-07

- [ ] **Question:** What is Human decision, and why does it matter in MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Human decision](README.md#human-decision)

**Answer:** **Definition.** Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Human decision matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original MLOps/LLMOps platform architecture journey rather than an administrative green status alone.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-08

- [ ] **Code:** **Question:** What does `curl -s http://metadata-api/health` help you verify for MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `curl -s http://metadata-api/health` tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. **Mechanism.** The relevant MLOps/LLMOps platform architecture concept is Reconciliation: Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a MLOps/LLMOps platform architecture rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-09

- [ ] **Code:** **Question:** What does `mlflow experiments search` help you verify for MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `mlflow experiments search` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant MLOps/LLMOps platform architecture concept is Multi-tenancy: Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a MLOps/LLMOps platform architecture rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JN-10

- [ ] **Code:** **Question:** What does `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` help you verify for MLOps/LLMOps platform architecture?
> **Covered in:** [MLOps/LLMOps platform architecture — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant MLOps/LLMOps platform architecture concept is Exit and retirement: Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a MLOps/LLMOps platform architecture rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-01

- [ ] **Code:** **Question:** A basic Lifecycle state machine check fails. What would you do first using the CLI?
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow experiments search`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-02

- [ ] **Question:** A basic Control plane check fails. What would you do first?
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-03

- [ ] **Question:** A basic Data plane check fails. What would you do first?
> **Covered in:** [MLOps/LLMOps platform architecture — Data plane](README.md#data-plane)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git log --graph --oneline --all`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-04

- [ ] **Code:** **Question:** A basic Release unit check fails. What would you do first using the CLI?
> **Covered in:** [MLOps/LLMOps platform architecture — Release unit](README.md#release-unit)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://metadata-api/health`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-05

- [ ] **Question:** A basic Metadata spine check fails. What would you do first?
> **Covered in:** [MLOps/LLMOps platform architecture — Metadata spine](README.md#metadata-spine)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow experiments search`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-06

- [ ] **Question:** A basic Environment promotion check fails. What would you do first?
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Promote immutable evidence/artifacts while configuration and credentials remain target-specific. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-07

- [ ] **Code:** **Question:** A basic Human decision check fails. What would you do first using the CLI?
> **Covered in:** [MLOps/LLMOps platform architecture — Human decision](README.md#human-decision)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git log --graph --oneline --all`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-08

- [ ] **Question:** A basic Reconciliation check fails. What would you do first?
> **Covered in:** [MLOps/LLMOps platform architecture — Reconciliation](README.md#reconciliation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://metadata-api/health`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-09

- [ ] **Question:** A basic Multi-tenancy check fails. What would you do first?
> **Covered in:** [MLOps/LLMOps platform architecture — Multi-tenancy](README.md#multi-tenancy)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `mlflow experiments search`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-JP-10

- [ ] **Code:** **Question:** A basic Exit and retirement check fails. What would you do first using the CLI?
> **Covered in:** [MLOps/LLMOps platform architecture — Exit and retirement](README.md#exit-and-retirement)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original MLOps/LLMOps platform architecture client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-01

- [ ] **Question:** Compare Lifecycle state machine with Control plane. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Definitions.** Lifecycle state machine: Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. Control plane: Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-02

- [ ] **Question:** Compare Control plane with Data plane. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Definitions.** Control plane: Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. Data plane: Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-03

- [ ] **Question:** Compare Data plane with Release unit. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Data plane](README.md#data-plane)

**Answer:** **Definitions.** Data plane: Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. Release unit: Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-04

- [ ] **Configuration review:** **Question:** Compare Release unit with Metadata spine. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Metadata spine](README.md#metadata-spine)

**Answer:** **Definitions.** Release unit: Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. Metadata spine: Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-05

- [ ] **Question:** Compare Metadata spine with Environment promotion. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Definitions.** Metadata spine: Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. Environment promotion: Promote immutable evidence/artifacts while configuration and credentials remain target-specific. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-06

- [ ] **Question:** Compare Environment promotion with Human decision. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Definitions.** Environment promotion: Promote immutable evidence/artifacts while configuration and credentials remain target-specific. Human decision: Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-07

- [ ] **Configuration review:** **Question:** Compare Human decision with Reconciliation. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Reconciliation](README.md#reconciliation)

**Answer:** **Definitions.** Human decision: Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. Reconciliation: Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-08

- [ ] **Question:** Compare Reconciliation with Multi-tenancy. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Reconciliation](README.md#reconciliation)

**Answer:** **Definitions.** Reconciliation: Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. Multi-tenancy: Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-09

- [ ] **Question:** Compare Multi-tenancy with Exit and retirement. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Exit and retirement](README.md#exit-and-retirement)

**Answer:** **Definitions.** Multi-tenancy: Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. Exit and retirement: Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MN-10

- [ ] **Configuration review:** **Question:** Compare Exit and retirement with Lifecycle state machine. When would each change your design?
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Definitions.** Exit and retirement: Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. Lifecycle state machine: Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. **Mechanism.** In MLOps/LLMOps platform architecture, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production MLOps/LLMOps platform architecture design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Lifecycle state machine; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow experiments search` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-02

- [ ] **Question:** Production is degraded around Control plane; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Data plane; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Data plane](README.md#data-plane)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git log --graph --oneline --all` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-04

- [ ] **Question:** Production is degraded around Release unit; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Release unit](README.md#release-unit)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://metadata-api/health` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Metadata spine; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Metadata spine](README.md#metadata-spine)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow experiments search` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-06

- [ ] **Question:** Production is degraded around Environment promotion; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Promote immutable evidence/artifacts while configuration and credentials remain target-specific. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Human decision; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Human decision](README.md#human-decision)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git log --graph --oneline --all` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-08

- [ ] **Question:** Production is degraded around Reconciliation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Reconciliation](README.md#reconciliation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://metadata-api/health` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Multi-tenancy; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Multi-tenancy](README.md#multi-tenancy)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `mlflow experiments search` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-MP-10

- [ ] **Question:** Production is degraded around Exit and retirement; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [MLOps/LLMOps platform architecture — Exit and retirement](README.md#exit-and-retirement)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the MLOps/LLMOps platform architecture source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-01

- [ ] **Question:** Design a production MLOps/LLMOps platform architecture capability where Lifecycle state machine, Release unit and Human decision are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Human decision](README.md#human-decision)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Lifecycle state machine: Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. Release unit: Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. Human decision: Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production MLOps/LLMOps platform architecture capability where Control plane, Metadata spine and Reconciliation are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Control plane: Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. Metadata spine: Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. Reconciliation: Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-03

- [ ] **Question:** Design a production MLOps/LLMOps platform architecture capability where Data plane, Environment promotion and Multi-tenancy are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Data plane: Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. Environment promotion: Promote immutable evidence/artifacts while configuration and credentials remain target-specific. Multi-tenancy: Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production MLOps/LLMOps platform architecture capability where Release unit, Human decision and Exit and retirement are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Human decision](README.md#human-decision)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Release unit: Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. Human decision: Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. Exit and retirement: Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-05

- [ ] **Question:** Design a production MLOps/LLMOps platform architecture capability where Metadata spine, Reconciliation and Lifecycle state machine are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Metadata spine: Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. Reconciliation: Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. Lifecycle state machine: Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production MLOps/LLMOps platform architecture capability where Environment promotion, Multi-tenancy and Control plane are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Environment promotion: Promote immutable evidence/artifacts while configuration and credentials remain target-specific. Multi-tenancy: Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. Control plane: Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-07

- [ ] **Question:** Design a production MLOps/LLMOps platform architecture capability where Human decision, Exit and retirement and Data plane are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Human decision](README.md#human-decision)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Human decision: Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. Exit and retirement: Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. Data plane: Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production MLOps/LLMOps platform architecture capability where Reconciliation, Lifecycle state machine and Release unit are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Reconciliation: Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. Lifecycle state machine: Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. Release unit: Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-09

- [ ] **Question:** Design a production MLOps/LLMOps platform architecture capability where Multi-tenancy, Control plane and Metadata spine are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Multi-tenancy: Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. Control plane: Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. Metadata spine: Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production MLOps/LLMOps platform architecture capability where Exit and retirement, Data plane and Environment promotion are first-class requirements.
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Exit and retirement: Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. Data plane: Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. Environment promotion: Promote immutable evidence/artifacts while configuration and credentials remain target-specific. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated MLOps/LLMOps platform architecture options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Lifecycle state machine. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Lifecycle state machine](README.md#lifecycle-state-machine)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow experiments search` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Idea, experiment, candidate, validated, approved, deployed, monitored, deprecated and retired need explicit transitions. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Control plane. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Control plane](README.md#control-plane)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Metadata, orchestration, policy, registry, approval and reconciliation coordinate work without carrying every inference payload. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Data plane. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Data plane](README.md#data-plane)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git log --graph --oneline --all` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Training, batch, retrieval and serving compute move protected data and model work under tenant boundaries. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Release unit. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Release unit](README.md#release-unit)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://metadata-api/health` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Data/code/config/model/runtime/evaluator/policy identity must travel together instead of calling a weights file the release. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Metadata spine. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Metadata spine](README.md#metadata-spine)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow experiments search` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Stable run, dataset, model, release, deployment and request identifiers join otherwise separate systems. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Environment promotion. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Environment promotion](README.md#environment-promotion)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Promote immutable evidence/artifacts while configuration and credentials remain target-specific. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Human decision. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Human decision](README.md#human-decision)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git log --graph --oneline --all` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Risk-based approvals and exceptions retain actor, evidence, reason, expiry and rollback authority. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Reconciliation. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Reconciliation](README.md#reconciliation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://metadata-api/health` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Controllers compare desired release with observed deployment/index/evaluation state and retry idempotently. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Multi-tenancy. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Multi-tenancy](README.md#multi-tenancy)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `mlflow experiments search` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Identity, quota, compute, metadata, artifacts, networks and cost allocation require explicit isolation. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### MLOPS-LLMOPS-PLATFORM-ARCHITECTURE-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Exit and retirement. How do you lead it end to end?
> **Covered in:** [MLOps/LLMOps platform architecture — Exit and retirement](README.md#exit-and-retirement)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get workflows,pipelines,trainingjobs,inferenceservices -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Revoke endpoints/credentials, preserve required evidence, delete protected data/artifacts and update inventories. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify MLOps/LLMOps platform architecture from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: MLOps and LLMOps lifecycle](../README.md) · [Study note](README.md) · [Next: Experiment tracking and comparison →](../02-experiment-tracking/README.md)

<!-- reading-navigation:end -->
