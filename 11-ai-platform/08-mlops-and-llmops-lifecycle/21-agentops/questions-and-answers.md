# AgentOps and tool-using system operations — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-01

- [ ] **Question:** What is Run state machine, and why does it matter in AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Definition.** Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. **Mechanism.** A write normally passes validation and authorization, enters a buffer or transaction, reaches a durable medium, and may then replicate or become visible to readers. Caches and indexes accelerate access but introduce freshness and eviction behavior; snapshots preserve a point-in-time representation but application consistency depends on write ordering. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines capacity, latency, error, replication and integrity signals with a tested read or restore. Typical failures include exhausted bytes or inodes, throttling, stale replicas, corrupt metadata, topology mismatch, lost encryption keys and backups that exist but cannot reconstruct the application within RPO/RTO. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Run state machine matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AgentOps and tool-using system operations journey rather than an administrative green status alone.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-02

- [ ] **Question:** What is Tool contract, and why does it matter in AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Definition.** Typed input/output/error/idempotency/timeout and side-effect classification are versioned. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Tool contract matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AgentOps and tool-using system operations journey rather than an administrative green status alone.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-03

- [ ] **Question:** What is Authorization, and why does it matter in AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Authorization](README.md#authorization)

**Answer:** **Definition.** User and workload identity plus current resource context authorize every call at execution time. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Authorization matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AgentOps and tool-using system operations journey rather than an administrative green status alone.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-04

- [ ] **Question:** What is Capability, and why does it matter in AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Capability](README.md#capability)

**Answer:** **Definition.** Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Capability matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AgentOps and tool-using system operations journey rather than an administrative green status alone.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-05

- [ ] **Question:** What is Idempotency, and why does it matter in AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Idempotency](README.md#idempotency)

**Answer:** **Definition.** Stable operation key and result lookup handle timeout with unknown side effect. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Idempotency matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AgentOps and tool-using system operations journey rather than an administrative green status alone.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-06

- [ ] **Question:** What is Approval, and why does it matter in AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Approval](README.md#approval)

**Answer:** **Definition.** High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Approval matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AgentOps and tool-using system operations journey rather than an administrative green status alone.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-07

- [ ] **Question:** What is Budgets, and why does it matter in AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Budgets](README.md#budgets)

**Answer:** **Definition.** Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Budgets matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original AgentOps and tool-using system operations journey rather than an administrative green status alone.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-08

- [ ] **Code:** **Question:** What does `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'` help you verify for AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant AgentOps and tool-using system operations concept is Sandbox: Generated code/browser/file/network operations run in isolated constrained environments. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a AgentOps and tool-using system operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-09

- [ ] **Code:** **Question:** What does `python -m pytest tests/agent_tools -q` help you verify for AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `python -m pytest tests/agent_tools -q` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant AgentOps and tool-using system operations concept is Trace: Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a AgentOps and tool-using system operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JN-10

- [ ] **Code:** **Question:** What does `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` help you verify for AgentOps and tool-using system operations?
> **Covered in:** [AgentOps and tool-using system operations — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant AgentOps and tool-using system operations concept is Evaluation: Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a AgentOps and tool-using system operations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-01

- [ ] **Code:** **Question:** A basic Run state machine check fails. What would you do first using the CLI?
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pytest tests/agent_tools -q`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-02

- [ ] **Question:** A basic Tool contract check fails. What would you do first?
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Typed input/output/error/idempotency/timeout and side-effect classification are versioned. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-03

- [ ] **Question:** A basic Authorization check fails. What would you do first?
> **Covered in:** [AgentOps and tool-using system operations — Authorization](README.md#authorization)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s AGENT_API/runs/RUN_ID | jq`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** User and workload identity plus current resource context authorize every call at execution time. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-04

- [ ] **Code:** **Question:** A basic Capability check fails. What would you do first using the CLI?
> **Covered in:** [AgentOps and tool-using system operations — Capability](README.md#capability)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-05

- [ ] **Question:** A basic Idempotency check fails. What would you do first?
> **Covered in:** [AgentOps and tool-using system operations — Idempotency](README.md#idempotency)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pytest tests/agent_tools -q`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Stable operation key and result lookup handle timeout with unknown side effect. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-06

- [ ] **Question:** A basic Approval check fails. What would you do first?
> **Covered in:** [AgentOps and tool-using system operations — Approval](README.md#approval)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-07

- [ ] **Code:** **Question:** A basic Budgets check fails. What would you do first using the CLI?
> **Covered in:** [AgentOps and tool-using system operations — Budgets](README.md#budgets)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s AGENT_API/runs/RUN_ID | jq`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-08

- [ ] **Question:** A basic Sandbox check fails. What would you do first?
> **Covered in:** [AgentOps and tool-using system operations — Sandbox](README.md#sandbox)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Generated code/browser/file/network operations run in isolated constrained environments. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-09

- [ ] **Question:** A basic Trace check fails. What would you do first?
> **Covered in:** [AgentOps and tool-using system operations — Trace](README.md#trace)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m pytest tests/agent_tools -q`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-JP-10

- [ ] **Code:** **Question:** A basic Evaluation check fails. What would you do first using the CLI?
> **Covered in:** [AgentOps and tool-using system operations — Evaluation](README.md#evaluation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original AgentOps and tool-using system operations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-01

- [ ] **Question:** Compare Run state machine with Tool contract. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Definitions.** Run state machine: Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. Tool contract: Typed input/output/error/idempotency/timeout and side-effect classification are versioned. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-02

- [ ] **Question:** Compare Tool contract with Authorization. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Definitions.** Tool contract: Typed input/output/error/idempotency/timeout and side-effect classification are versioned. Authorization: User and workload identity plus current resource context authorize every call at execution time. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-03

- [ ] **Question:** Compare Authorization with Capability. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Capability](README.md#capability)

**Answer:** **Definitions.** Authorization: User and workload identity plus current resource context authorize every call at execution time. Capability: Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-04

- [ ] **Configuration review:** **Question:** Compare Capability with Idempotency. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Capability](README.md#capability)

**Answer:** **Definitions.** Capability: Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. Idempotency: Stable operation key and result lookup handle timeout with unknown side effect. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-05

- [ ] **Question:** Compare Idempotency with Approval. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Approval](README.md#approval)

**Answer:** **Definitions.** Idempotency: Stable operation key and result lookup handle timeout with unknown side effect. Approval: High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-06

- [ ] **Question:** Compare Approval with Budgets. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Approval](README.md#approval)

**Answer:** **Definitions.** Approval: High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. Budgets: Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-07

- [ ] **Configuration review:** **Question:** Compare Budgets with Sandbox. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Sandbox](README.md#sandbox)

**Answer:** **Definitions.** Budgets: Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. Sandbox: Generated code/browser/file/network operations run in isolated constrained environments. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-08

- [ ] **Question:** Compare Sandbox with Trace. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Sandbox](README.md#sandbox)

**Answer:** **Definitions.** Sandbox: Generated code/browser/file/network operations run in isolated constrained environments. Trace: Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-09

- [ ] **Question:** Compare Trace with Evaluation. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Evaluation](README.md#evaluation)

**Answer:** **Definitions.** Trace: Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. Evaluation: Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MN-10

- [ ] **Configuration review:** **Question:** Compare Evaluation with Run state machine. When would each change your design?
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Definitions.** Evaluation: Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. Run state machine: Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. **Mechanism.** In AgentOps and tool-using system operations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production AgentOps and tool-using system operations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Run state machine; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pytest tests/agent_tools -q` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-02

- [ ] **Question:** Production is degraded around Tool contract; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Typed input/output/error/idempotency/timeout and side-effect classification are versioned. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Authorization; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Authorization](README.md#authorization)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s AGENT_API/runs/RUN_ID | jq` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** User and workload identity plus current resource context authorize every call at execution time. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-04

- [ ] **Question:** Production is degraded around Capability; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Capability](README.md#capability)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Idempotency; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Idempotency](README.md#idempotency)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pytest tests/agent_tools -q` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Stable operation key and result lookup handle timeout with unknown side effect. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-06

- [ ] **Question:** Production is degraded around Approval; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Approval](README.md#approval)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Budgets; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Budgets](README.md#budgets)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s AGENT_API/runs/RUN_ID | jq` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-08

- [ ] **Question:** Production is degraded around Sandbox; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Sandbox](README.md#sandbox)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Generated code/browser/file/network operations run in isolated constrained environments. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Trace; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Trace](README.md#trace)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m pytest tests/agent_tools -q` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-MP-10

- [ ] **Question:** Production is degraded around Evaluation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AgentOps and tool-using system operations — Evaluation](README.md#evaluation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the AgentOps and tool-using system operations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-01

- [ ] **Question:** Design a production AgentOps and tool-using system operations capability where Run state machine, Capability and Budgets are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Run state machine: Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. Capability: Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. Budgets: Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production AgentOps and tool-using system operations capability where Tool contract, Idempotency and Sandbox are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Tool contract: Typed input/output/error/idempotency/timeout and side-effect classification are versioned. Idempotency: Stable operation key and result lookup handle timeout with unknown side effect. Sandbox: Generated code/browser/file/network operations run in isolated constrained environments. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-03

- [ ] **Question:** Design a production AgentOps and tool-using system operations capability where Authorization, Approval and Trace are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Authorization](README.md#authorization)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Authorization: User and workload identity plus current resource context authorize every call at execution time. Approval: High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. Trace: Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production AgentOps and tool-using system operations capability where Capability, Budgets and Evaluation are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Capability](README.md#capability)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Capability: Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. Budgets: Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. Evaluation: Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-05

- [ ] **Question:** Design a production AgentOps and tool-using system operations capability where Idempotency, Sandbox and Run state machine are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Idempotency: Stable operation key and result lookup handle timeout with unknown side effect. Sandbox: Generated code/browser/file/network operations run in isolated constrained environments. Run state machine: Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production AgentOps and tool-using system operations capability where Approval, Trace and Tool contract are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Approval: High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. Trace: Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. Tool contract: Typed input/output/error/idempotency/timeout and side-effect classification are versioned. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-07

- [ ] **Question:** Design a production AgentOps and tool-using system operations capability where Budgets, Evaluation and Authorization are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Authorization](README.md#authorization)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Budgets: Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. Evaluation: Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. Authorization: User and workload identity plus current resource context authorize every call at execution time. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production AgentOps and tool-using system operations capability where Sandbox, Run state machine and Capability are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Sandbox: Generated code/browser/file/network operations run in isolated constrained environments. Run state machine: Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. Capability: Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-09

- [ ] **Question:** Design a production AgentOps and tool-using system operations capability where Trace, Tool contract and Idempotency are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Trace: Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. Tool contract: Typed input/output/error/idempotency/timeout and side-effect classification are versioned. Idempotency: Stable operation key and result lookup handle timeout with unknown side effect. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production AgentOps and tool-using system operations capability where Evaluation, Authorization and Approval are first-class requirements.
> **Covered in:** [AgentOps and tool-using system operations — Approval](README.md#approval)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Evaluation: Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. Authorization: User and workload identity plus current resource context authorize every call at execution time. Approval: High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated AgentOps and tool-using system operations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Run state machine. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Run state machine](README.md#run-state-machine)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pytest tests/agent_tools -q` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Queued/running/waiting-approval/succeeded/failed/canceled/compensating states survive worker restart. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Tool contract. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Tool contract](README.md#tool-contract)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Typed input/output/error/idempotency/timeout and side-effect classification are versioned. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Authorization. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Authorization](README.md#authorization)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s AGENT_API/runs/RUN_ID | jq` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** User and workload identity plus current resource context authorize every call at execution time. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Capability. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Capability](README.md#capability)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Agent receives narrow short-lived tool/resource scope rather than ambient broad credentials. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Idempotency. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Idempotency](README.md#idempotency)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pytest tests/agent_tools -q` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Stable operation key and result lookup handle timeout with unknown side effect. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Approval. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Approval](README.md#approval)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** High-risk proposed action includes diff/target/effect/evidence/expiry and revalidates after approval. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Budgets. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Budgets](README.md#budgets)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s AGENT_API/runs/RUN_ID | jq` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Steps, wall time, tokens, spend, retries and external calls bound loops and denial-of-wallet. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Sandbox. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Sandbox](README.md#sandbox)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `opa eval --data policy --input proposed-tool-call.json 'data.tools.allow'` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Generated code/browser/file/network operations run in isolated constrained environments. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Trace. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Trace](README.md#trace)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m pytest tests/agent_tools -q` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Model/tool/policy/approval state transitions and safe arguments/results support replay and evaluation. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### AGENTOPS-AND-TOOL-USING-SYSTEM-OPERATIONS-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Evaluation. How do you lead it end to end?
> **Covered in:** [AgentOps and tool-using system operations — Evaluation](README.md#evaluation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python run_agent.py --fixture tests/task.json --max-steps 8 --dry-run` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Task success, policy compliance, tool correctness, recovery, cost and adversarial behavior are release gates. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify AgentOps and tool-using system operations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: LLM gateway and ProviderOps](../20-gateway-providerops/README.md) · [Study note](README.md) · [Next: LLM observability and quality telemetry →](../22-llm-observability/README.md)

<!-- reading-navigation:end -->
