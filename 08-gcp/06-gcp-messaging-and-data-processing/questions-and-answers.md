# GCP messaging and data processing — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-01

- [ ] **Question:** What is Pub/Sub, and why does it matter in GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Definition.** The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** A producer serializes and publishes a message, the service stores or routes it, a consumer receives it under a delivery contract, and acknowledgement advances or removes the item. At-least-once systems can redeliver after an unknown outcome, so stable operation identifiers and idempotent state changes are part of correctness. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes publish and consume rates, queue depth and age, processing latency, retry/dead-letter counts and end-to-end business completion. Poison messages, hot partitions, backlog growth, duplicate effects and retry storms are common; adding consumers helps only when the downstream dependency and partition model can absorb them. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Pub/Sub matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP messaging and data processing journey rather than an administrative green status alone.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-02

- [ ] **Question:** What is Dataflow, and why does it matter in GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Dataflow](README.md#dataflow)

**Answer:** **Definition.** The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** The mechanism receives configuration or runtime input, validates preconditions, changes or derives state, and exposes status to the next component. Synchronous parts return a direct outcome; asynchronous parts need durable status, reconciliation and idempotency because the original caller may disappear. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence shows the expected state transition and its owner, timestamp and reason. Invalid input, stale state, conflicting writers, hidden limits and dependencies that partially complete are the most useful failure categories to distinguish. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Dataflow matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP messaging and data processing journey rather than an administrative green status alone.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-03

- [ ] **Question:** What is Dataproc, and why does it matter in GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Dataproc](README.md#dataproc)

**Answer:** **Definition.** The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** The mechanism receives configuration or runtime input, validates preconditions, changes or derives state, and exposes status to the next component. Synchronous parts return a direct outcome; asynchronous parts need durable status, reconciliation and idempotency because the original caller may disappear. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence shows the expected state transition and its owner, timestamp and reason. Invalid input, stale state, conflicting writers, hidden limits and dependencies that partially complete are the most useful failure categories to distinguish. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Dataproc matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP messaging and data processing journey rather than an administrative green status alone.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-04

- [ ] **Question:** What is Cloud Composer, and why does it matter in GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Cloud Composer](README.md#cloud-composer)

**Answer:** **Definition.** The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** The mechanism receives configuration or runtime input, validates preconditions, changes or derives state, and exposes status to the next component. Synchronous parts return a direct outcome; asynchronous parts need durable status, reconciliation and idempotency because the original caller may disappear. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence shows the expected state transition and its owner, timestamp and reason. Invalid input, stale state, conflicting writers, hidden limits and dependencies that partially complete are the most useful failure categories to distinguish. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Cloud Composer matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP messaging and data processing journey rather than an administrative green status alone.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-05

- [ ] **Question:** What is Eventarc, and why does it matter in GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Eventarc](README.md#eventarc)

**Answer:** **Definition.** The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. **Mechanism.** A producer serializes and publishes a message, the service stores or routes it, a consumer receives it under a delivery contract, and acknowledgement advances or removes the item. At-least-once systems can redeliver after an unknown outcome, so stable operation identifiers and idempotent state changes are part of correctness. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes publish and consume rates, queue depth and age, processing latency, retry/dead-letter counts and end-to-end business completion. Poison messages, hot partitions, backlog growth, duplicate effects and retry storms are common; adding consumers helps only when the downstream dependency and partition model can absorb them. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Eventarc matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP messaging and data processing journey rather than an administrative green status alone.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-06

- [ ] **Question:** What is Workflows, and why does it matter in GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Workflows](README.md#workflows)

**Answer:** **Definition.** The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Workflows matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP messaging and data processing journey rather than an administrative green status alone.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-07

- [ ] **Question:** What is Kafka integration, and why does it matter in GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Definition.** The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** A producer serializes and publishes a message, the service stores or routes it, a consumer receives it under a delivery contract, and acknowledgement advances or removes the item. At-least-once systems can redeliver after an unknown outcome, so stable operation identifiers and idempotent state changes are part of correctness. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes publish and consume rates, queue depth and age, processing latency, retry/dead-letter counts and end-to-end business completion. Poison messages, hot partitions, backlog growth, duplicate effects and retry storms are common; adding consumers helps only when the downstream dependency and partition model can absorb them. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Kafka integration matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP messaging and data processing journey rather than an administrative green status alone.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-08

- [ ] **Code:** **Question:** What does `gcloud logging read 'severity>=ERROR' --limit=20` help you verify for GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `gcloud logging read 'severity>=ERROR' --limit=20` queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. **Mechanism.** The relevant GCP messaging and data processing concept is Dead-letter handling: The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a GCP messaging and data processing rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-09

- [ ] **Code:** **Question:** What does `gcloud auth list` help you verify for GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `gcloud auth list` establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. **Mechanism.** The relevant GCP messaging and data processing concept is Streaming and batch pipelines: The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a GCP messaging and data processing rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JN-10

- [ ] **Code:** **Question:** What does `gcloud config list` help you verify for GCP messaging and data processing?
> **Covered in:** [GCP messaging and data processing — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `gcloud config list` establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. **Mechanism.** The relevant GCP messaging and data processing concept is Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a GCP messaging and data processing rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-01

- [ ] **Code:** **Question:** A basic Pub/Sub check fails. What would you do first using the CLI?
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud auth list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-02

- [ ] **Question:** A basic Dataflow check fails. What would you do first?
> **Covered in:** [GCP messaging and data processing — Dataflow](README.md#dataflow)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud config list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-03

- [ ] **Question:** A basic Dataproc check fails. What would you do first?
> **Covered in:** [GCP messaging and data processing — Dataproc](README.md#dataproc)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud projects describe PROJECT`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-04

- [ ] **Code:** **Question:** A basic Cloud Composer check fails. What would you do first using the CLI?
> **Covered in:** [GCP messaging and data processing — Cloud Composer](README.md#cloud-composer)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud logging read 'severity>=ERROR' --limit=20`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-05

- [ ] **Question:** A basic Eventarc check fails. What would you do first?
> **Covered in:** [GCP messaging and data processing — Eventarc](README.md#eventarc)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud auth list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-06

- [ ] **Question:** A basic Workflows check fails. What would you do first?
> **Covered in:** [GCP messaging and data processing — Workflows](README.md#workflows)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud config list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-07

- [ ] **Code:** **Question:** A basic Kafka integration check fails. What would you do first using the CLI?
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud projects describe PROJECT`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-08

- [ ] **Question:** A basic Dead-letter handling check fails. What would you do first?
> **Covered in:** [GCP messaging and data processing — Dead-letter handling](README.md#dead-letter-handling)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud logging read 'severity>=ERROR' --limit=20`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-09

- [ ] **Question:** A basic Streaming and batch pipelines check fails. What would you do first?
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud auth list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-JP-10

- [ ] **Code:** **Question:** A basic Pub/Sub check fails. What would you do first using the CLI?
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud config list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP messaging and data processing client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-01

- [ ] **Question:** Compare Pub/Sub with Dataflow. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Dataflow](README.md#dataflow)

**Answer:** **Definitions.** Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Dataflow: The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-02

- [ ] **Question:** Compare Dataflow with Dataproc. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Dataflow](README.md#dataflow)

**Answer:** **Definitions.** Dataflow: The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Dataproc: The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-03

- [ ] **Question:** Compare Dataproc with Cloud Composer. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Cloud Composer](README.md#cloud-composer)

**Answer:** **Definitions.** Dataproc: The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Cloud Composer: The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-04

- [ ] **Configuration review:** **Question:** Compare Cloud Composer with Eventarc. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Cloud Composer](README.md#cloud-composer)

**Answer:** **Definitions.** Cloud Composer: The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Eventarc: The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-05

- [ ] **Question:** Compare Eventarc with Workflows. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Workflows](README.md#workflows)

**Answer:** **Definitions.** Eventarc: The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. Workflows: The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-06

- [ ] **Question:** Compare Workflows with Kafka integration. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Definitions.** Workflows: The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. Kafka integration: The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-07

- [ ] **Configuration review:** **Question:** Compare Kafka integration with Dead-letter handling. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Dead-letter handling](README.md#dead-letter-handling)

**Answer:** **Definitions.** Kafka integration: The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Dead-letter handling: The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-08

- [ ] **Question:** Compare Dead-letter handling with Streaming and batch pipelines. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Definitions.** Dead-letter handling: The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Streaming and batch pipelines: The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-09

- [ ] **Question:** Compare Streaming and batch pipelines with Pub/Sub. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Definitions.** Streaming and batch pipelines: The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MN-10

- [ ] **Configuration review:** **Question:** Compare Pub/Sub with Pub/Sub. When would each change your design?
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Definitions.** Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. **Mechanism.** In GCP messaging and data processing, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP messaging and data processing design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Pub/Sub; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud auth list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-02

- [ ] **Question:** Production is degraded around Dataflow; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Dataflow](README.md#dataflow)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud config list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Dataproc; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Dataproc](README.md#dataproc)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud projects describe PROJECT` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-04

- [ ] **Question:** Production is degraded around Cloud Composer; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Cloud Composer](README.md#cloud-composer)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud logging read 'severity>=ERROR' --limit=20` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Eventarc; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Eventarc](README.md#eventarc)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud auth list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-06

- [ ] **Question:** Production is degraded around Workflows; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Workflows](README.md#workflows)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud config list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Kafka integration; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud projects describe PROJECT` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-08

- [ ] **Question:** Production is degraded around Dead-letter handling; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Dead-letter handling](README.md#dead-letter-handling)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud logging read 'severity>=ERROR' --limit=20` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Streaming and batch pipelines; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud auth list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-MP-10

- [ ] **Question:** Production is degraded around Pub/Sub; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud config list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP messaging and data processing source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-01

- [ ] **Question:** Design a production GCP messaging and data processing capability where Pub/Sub, Cloud Composer and Kafka integration are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Cloud Composer: The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Kafka integration: The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production GCP messaging and data processing capability where Dataflow, Eventarc and Dead-letter handling are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Dead-letter handling](README.md#dead-letter-handling)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Dataflow: The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Eventarc: The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. Dead-letter handling: The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-03

- [ ] **Question:** Design a production GCP messaging and data processing capability where Dataproc, Workflows and Streaming and batch pipelines are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Dataproc: The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Workflows: The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. Streaming and batch pipelines: The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production GCP messaging and data processing capability where Cloud Composer, Kafka integration and Pub/Sub are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Cloud Composer: The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Kafka integration: The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-05

- [ ] **Question:** Design a production GCP messaging and data processing capability where Eventarc, Dead-letter handling and Pub/Sub are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Dead-letter handling](README.md#dead-letter-handling)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Eventarc: The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. Dead-letter handling: The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production GCP messaging and data processing capability where Workflows, Streaming and batch pipelines and Dataflow are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Workflows: The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. Streaming and batch pipelines: The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. Dataflow: The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-07

- [ ] **Question:** Design a production GCP messaging and data processing capability where Kafka integration, Pub/Sub and Dataproc are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Kafka integration: The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Dataproc: The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production GCP messaging and data processing capability where Dead-letter handling, Pub/Sub and Cloud Composer are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Dead-letter handling](README.md#dead-letter-handling)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Dead-letter handling: The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Cloud Composer: The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-09

- [ ] **Question:** Design a production GCP messaging and data processing capability where Streaming and batch pipelines, Dataflow and Eventarc are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Streaming and batch pipelines: The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. Dataflow: The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Eventarc: The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production GCP messaging and data processing capability where Pub/Sub, Dataproc and Workflows are first-class requirements.
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Pub/Sub: The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Dataproc: The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Workflows: The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP messaging and data processing options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Pub/Sub. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud auth list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Dataflow. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Dataflow](README.md#dataflow)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud config list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Dataflow refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Dataproc. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Dataproc](README.md#dataproc)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud projects describe PROJECT` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Dataproc refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Cloud Composer. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Cloud Composer](README.md#cloud-composer)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud logging read 'severity>=ERROR' --limit=20` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Cloud Composer refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Eventarc. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Eventarc](README.md#eventarc)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud auth list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Eventarc is an asynchronous hand-off mechanism that decouples producers from consumers and therefore must define ordering, retention, delivery, acknowledgement, retry and backlog behavior within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Workflows. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Workflows](README.md#workflows)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud config list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Workflows describes the ordered states from creation or submission through active use, change, failure, recovery and retirement, including who owns each transition within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Kafka integration. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Kafka integration](README.md#kafka-integration)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud projects describe PROJECT` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Kafka integration refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Dead-letter handling. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Dead-letter handling](README.md#dead-letter-handling)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud logging read 'severity>=ERROR' --limit=20` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Dead-letter handling refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Streaming and batch pipelines. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Streaming and batch pipelines](README.md#streaming-and-batch-pipelines)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud auth list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Streaming and batch pipelines is an ordered or dependency-aware chain of stages that transforms versioned inputs into outputs and records status so failed work can be retried, resumed or rolled back safely within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-MESSAGING-AND-DATA-PROCESSING-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Pub/Sub. How do you lead it end to end?
> **Covered in:** [GCP messaging and data processing — Pub/Sub](README.md#pubsub)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud config list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The term Pub/Sub refers to a Google Cloud control- or data-plane mechanism bounded by resource hierarchy, location, identity, quota and service contract within GCP messaging and data processing. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP messaging and data processing from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: GCP storage and databases](../05-gcp-storage-and-databases/README.md) · [Study note](README.md) · [Next: Vertex AI and GCP AI platform →](../07-vertex-ai-and-gcp-ai-platform/README.md)

<!-- reading-navigation:end -->
