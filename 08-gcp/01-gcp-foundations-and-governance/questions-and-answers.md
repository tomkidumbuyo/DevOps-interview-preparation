# GCP foundations and governance — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-01

- [ ] **Question:** What is Organizations, and why does it matter in GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Organizations](README.md#organizations)

**Answer:** **Definition.** A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Organizations matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP foundations and governance journey rather than an administrative green status alone.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-02

- [ ] **Question:** What is Folders, and why does it matter in GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Folders](README.md#folders)

**Answer:** **Definition.** A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Folders matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP foundations and governance journey rather than an administrative green status alone.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-03

- [ ] **Question:** What is Projects, and why does it matter in GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Projects](README.md#projects)

**Answer:** **Definition.** A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Projects matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP foundations and governance journey rather than an administrative green status alone.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-04

- [ ] **Question:** What is Billing accounts, and why does it matter in GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Billing accounts](README.md#billing-accounts)

**Answer:** **Definition.** A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Billing accounts matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP foundations and governance journey rather than an administrative green status alone.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-05

- [ ] **Question:** What is Resource hierarchy, and why does it matter in GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Definition.** The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. **Mechanism.** An asset or service receives an owner, lifecycle state, data/risk class, policy, budget and evidence requirements. Usage is attributed to stable organizational dimensions, compared with outcome and SLO, and reviewed through changes, exceptions and retirement; automated guardrails enforce the decisions at the relevant control point. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects an accountable owner and approved intent to runtime inventory, audit and unit cost. Orphaned resources, permanent exceptions, unallocated shared cost, vanity utilization and savings that damage reliability or quality are common failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Resource hierarchy matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP foundations and governance journey rather than an administrative green status alone.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-06

- [ ] **Question:** What is IAM principals, and why does it matter in GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — IAM principals](README.md#iam-principals)

**Answer:** **Definition.** IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which IAM principals matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP foundations and governance journey rather than an administrative green status alone.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-07

- [ ] **Question:** What is Roles, and why does it matter in GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Roles](README.md#roles)

**Answer:** **Definition.** A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Roles matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original GCP foundations and governance journey rather than an administrative green status alone.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-08

- [ ] **Code:** **Question:** What does `gcloud logging read 'severity>=ERROR' --limit=20` help you verify for GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `gcloud logging read 'severity>=ERROR' --limit=20` queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. **Mechanism.** The relevant GCP foundations and governance concept is Service accounts: A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a GCP foundations and governance rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-09

- [ ] **Code:** **Question:** What does `gcloud auth list` help you verify for GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `gcloud auth list` establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. **Mechanism.** The relevant GCP foundations and governance concept is Workload Identity Federation: Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a GCP foundations and governance rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JN-10

- [ ] **Code:** **Question:** What does `gcloud config list` help you verify for GCP foundations and governance?
> **Covered in:** [GCP foundations and governance — Worked command and configuration example](README.md#worked-command-and-configuration-example)

**Answer:** **Purpose.** `gcloud config list` establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. **Mechanism.** The relevant GCP foundations and governance concept is Organization policies: Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a GCP foundations and governance rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-01

- [ ] **Code:** **Question:** A basic Organizations check fails. What would you do first using the CLI?
> **Covered in:** [GCP foundations and governance — Organizations](README.md#organizations)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud auth list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-02

- [ ] **Question:** A basic Folders check fails. What would you do first?
> **Covered in:** [GCP foundations and governance — Folders](README.md#folders)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud config list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-03

- [ ] **Question:** A basic Projects check fails. What would you do first?
> **Covered in:** [GCP foundations and governance — Projects](README.md#projects)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud projects describe PROJECT`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-04

- [ ] **Code:** **Question:** A basic Billing accounts check fails. What would you do first using the CLI?
> **Covered in:** [GCP foundations and governance — Billing accounts](README.md#billing-accounts)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud logging read 'severity>=ERROR' --limit=20`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-05

- [ ] **Question:** A basic Resource hierarchy check fails. What would you do first?
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud auth list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-06

- [ ] **Question:** A basic IAM principals check fails. What would you do first?
> **Covered in:** [GCP foundations and governance — IAM principals](README.md#iam-principals)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud config list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-07

- [ ] **Code:** **Question:** A basic Roles check fails. What would you do first using the CLI?
> **Covered in:** [GCP foundations and governance — Roles](README.md#roles)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud projects describe PROJECT`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-08

- [ ] **Question:** A basic Service accounts check fails. What would you do first?
> **Covered in:** [GCP foundations and governance — Service accounts](README.md#service-accounts)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud logging read 'severity>=ERROR' --limit=20`. This queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-09

- [ ] **Question:** A basic Workload Identity Federation check fails. What would you do first?
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud auth list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-JP-10

- [ ] **Code:** **Question:** A basic Organization policies check fails. What would you do first using the CLI?
> **Covered in:** [GCP foundations and governance — Organization policies](README.md#organization-policies)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `gcloud config list`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original GCP foundations and governance client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-01

- [ ] **Question:** Compare Organizations with Folders. When would each change your design?
> **Covered in:** [GCP foundations and governance — Organizations](README.md#organizations)

**Answer:** **Definitions.** Organizations: A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. Folders: A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-02

- [ ] **Question:** Compare Folders with Projects. When would each change your design?
> **Covered in:** [GCP foundations and governance — Folders](README.md#folders)

**Answer:** **Definitions.** Folders: A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. Projects: A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-03

- [ ] **Question:** Compare Projects with Billing accounts. When would each change your design?
> **Covered in:** [GCP foundations and governance — Billing accounts](README.md#billing-accounts)

**Answer:** **Definitions.** Projects: A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. Billing accounts: A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-04

- [ ] **Configuration review:** **Question:** Compare Billing accounts with Resource hierarchy. When would each change your design?
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Definitions.** Billing accounts: A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. Resource hierarchy: The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-05

- [ ] **Question:** Compare Resource hierarchy with IAM principals. When would each change your design?
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Definitions.** Resource hierarchy: The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. IAM principals: IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-06

- [ ] **Question:** Compare IAM principals with Roles. When would each change your design?
> **Covered in:** [GCP foundations and governance — IAM principals](README.md#iam-principals)

**Answer:** **Definitions.** IAM principals: IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. Roles: A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-07

- [ ] **Configuration review:** **Question:** Compare Roles with Service accounts. When would each change your design?
> **Covered in:** [GCP foundations and governance — Service accounts](README.md#service-accounts)

**Answer:** **Definitions.** Roles: A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. Service accounts: A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-08

- [ ] **Question:** Compare Service accounts with Workload Identity Federation. When would each change your design?
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Definitions.** Service accounts: A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. Workload Identity Federation: Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-09

- [ ] **Question:** Compare Workload Identity Federation with Organization policies. When would each change your design?
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Definitions.** Workload Identity Federation: Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. Organization policies: Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MN-10

- [ ] **Configuration review:** **Question:** Compare Organization policies with Organizations. When would each change your design?
> **Covered in:** [GCP foundations and governance — Organization policies](README.md#organization-policies)

**Answer:** **Definitions.** Organization policies: Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. Organizations: A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. **Mechanism.** In GCP foundations and governance, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production GCP foundations and governance design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Organizations; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Organizations](README.md#organizations)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud auth list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-02

- [ ] **Question:** Production is degraded around Folders; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Folders](README.md#folders)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud config list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Projects; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Projects](README.md#projects)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud projects describe PROJECT` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-04

- [ ] **Question:** Production is degraded around Billing accounts; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Billing accounts](README.md#billing-accounts)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud logging read 'severity>=ERROR' --limit=20` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Resource hierarchy; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud auth list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-06

- [ ] **Question:** Production is degraded around IAM principals; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — IAM principals](README.md#iam-principals)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud config list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Roles; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Roles](README.md#roles)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud projects describe PROJECT` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-08

- [ ] **Question:** Production is degraded around Service accounts; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Service accounts](README.md#service-accounts)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud logging read 'severity>=ERROR' --limit=20` plus recent changes and the user-facing SLI. The command queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Workload Identity Federation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud auth list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-MP-10

- [ ] **Question:** Production is degraded around Organization policies; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP foundations and governance — Organization policies](README.md#organization-policies)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `gcloud config list` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the GCP foundations and governance source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-01

- [ ] **Question:** Design a production GCP foundations and governance capability where Organizations, Billing accounts and Roles are first-class requirements.
> **Covered in:** [GCP foundations and governance — Billing accounts](README.md#billing-accounts)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Organizations: A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. Billing accounts: A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. Roles: A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production GCP foundations and governance capability where Folders, Resource hierarchy and Service accounts are first-class requirements.
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Folders: A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. Resource hierarchy: The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. Service accounts: A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-03

- [ ] **Question:** Design a production GCP foundations and governance capability where Projects, IAM principals and Workload Identity Federation are first-class requirements.
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Projects: A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. IAM principals: IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. Workload Identity Federation: Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production GCP foundations and governance capability where Billing accounts, Roles and Organization policies are first-class requirements.
> **Covered in:** [GCP foundations and governance — Organization policies](README.md#organization-policies)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Billing accounts: A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. Roles: A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. Organization policies: Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-05

- [ ] **Question:** Design a production GCP foundations and governance capability where Resource hierarchy, Service accounts and Organizations are first-class requirements.
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Resource hierarchy: The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. Service accounts: A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. Organizations: A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production GCP foundations and governance capability where IAM principals, Workload Identity Federation and Folders are first-class requirements.
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** IAM principals: IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. Workload Identity Federation: Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. Folders: A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-07

- [ ] **Question:** Design a production GCP foundations and governance capability where Roles, Organization policies and Projects are first-class requirements.
> **Covered in:** [GCP foundations and governance — Organization policies](README.md#organization-policies)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Roles: A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. Organization policies: Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. Projects: A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production GCP foundations and governance capability where Service accounts, Organizations and Billing accounts are first-class requirements.
> **Covered in:** [GCP foundations and governance — Billing accounts](README.md#billing-accounts)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Service accounts: A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. Organizations: A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. Billing accounts: A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-09

- [ ] **Question:** Design a production GCP foundations and governance capability where Workload Identity Federation, Folders and Resource hierarchy are first-class requirements.
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Workload Identity Federation: Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. Folders: A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. Resource hierarchy: The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production GCP foundations and governance capability where Organization policies, Projects and IAM principals are first-class requirements.
> **Covered in:** [GCP foundations and governance — Organization policies](README.md#organization-policies)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Organization policies: Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. Projects: A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. IAM principals: IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated GCP foundations and governance options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Organizations. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Organizations](README.md#organizations)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud auth list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Google Cloud organization is the top-level resource representing a company or institutional domain. It is the ancestor for folders and projects and the highest normal inheritance point for IAM and organization policy. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Folders. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Folders](README.md#folders)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud config list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Google Cloud folder is an optional grouping node between the organization and projects. Folders model departments, environments or policy boundaries and pass inherited IAM and organization policies to their descendants. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Projects. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Projects](README.md#projects)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud projects describe PROJECT` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Google Cloud project is the main ownership, API enablement, quota and billing container for cloud resources. Resources belong to a project even when their data plane is regional, zonal or global. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Billing accounts. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Billing accounts](README.md#billing-accounts)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud logging read 'severity>=ERROR' --limit=20` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A billing account is the payment and invoicing relationship linked to one or more projects. Linking a project enables billable use but does not grant IAM access to that project's resources. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Resource hierarchy. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Resource hierarchy](README.md#resource-hierarchy)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud auth list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** The Google Cloud resource hierarchy is the organization → folder → project → resource ancestry used for policy inheritance, ownership and inventory. A child receives applicable ancestor policy in addition to policy set directly on it. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving IAM principals. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — IAM principals](README.md#iam-principals)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud config list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** IAM principals are the identities that can request Google Cloud actions, including users, groups, service accounts, workforce identities and federated workload identities. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Roles. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Roles](README.md#roles)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud projects describe PROJECT` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Google Cloud IAM role is a named bundle of permissions. Basic, predefined and custom roles differ in scope and maintenance; a role grants nothing until an IAM policy binds it to a principal on a resource. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Service accounts. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Service accounts](README.md#service-accounts)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud logging read 'severity>=ERROR' --limit=20` as one checkpoint because it queries Google Cloud's control plane using the active account, project and location; compare the returned resource with source-controlled intent. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** A Google Cloud service account is a project-owned workload identity used by applications and automation. Code should obtain short-lived credentials for it instead of distributing long-lived service-account keys. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Workload Identity Federation. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Workload Identity Federation](README.md#workload-identity-federation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud auth list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Workload Identity Federation exchanges a trusted external OIDC or AWS identity assertion for short-lived Google Cloud credentials without storing a Google service-account key in the external system. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GCP-FOUNDATIONS-AND-GOVERNANCE-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Organization policies. How do you lead it end to end?
> **Covered in:** [GCP foundations and governance — Organization policies](README.md#organization-policies)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `gcloud config list` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Organization Policy Service applies inherited constraints to resource configuration, such as allowed locations or whether a capability may be enabled. It limits what configurations are permitted but does not replace IAM authorization. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify GCP foundations and governance from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Back: Google Cloud Platform](../README.md) · [Study note](README.md) · [Next: GCP networking →](../02-gcp-networking/README.md)

<!-- reading-navigation:end -->
