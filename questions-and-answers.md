# Mega DevOps and AI Platform interview questions and answers

> Mark a question complete by changing `- [ ]` to `- [x]`. This bank contains 180 answered questions; every topic folder has its own additional bank.

# General DevOps foundations — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-01

- [ ] **Question:** What is Linux resource path, and why does it matter in General DevOps foundations?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Linux resource path matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original General DevOps foundations journey rather than an administrative green status alone.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-02

- [ ] **Question:** What is Network request path, and why does it matter in General DevOps foundations?
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Definition.** DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. **Mechanism.** A real request crosses several independent states: name resolution returns an address, the source selects a route and source address, link or overlay forwarding reaches the next hop, stateful or stateless policy evaluates the flow, transport establishes communication, and TLS/application protocols negotiate their own contract. The return path must also work. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence progresses layer by layer: correct name/address, expected route, permitted flow, listening endpoint, successful handshake and valid application response. Timeouts, refusals, resets and protocol errors mean different layers; packet loss, MTU, asymmetric paths, connection-state exhaustion and proxy timeout mismatch are common production failures. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Network request path matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original General DevOps foundations journey rather than an administrative green status alone.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-03

- [ ] **Question:** What is Distributed failure, and why does it matter in General DevOps foundations?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. **Mechanism.** A caller assigns a deadline and attempt budget, classifies an error, applies bounded backoff with jitter only when retry is safe, and propagates capacity limits upstream. Circuit breakers, bulkheads and load shedding prevent one dependency from consuming every worker; recovery probes and gradual traffic return avoid a second overload. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence shows bounded queues, stable retry ratios and recovery within an objective. Unbounded retries, identical timeouts at every layer, non-idempotent replays, shared resource pools and failover to an unqualified target commonly turn a small fault into an outage. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Distributed failure matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original General DevOps foundations journey rather than an administrative green status alone.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-04

- [ ] **Question:** What is Git delivery, and why does it matter in General DevOps foundations?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Git delivery matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original General DevOps foundations journey rather than an administrative green status alone.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-05

- [ ] **Question:** What is Container isolation, and why does it matter in General DevOps foundations?
> **Covered in:** [Training platform operations — Isolation](11-ai-platform/08-mlops-and-llmops-lifecycle/06-training-platform/README.md#isolation)

**Answer:** **Definition.** Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Container isolation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original General DevOps foundations journey rather than an administrative green status alone.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-06

- [ ] **Question:** What is CI/CD security, and why does it matter in General DevOps foundations?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** Untrusted code near runner credentials requires isolation, pinning and short-lived identity. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which CI/CD security matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original General DevOps foundations journey rather than an administrative green status alone.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-07

- [ ] **Question:** What is Observability, and why does it matter in General DevOps foundations?
> **Covered in:** [Kubernetes admission policies and webhooks — Observability](06-kubernetes/05-security/04-admission-policies-webhooks/README.md#observability)

**Answer:** **Definition.** Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. **Mechanism.** Instrumentation observes a defined event or state, attaches bounded context, transports the signal, stores it under retention rules and makes it queryable. A useful signal has documented semantics and can distinguish at least two competing explanations; correlation identifiers belong in logs or traces rather than unbounded metric labels. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes coverage of the user journey, known collection delay and actionable SLO or saturation views. Missing telemetry, sampling bias, cardinality explosions, sensitive payload capture and alert thresholds disconnected from user impact are failures of the observability system itself. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Observability matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original General DevOps foundations journey rather than an administrative green status alone.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-08

- [ ] **Code:** **Question:** What does `docker inspect CONTAINER` help you verify for General DevOps foundations?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Purpose.** `docker inspect CONTAINER` shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. **Mechanism.** The relevant General DevOps foundations concept is SLO/error budget: User outcome target and burn rate guide paging and risk decisions. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a General DevOps foundations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-09

- [ ] **Code:** **Question:** What does `uptime; vmstat 1; iostat -xz 1` help you verify for General DevOps foundations?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Purpose.** `uptime; vmstat 1; iostat -xz 1` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant General DevOps foundations concept is Incident response: Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a General DevOps foundations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JN-10

- [ ] **Code:** **Question:** What does `ip route; ss -lntp; dig NAME; curl -v URL` help you verify for General DevOps foundations?
> **Covered in:** [Essential Linux commands — Network: ip, ss, ping, traceroute, dig, nslookup, curl, wget, tcpdump](02-linux/12-essential-linux-commands/README.md#network-ip-ss-ping-traceroute-dig-nslookup-curl-wget-tcpdump)

**Answer:** **Purpose.** `ip route; ss -lntp; dig NAME; curl -v URL` tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. **Mechanism.** The relevant General DevOps foundations concept is Disaster recovery: Protected backup plus timed application restore/failover proves RPO and RTO. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a General DevOps foundations rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-01

- [ ] **Code:** **Question:** A basic Linux resource path check fails. What would you do first using the CLI?
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `uptime; vmstat 1; iostat -xz 1`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-02

- [ ] **Question:** A basic Network request path check fails. What would you do first?
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `ip route; ss -lntp; dig NAME; curl -v URL`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-03

- [ ] **Question:** A basic Distributed failure check fails. What would you do first?
> **Covered in:** [Pulumi — C#](09-iac-delivery/02-pulumi/README.md#c)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git log --graph --oneline --all`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-04

- [ ] **Code:** **Question:** A basic Git delivery check fails. What would you do first using the CLI?
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker inspect CONTAINER`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-05

- [ ] **Question:** A basic Container isolation check fails. What would you do first?
> **Covered in:** [Training platform operations — Isolation](11-ai-platform/08-mlops-and-llmops-lifecycle/06-training-platform/README.md#isolation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `uptime; vmstat 1; iostat -xz 1`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-06

- [ ] **Question:** A basic CI/CD security check fails. What would you do first?
> **Covered in:** [Pulumi — C#](09-iac-delivery/02-pulumi/README.md#c)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `ip route; ss -lntp; dig NAME; curl -v URL`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Untrusted code near runner credentials requires isolation, pinning and short-lived identity. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-07

- [ ] **Code:** **Question:** A basic Observability check fails. What would you do first using the CLI?
> **Covered in:** [Kubernetes admission policies and webhooks — Observability](06-kubernetes/05-security/04-admission-policies-webhooks/README.md#observability)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `git log --graph --oneline --all`. This inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-08

- [ ] **Question:** A basic SLO/error budget check fails. What would you do first?
> **Covered in:** [Cluster Autoscaler, Karpenter and node lifecycle — Budget](06-kubernetes/06-scheduling-autoscaling/04-node-autoscaling/README.md#budget)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `docker inspect CONTAINER`. This shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** User outcome target and burn rate guide paging and risk decisions. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-09

- [ ] **Question:** A basic Incident response check fails. What would you do first?
> **Covered in:** [ML monitoring, drift and data quality — Response](11-ai-platform/08-mlops-and-llmops-lifecycle/14-monitoring-drift/README.md#response)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `uptime; vmstat 1; iostat -xz 1`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-JP-10

- [ ] **Code:** **Question:** A basic Disaster recovery check fails. What would you do first using the CLI?
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `ip route; ss -lntp; dig NAME; curl -v URL`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Protected backup plus timed application restore/failover proves RPO and RTO. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original General DevOps foundations client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-01

- [ ] **Question:** Compare Linux resource path with Network request path. When would each change your design?
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Definitions.** Linux resource path: CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. Network request path: DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-02

- [ ] **Question:** Compare Network request path with Distributed failure. When would each change your design?
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Definitions.** Network request path: DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. Distributed failure: Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-03

- [ ] **Question:** Compare Distributed failure with Git delivery. When would each change your design?
> **Covered in:** [Pulumi — Configuration](09-iac-delivery/02-pulumi/README.md#configuration)

**Answer:** **Definitions.** Distributed failure: Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. Git delivery: Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-04

- [ ] **Configuration review:** **Question:** Compare Git delivery with Container isolation. When would each change your design?
> **Covered in:** [Pulumi — Configuration](09-iac-delivery/02-pulumi/README.md#configuration)

**Answer:** **Definitions.** Git delivery: Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. Container isolation: Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-05

- [ ] **Question:** Compare Container isolation with CI/CD security. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Container isolation: Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. CI/CD security: Untrusted code near runner credentials requires isolation, pinning and short-lived identity. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-06

- [ ] **Question:** Compare CI/CD security with Observability. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** CI/CD security: Untrusted code near runner credentials requires isolation, pinning and short-lived identity. Observability: Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-07

- [ ] **Configuration review:** **Question:** Compare Observability with SLO/error budget. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Observability: Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. SLO/error budget: User outcome target and burn rate guide paging and risk decisions. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-08

- [ ] **Question:** Compare SLO/error budget with Incident response. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** SLO/error budget: User outcome target and burn rate guide paging and risk decisions. Incident response: Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-09

- [ ] **Question:** Compare Incident response with Disaster recovery. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Incident response: Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. Disaster recovery: Protected backup plus timed application restore/failover proves RPO and RTO. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MN-10

- [ ] **Configuration review:** **Question:** Compare Disaster recovery with Linux resource path. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Disaster recovery: Protected backup plus timed application restore/failover proves RPO and RTO. Linux resource path: CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. **Mechanism.** In General DevOps foundations, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production General DevOps foundations design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Linux resource path; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `uptime; vmstat 1; iostat -xz 1` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-02

- [ ] **Question:** Production is degraded around Network request path; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `ip route; ss -lntp; dig NAME; curl -v URL` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Distributed failure; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git log --graph --oneline --all` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-04

- [ ] **Question:** Production is degraded around Git delivery; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker inspect CONTAINER` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Container isolation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `uptime; vmstat 1; iostat -xz 1` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-06

- [ ] **Question:** Production is degraded around CI/CD security; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `ip route; ss -lntp; dig NAME; curl -v URL` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Untrusted code near runner credentials requires isolation, pinning and short-lived identity. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Observability; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Kubernetes admission policies and webhooks — Observability](06-kubernetes/05-security/04-admission-policies-webhooks/README.md#observability)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `git log --graph --oneline --all` plus recent changes and the user-facing SLI. The command inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-08

- [ ] **Question:** Production is degraded around SLO/error budget; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Cluster Autoscaler, Karpenter and node lifecycle — Budget](06-kubernetes/06-scheduling-autoscaling/04-node-autoscaling/README.md#budget)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `docker inspect CONTAINER` plus recent changes and the user-facing SLI. The command shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** User outcome target and burn rate guide paging and risk decisions. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around Incident response; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Incident response — Mitigation](10-operations/02-sre-and-reliability-engineering/05-incident-response/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `uptime; vmstat 1; iostat -xz 1` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-MP-10

- [ ] **Question:** Production is degraded around Disaster recovery; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `ip route; ss -lntp; dig NAME; curl -v URL` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Protected backup plus timed application restore/failover proves RPO and RTO. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the General DevOps foundations source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-01

- [ ] **Question:** Design a production General DevOps foundations capability where Linux resource path, Git delivery and Observability are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Linux resource path: CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. Git delivery: Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. Observability: Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production General DevOps foundations capability where Network request path, Container isolation and SLO/error budget are first-class requirements.
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Network request path: DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. Container isolation: Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. SLO/error budget: User outcome target and burn rate guide paging and risk decisions. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-03

- [ ] **Question:** Design a production General DevOps foundations capability where Distributed failure, CI/CD security and Incident response are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Distributed failure: Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. CI/CD security: Untrusted code near runner credentials requires isolation, pinning and short-lived identity. Incident response: Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production General DevOps foundations capability where Git delivery, Observability and Disaster recovery are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Git delivery: Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. Observability: Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. Disaster recovery: Protected backup plus timed application restore/failover proves RPO and RTO. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-05

- [ ] **Question:** Design a production General DevOps foundations capability where Container isolation, SLO/error budget and Linux resource path are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Container isolation: Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. SLO/error budget: User outcome target and burn rate guide paging and risk decisions. Linux resource path: CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production General DevOps foundations capability where CI/CD security, Incident response and Network request path are first-class requirements.
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** CI/CD security: Untrusted code near runner credentials requires isolation, pinning and short-lived identity. Incident response: Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. Network request path: DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-07

- [ ] **Question:** Design a production General DevOps foundations capability where Observability, Disaster recovery and Distributed failure are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Observability: Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. Disaster recovery: Protected backup plus timed application restore/failover proves RPO and RTO. Distributed failure: Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production General DevOps foundations capability where SLO/error budget, Linux resource path and Git delivery are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** SLO/error budget: User outcome target and burn rate guide paging and risk decisions. Linux resource path: CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. Git delivery: Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-09

- [ ] **Question:** Design a production General DevOps foundations capability where Incident response, Network request path and Container isolation are first-class requirements.
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Incident response: Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. Network request path: DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. Container isolation: Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production General DevOps foundations capability where Disaster recovery, Distributed failure and CI/CD security are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Disaster recovery: Protected backup plus timed application restore/failover proves RPO and RTO. Distributed failure: Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. CI/CD security: Untrusted code near runner credentials requires isolation, pinning and short-lived identity. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated General DevOps foundations options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Linux resource path. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `uptime; vmstat 1; iostat -xz 1` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** CPU, memory, disk, descriptors, processes, cgroups and kernel evidence explain application symptoms. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Network request path. How do you lead it end to end?
> **Covered in:** [Architecture — Request path](06-kubernetes/01-architecture/README.md#request-path)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `ip route; ss -lntp; dig NAME; curl -v URL` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** DNS, route, firewall/NAT, transport, TLS, proxy/load balancer and application must all agree. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Distributed failure. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git log --graph --oneline --all` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Timeout does not reveal whether a remote side effect occurred, so idempotency and reconciliation matter. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Git delivery. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker inspect CONTAINER` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Reviewed source must bind to immutable artifact, provenance, environment and rollback evidence. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Container isolation. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `uptime; vmstat 1; iostat -xz 1` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Namespaces, cgroups, capabilities, seccomp and mounts constrain ordinary host processes. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving CI/CD security. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `ip route; ss -lntp; dig NAME; curl -v URL` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Untrusted code near runner credentials requires isolation, pinning and short-lived identity. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Observability. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `git log --graph --oneline --all` as one checkpoint because it inspects the repository's object graph, refs or diff so a delivery decision is tied to exact content rather than an assumed branch name. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Metrics, logs, traces and profiles answer distinct questions under cardinality/privacy/cost limits. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving SLO/error budget. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `docker inspect CONTAINER` as one checkpoint because it shows image or runtime state so you can separate declared container configuration from the host process, namespaces and resource limits that actually exist. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** User outcome target and burn rate guide paging and risk decisions. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Incident response. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `uptime; vmstat 1; iostat -xz 1` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Command, containment, evidence, reversible mitigation, verification and prevention form the lifecycle. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-GENERAL-DEVOPS-FOUNDATIONS-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Disaster recovery. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `ip route; ss -lntp; dig NAME; curl -v URL` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Protected backup plus timed application restore/failover proves RPO and RTO. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify General DevOps foundations from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

# Cloud, Kubernetes and Infrastructure as Code — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-01

- [ ] **Question:** What is Cloud account/project, and why does it matter in Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** Ownership, quota, billing and policy boundary reduces blast radius. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Cloud account/project matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Cloud, Kubernetes and Infrastructure as Code journey rather than an administrative green status alone.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-02

- [ ] **Question:** What is Workload identity, and why does it matter in Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Definition.** Short-lived federated runtime credentials replace static cloud keys. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Workload identity matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Cloud, Kubernetes and Infrastructure as Code journey rather than an administrative green status alone.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-03

- [ ] **Question:** What is Kubernetes reconciliation, and why does it matter in Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Packaging Extensions — Reconciliation](06-kubernetes/07-packaging-extensions/README.md#reconciliation)

**Answer:** **Definition.** API/controllers/scheduler/kubelet converge desired state through plugins/runtime. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Kubernetes reconciliation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Cloud, Kubernetes and Infrastructure as Code journey rather than an administrative green status alone.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-04

- [ ] **Question:** What is Kubernetes networking, and why does it matter in Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. **Mechanism.** A real request crosses several independent states: name resolution returns an address, the source selects a route and source address, link or overlay forwarding reaches the next hop, stateful or stateless policy evaluates the flow, transport establishes communication, and TLS/application protocols negotiate their own contract. The return path must also work. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence progresses layer by layer: correct name/address, expected route, permitted flow, listening endpoint, successful handshake and valid application response. Timeouts, refusals, resets and protocol errors mean different layers; packet loss, MTU, asymmetric paths, connection-state exhaustion and proxy timeout mismatch are common production failures. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Kubernetes networking matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Cloud, Kubernetes and Infrastructure as Code journey rather than an administrative green status alone.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-05

- [ ] **Question:** What is Kubernetes storage, and why does it matter in Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Linux and operating systems — Storage](02-linux/README.md#storage)

**Answer:** **Definition.** PVC/StorageClass/CSI/topology/snapshot bind state to workloads. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Kubernetes storage matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Cloud, Kubernetes and Infrastructure as Code journey rather than an administrative green status alone.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-06

- [ ] **Question:** What is Autoscaling, and why does it matter in Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Gateway Load Balancer — Autoscaling](07-aws/04-load-balancing/03-gwlb/README.md#autoscaling)

**Answer:** **Definition.** Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Autoscaling matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Cloud, Kubernetes and Infrastructure as Code journey rather than an administrative green status alone.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-07

- [ ] **Question:** What is Terraform state, and why does it matter in Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Agents and tool-using AI — State](11-ai-platform/09-agents-and-tool-using-ai/README.md#state)

**Answer:** **Definition.** Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. **Mechanism.** The lifecycle begins with versioned intent, validates syntax and policy, resolves dependencies, builds or selects immutable inputs, produces a diff or release plan, changes the target in bounded waves, and records status. Reconciliation keeps desired and observed state aligned; rollback is another tested state transition, not merely a command name. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence links source revision, review, test, artifact digest, signer/provenance, deployment target and user-facing verification. Mutable tags, environment-specific rebuilds, unpinned dependencies, non-idempotent migrations and controllers fighting emergency changes are recurring failure modes. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Terraform state matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Cloud, Kubernetes and Infrastructure as Code journey rather than an administrative green status alone.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-08

- [ ] **Code:** **Question:** What does `aws sts get-caller-identity` help you verify for Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Purpose.** `aws sts get-caller-identity` establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. **Mechanism.** The relevant Cloud, Kubernetes and Infrastructure as Code concept is Pulumi Outputs: Asynchronous dependency/secret values must stay inside engine-aware transformations. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Cloud, Kubernetes and Infrastructure as Code rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-09

- [ ] **Code:** **Question:** What does `kubectl get pods,nodes -A -o wide` help you verify for Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Purpose.** `kubectl get pods,nodes -A -o wide` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant Cloud, Kubernetes and Infrastructure as Code concept is GitOps: Pull reconciliation audits and repairs drift but can amplify bad desired state. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Cloud, Kubernetes and Infrastructure as Code rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JN-10

- [ ] **Code:** **Question:** What does `terraform plan -out=tfplan` help you verify for Cloud, Kubernetes and Infrastructure as Code?
> **Covered in:** [Terraform — Plan](09-iac-delivery/01-terraform/README.md#plan)

**Answer:** **Purpose.** `terraform plan -out=tfplan` checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. **Mechanism.** The relevant Cloud, Kubernetes and Infrastructure as Code concept is Cloud economics: Fixed capacity, work units, storage, network and telemetry combine into unit cost. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Cloud, Kubernetes and Infrastructure as Code rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-01

- [ ] **Code:** **Question:** A basic Cloud account/project check fails. What would you do first using the CLI?
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get pods,nodes -A -o wide`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Ownership, quota, billing and policy boundary reduces blast radius. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-02

- [ ] **Question:** A basic Workload identity check fails. What would you do first?
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform plan -out=tfplan`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Short-lived federated runtime credentials replace static cloud keys. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-03

- [ ] **Question:** A basic Kubernetes reconciliation check fails. What would you do first?
> **Covered in:** [CI/CD — Reconciliation](09-iac-delivery/03-ci-cd/README.md#reconciliation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `pulumi preview --diff`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** API/controllers/scheduler/kubelet converge desired state through plugins/runtime. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-04

- [ ] **Code:** **Question:** A basic Kubernetes networking check fails. What would you do first using the CLI?
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `aws sts get-caller-identity`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-05

- [ ] **Question:** A basic Kubernetes storage check fails. What would you do first?
> **Covered in:** [Linux and operating systems — Storage](02-linux/README.md#storage)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get pods,nodes -A -o wide`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** PVC/StorageClass/CSI/topology/snapshot bind state to workloads. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-06

- [ ] **Question:** A basic Autoscaling check fails. What would you do first?
> **Covered in:** [Gateway Load Balancer — Autoscaling](07-aws/04-load-balancing/03-gwlb/README.md#autoscaling)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform plan -out=tfplan`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-07

- [ ] **Code:** **Question:** A basic Terraform state check fails. What would you do first using the CLI?
> **Covered in:** [Agents and tool-using AI — State](11-ai-platform/09-agents-and-tool-using-ai/README.md#state)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `pulumi preview --diff`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-08

- [ ] **Question:** A basic Pulumi Outputs check fails. What would you do first?
> **Covered in:** [Pulumi — Outputs](09-iac-delivery/02-pulumi/README.md#outputs)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `aws sts get-caller-identity`. This establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Asynchronous dependency/secret values must stay inside engine-aware transformations. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-09

- [ ] **Question:** A basic GitOps check fails. What would you do first?
> **Covered in:** [Pulumi — C#](09-iac-delivery/02-pulumi/README.md#c)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get pods,nodes -A -o wide`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Pull reconciliation audits and repairs drift but can amplify bad desired state. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-JP-10

- [ ] **Code:** **Question:** A basic Cloud economics check fails. What would you do first using the CLI?
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `terraform plan -out=tfplan`. This checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Fixed capacity, work units, storage, network and telemetry combine into unit cost. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Cloud, Kubernetes and Infrastructure as Code client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-01

- [ ] **Question:** Compare Cloud account/project with Workload identity. When would each change your design?
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Definitions.** Cloud account/project: Ownership, quota, billing and policy boundary reduces blast radius. Workload identity: Short-lived federated runtime credentials replace static cloud keys. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-02

- [ ] **Question:** Compare Workload identity with Kubernetes reconciliation. When would each change your design?
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Definitions.** Workload identity: Short-lived federated runtime credentials replace static cloud keys. Kubernetes reconciliation: API/controllers/scheduler/kubelet converge desired state through plugins/runtime. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-03

- [ ] **Question:** Compare Kubernetes reconciliation with Kubernetes networking. When would each change your design?
> **Covered in:** [Pulumi — Configuration](09-iac-delivery/02-pulumi/README.md#configuration)

**Answer:** **Definitions.** Kubernetes reconciliation: API/controllers/scheduler/kubelet converge desired state through plugins/runtime. Kubernetes networking: Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-04

- [ ] **Configuration review:** **Question:** Compare Kubernetes networking with Kubernetes storage. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Kubernetes networking: Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. Kubernetes storage: PVC/StorageClass/CSI/topology/snapshot bind state to workloads. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-05

- [ ] **Question:** Compare Kubernetes storage with Autoscaling. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Kubernetes storage: PVC/StorageClass/CSI/topology/snapshot bind state to workloads. Autoscaling: Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-06

- [ ] **Question:** Compare Autoscaling with Terraform state. When would each change your design?
> **Covered in:** [Pulumi — Configuration](09-iac-delivery/02-pulumi/README.md#configuration)

**Answer:** **Definitions.** Autoscaling: Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. Terraform state: Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-07

- [ ] **Configuration review:** **Question:** Compare Terraform state with Pulumi Outputs. When would each change your design?
> **Covered in:** [Terraform — Outputs](09-iac-delivery/01-terraform/README.md#outputs)

**Answer:** **Definitions.** Terraform state: Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. Pulumi Outputs: Asynchronous dependency/secret values must stay inside engine-aware transformations. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-08

- [ ] **Question:** Compare Pulumi Outputs with GitOps. When would each change your design?
> **Covered in:** [Pulumi — Outputs](09-iac-delivery/02-pulumi/README.md#outputs)

**Answer:** **Definitions.** Pulumi Outputs: Asynchronous dependency/secret values must stay inside engine-aware transformations. GitOps: Pull reconciliation audits and repairs drift but can amplify bad desired state. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-09

- [ ] **Question:** Compare GitOps with Cloud economics. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** GitOps: Pull reconciliation audits and repairs drift but can amplify bad desired state. Cloud economics: Fixed capacity, work units, storage, network and telemetry combine into unit cost. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MN-10

- [ ] **Configuration review:** **Question:** Compare Cloud economics with Cloud account/project. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Cloud economics: Fixed capacity, work units, storage, network and telemetry combine into unit cost. Cloud account/project: Ownership, quota, billing and policy boundary reduces blast radius. **Mechanism.** In Cloud, Kubernetes and Infrastructure as Code, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Cloud, Kubernetes and Infrastructure as Code design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around Cloud account/project; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get pods,nodes -A -o wide` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Ownership, quota, billing and policy boundary reduces blast radius. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-02

- [ ] **Question:** Production is degraded around Workload identity; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform plan -out=tfplan` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Short-lived federated runtime credentials replace static cloud keys. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Kubernetes reconciliation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Packaging Extensions — Reconciliation](06-kubernetes/07-packaging-extensions/README.md#reconciliation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `pulumi preview --diff` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** API/controllers/scheduler/kubelet converge desired state through plugins/runtime. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-04

- [ ] **Question:** Production is degraded around Kubernetes networking; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `aws sts get-caller-identity` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around Kubernetes storage; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Linux and operating systems — Storage](02-linux/README.md#storage)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get pods,nodes -A -o wide` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** PVC/StorageClass/CSI/topology/snapshot bind state to workloads. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-06

- [ ] **Question:** Production is degraded around Autoscaling; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Gateway Load Balancer — Autoscaling](07-aws/04-load-balancing/03-gwlb/README.md#autoscaling)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform plan -out=tfplan` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Terraform state; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Agents and tool-using AI — State](11-ai-platform/09-agents-and-tool-using-ai/README.md#state)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `pulumi preview --diff` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-08

- [ ] **Question:** Production is degraded around Pulumi Outputs; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Pulumi — Outputs](09-iac-delivery/02-pulumi/README.md#outputs)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `aws sts get-caller-identity` plus recent changes and the user-facing SLI. The command establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Asynchronous dependency/secret values must stay inside engine-aware transformations. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around GitOps; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get pods,nodes -A -o wide` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Pull reconciliation audits and repairs drift but can amplify bad desired state. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-MP-10

- [ ] **Question:** Production is degraded around Cloud economics; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `terraform plan -out=tfplan` plus recent changes and the user-facing SLI. The command checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Fixed capacity, work units, storage, network and telemetry combine into unit cost. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Cloud, Kubernetes and Infrastructure as Code source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-01

- [ ] **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Cloud account/project, Kubernetes networking and Terraform state are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Cloud account/project: Ownership, quota, billing and policy boundary reduces blast radius. Kubernetes networking: Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. Terraform state: Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Workload identity, Kubernetes storage and Pulumi Outputs are first-class requirements.
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Workload identity: Short-lived federated runtime credentials replace static cloud keys. Kubernetes storage: PVC/StorageClass/CSI/topology/snapshot bind state to workloads. Pulumi Outputs: Asynchronous dependency/secret values must stay inside engine-aware transformations. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-03

- [ ] **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Kubernetes reconciliation, Autoscaling and GitOps are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Kubernetes reconciliation: API/controllers/scheduler/kubelet converge desired state through plugins/runtime. Autoscaling: Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. GitOps: Pull reconciliation audits and repairs drift but can amplify bad desired state. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Kubernetes networking, Terraform state and Cloud economics are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Kubernetes networking: Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. Terraform state: Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. Cloud economics: Fixed capacity, work units, storage, network and telemetry combine into unit cost. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-05

- [ ] **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Kubernetes storage, Pulumi Outputs and Cloud account/project are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Kubernetes storage: PVC/StorageClass/CSI/topology/snapshot bind state to workloads. Pulumi Outputs: Asynchronous dependency/secret values must stay inside engine-aware transformations. Cloud account/project: Ownership, quota, billing and policy boundary reduces blast radius. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Autoscaling, GitOps and Workload identity are first-class requirements.
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Autoscaling: Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. GitOps: Pull reconciliation audits and repairs drift but can amplify bad desired state. Workload identity: Short-lived federated runtime credentials replace static cloud keys. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-07

- [ ] **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Terraform state, Cloud economics and Kubernetes reconciliation are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Terraform state: Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. Cloud economics: Fixed capacity, work units, storage, network and telemetry combine into unit cost. Kubernetes reconciliation: API/controllers/scheduler/kubelet converge desired state through plugins/runtime. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Pulumi Outputs, Cloud account/project and Kubernetes networking are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Pulumi Outputs: Asynchronous dependency/secret values must stay inside engine-aware transformations. Cloud account/project: Ownership, quota, billing and policy boundary reduces blast radius. Kubernetes networking: Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-09

- [ ] **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where GitOps, Workload identity and Kubernetes storage are first-class requirements.
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** GitOps: Pull reconciliation audits and repairs drift but can amplify bad desired state. Workload identity: Short-lived federated runtime credentials replace static cloud keys. Kubernetes storage: PVC/StorageClass/CSI/topology/snapshot bind state to workloads. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Cloud, Kubernetes and Infrastructure as Code capability where Cloud economics, Kubernetes reconciliation and Autoscaling are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Cloud economics: Fixed capacity, work units, storage, network and telemetry combine into unit cost. Kubernetes reconciliation: API/controllers/scheduler/kubelet converge desired state through plugins/runtime. Autoscaling: Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Cloud, Kubernetes and Infrastructure as Code options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Cloud account/project. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get pods,nodes -A -o wide` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Ownership, quota, billing and policy boundary reduces blast radius. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Workload identity. How do you lead it end to end?
> **Covered in:** [GCP compute and containers — Workload Identity](08-gcp/04-gcp-compute-and-containers/README.md#workload-identity)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform plan -out=tfplan` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Short-lived federated runtime credentials replace static cloud keys. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Kubernetes reconciliation. How do you lead it end to end?
> **Covered in:** [Packaging Extensions — Reconciliation](06-kubernetes/07-packaging-extensions/README.md#reconciliation)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `pulumi preview --diff` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** API/controllers/scheduler/kubelet converge desired state through plugins/runtime. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Kubernetes networking. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `aws sts get-caller-identity` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Service/EndpointSlice/CNI/DNS/policy/load balancer form the request path. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Kubernetes storage. How do you lead it end to end?
> **Covered in:** [Linux and operating systems — Storage](02-linux/README.md#storage)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get pods,nodes -A -o wide` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** PVC/StorageClass/CSI/topology/snapshot bind state to workloads. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Autoscaling. How do you lead it end to end?
> **Covered in:** [GCP compute and containers — Autoscaling](08-gcp/04-gcp-compute-and-containers/README.md#autoscaling)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform plan -out=tfplan` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Demand metric scales replicas and unschedulable requests scale infrastructure after cold delay. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Terraform state. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `pulumi preview --diff` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Sensitive mapping from addresses to remote identities makes safe plan/refactor possible. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Pulumi Outputs. How do you lead it end to end?
> **Covered in:** [Pulumi — Outputs](09-iac-delivery/02-pulumi/README.md#outputs)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `aws sts get-caller-identity` as one checkpoint because it establishes which identity and target context subsequent commands will inspect; a correct resource in the wrong account, project or cluster is still the wrong result. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Asynchronous dependency/secret values must stay inside engine-aware transformations. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving GitOps. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get pods,nodes -A -o wide` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Pull reconciliation audits and repairs drift but can amplify bad desired state. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-CLOUD-KUBERNETES-AND-INFRASTRUCTURE-AS-CO-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Cloud economics. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `terraform plan -out=tfplan` as one checkpoint because it checks configuration or previews the dependency-aware state transition before any apply; the preview must be reviewed for replacement, deletion, identity and cost. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Fixed capacity, work units, storage, network and telemetry combine into unit cost. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Cloud, Kubernetes and Infrastructure as Code from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

# Senior AI platform engineering — interview questions and answers

> Study first: [README.md](README.md) · Minimum: 20 answered questions per level, split into normal and procedural. Code/CLI prompts are marked **Code**.

## Junior — normal conceptual and code questions

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-01

- [ ] **Question:** What is GPU compatibility chain, and why does it matter in Senior AI platform engineering?
> **Covered in:** [Leadership and behavioural questions — This gives us 66 major branches and several hundred true study leaves. It is broad enough for a conventional Senior DevOps interview while also covering the specialized AI Platform, GPU, LLMOps and RAG responsibilities descried in the job post.](13-scenarios/06-leadership-and-behavioural-questions/README.md#this-gives-us-66-major-branches-and-several-hundred-true-study-leaves-it-is-broad-enough-for-a-conventional-senior-devops-interview-while-also-covering-the-specialized-ai-platform-gpu-llmops-and-rag-responsibilities-descried-in-the-job-post)

**Answer:** **Definition.** Hardware, driver, runtime, device allocation, framework and model must be qualified together. **Mechanism.** Work enters a runnable or pending state, eligibility filters remove incompatible placements, scoring or queue policy selects a worker, and a runtime creates the process and accounts for CPU, memory, devices and time. Autoscaling observes demand, requests more replicas or machines, and waits through provisioning and warm-up before capacity becomes useful. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence connects queue delay, placement, utilization, throttling, memory/device pressure, runtime readiness and successful work. Fragmentation, inaccurate requests, quota, topology, cold starts and a saturated downstream service can leave capacity apparently free while latency and pending work grow. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which GPU compatibility chain matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Senior AI platform engineering journey rather than an administrative green status alone.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-02

- [ ] **Question:** What is Inference queue, and why does it matter in Senior AI platform engineering?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. **Mechanism.** A producer serializes and publishes a message, the service stores or routes it, a consumer receives it under a delivery contract, and acknowledgement advances or removes the item. At-least-once systems can redeliver after an unknown outcome, so stable operation identifiers and idempotent state changes are part of correctness. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes publish and consume rates, queue depth and age, processing latency, retry/dead-letter counts and end-to-end business completion. Poison messages, hot partitions, backlog growth, duplicate effects and retry storms are common; adding consumers helps only when the downstream dependency and partition model can absorb them. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Inference queue matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Senior AI platform engineering journey rather than an administrative green status alone.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-03

- [ ] **Question:** What is Model release, and why does it matter in Senior AI platform engineering?
> **Covered in:** [LLM gateway — Model](11-ai-platform/05-llm-gateway/README.md#model)

**Answer:** **Definition.** Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Model release matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Senior AI platform engineering journey rather than an administrative green status alone.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-04

- [ ] **Question:** What is LLM gateway, and why does it matter in Senior AI platform engineering?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Definition.** Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. **Mechanism.** A real request crosses several independent states: name resolution returns an address, the source selects a route and source address, link or overlay forwarding reaches the next hop, stateful or stateless policy evaluates the flow, transport establishes communication, and TLS/application protocols negotiate their own contract. The return path must also work. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence progresses layer by layer: correct name/address, expected route, permitted flow, listening endpoint, successful handshake and valid application response. Timeouts, refusals, resets and protocol errors mean different layers; packet loss, MTU, asymmetric paths, connection-state exhaustion and proxy timeout mismatch are common production failures. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which LLM gateway matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Senior AI platform engineering journey rather than an administrative green status alone.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-05

- [ ] **Question:** What is RAG authorization, and why does it matter in Senior AI platform engineering?
> **Covered in:** [Leadership and behavioural questions — This gives us 66 major branches and several hundred true study leaves. It is broad enough for a conventional Senior DevOps interview while also covering the specialized AI Platform, GPU, LLMOps and RAG responsibilities descried in the job post.](13-scenarios/06-leadership-and-behavioural-questions/README.md#this-gives-us-66-major-branches-and-several-hundred-true-study-leaves-it-is-broad-enough-for-a-conventional-senior-devops-interview-while-also-covering-the-specialized-ai-platform-gpu-llmops-and-rag-responsibilities-descried-in-the-job-post)

**Answer:** **Definition.** Retrieval must enforce source and tenant access before context reaches a model. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which RAG authorization matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Senior AI platform engineering journey rather than an administrative green status alone.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-06

- [ ] **Question:** What is Evaluation, and why does it matter in Senior AI platform engineering?
> **Covered in:** [Vertex AI and GCP AI platform — Evaluation](08-gcp/07-vertex-ai-and-gcp-ai-platform/README.md#evaluation)

**Answer:** **Definition.** Versioned datasets and calibrated human/automated judges gate quality/safety regressions. **Mechanism.** Inputs are validated and transformed, a versioned model or pipeline performs work, and post-processing and policy produce the exposed result. Offline evaluation compares a candidate with a baseline; shadow or canary traffic supplies production evidence; lineage connects the decision back to exact artifacts and data. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence combines task quality and safety with latency, token or accelerator work, failure rate and unit cost. Training-serving skew, stale or unauthorized retrieval, incompatible runtime/hardware, biased evaluators, prompt injection and silent provider/model changes are core production risks. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Evaluation matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Senior AI platform engineering journey rather than an administrative green status alone.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-07

- [ ] **Question:** What is Agent tool security, and why does it matter in Senior AI platform engineering?
> **Covered in:** [Amazon Bedrock — Agent](07-aws/11-ai-platform/01-bedrock/README.md#agent)

**Answer:** **Definition.** Model output proposes actions; deterministic policy, least privilege and approval authorize them. **Mechanism.** At runtime a caller presents or derives an identity, the enforcement point gathers identity, resource and request context, and applicable rules produce allow or deny. The effective decision is the intersection of all guardrails; encryption protects bytes but does not replace authorization, and audit records explain which decision was made. Its guarantee applies only at its own boundary, so upstream identity, downstream dependencies and asynchronous convergence still matter. **Evidence and failure behavior.** Healthy evidence includes a short-lived attributable identity, narrowly scoped access and an audit event for the intended resource. Failures commonly come from expired credentials, mismatched trust, an overriding deny, wrong resource scope or key/certificate lifecycle problems; widening access may hide the cause while creating a breach path. Compare those signals with desired configuration, workload shape and a known healthy baseline before choosing a mitigation. **Trade-off.** The choice normally balances simplicity, isolation, performance, recovery effort, portability and cost; moving a boundary solves one problem while shifting another to the caller or operator. **Production example.** A team operating a service in which Agent tool security matters records the workload and configuration baseline, injects one bounded failure in a sandbox, alerts on the first user-impact or saturation signal, and verifies recovery through the original Senior AI platform engineering journey rather than an administrative green status alone.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-08

- [ ] **Code:** **Question:** What does `python -m evals.run --manifest release.yaml` help you verify for Senior AI platform engineering?
> **Covered in:** [Pulumi — Python](09-iac-delivery/02-pulumi/README.md#python)

**Answer:** **Purpose.** `python -m evals.run --manifest release.yaml` captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. **Mechanism.** The relevant Senior AI platform engineering concept is AI governance: Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Senior AI platform engineering rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-09

- [ ] **Code:** **Question:** What does `nvidia-smi topo -m` help you verify for Senior AI platform engineering?
> **Covered in:** [Leadership and behavioural questions — Normal conceptual questions with full answers](13-scenarios/06-leadership-and-behavioural-questions/README.md#normal-conceptual-questions-with-full-answers)

**Answer:** **Purpose.** `nvidia-smi topo -m` shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. **Mechanism.** The relevant Senior AI platform engineering concept is AI FinOps: Optimize cost per successful quality-controlled task rather than raw token or GPU hour. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Senior AI platform engineering rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JN-10

- [ ] **Code:** **Question:** What does `kubectl get inferenceservice -A` help you verify for Senior AI platform engineering?
> **Covered in:** [Model serving and inference platforms — InferenceService](11-ai-platform/04-model-serving-and-inference-platforms/README.md#inferenceservice)

**Answer:** **Purpose.** `kubectl get inferenceservice -A` reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. **Mechanism.** The relevant Senior AI platform engineering concept is Deployment portability: SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. The command reads one representation of desired or observed state; it does not by itself prove that every controller, dependency or user path is healthy. **Evidence.** Confirm identity, account/project/cluster, location and exact resource first, then preserve IDs, status/reason fields, timestamps and exit status. Compare them with source-controlled intent and a known healthy baseline, and correlate the nearest event, metric, log, trace or audit record. **Failure and trade-off.** Empty output, permission denial, stale state and an unavailable dependency require different next checks; immediately mutating the target destroys evidence and may widen impact. **Production example.** During a Senior AI platform engineering rollout, an operator uses this check to decide whether the fault is in accepted configuration, reconciliation or the live data path, and verifies the final repair from the original client.

## Junior — procedural and command questions

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-01

- [ ] **Code:** **Question:** A basic GPU compatibility chain check fails. What would you do first using the CLI?
> **Covered in:** [ML/LLM deployment and progressive release — Compatibility](11-ai-platform/08-mlops-and-llmops-lifecycle/13-deployment-progressive-release/README.md#compatibility)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `nvidia-smi topo -m`. This shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Hardware, driver, runtime, device allocation, framework and model must be qualified together. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-02

- [ ] **Question:** A basic Inference queue check fails. What would you do first?
> **Covered in:** [Pulumi — C#](09-iac-delivery/02-pulumi/README.md#c)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get inferenceservice -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-03

- [ ] **Question:** A basic Model release check fails. What would you do first?
> **Covered in:** [AI evaluation infrastructure — Release](11-ai-platform/07-ai-evaluation-infrastructure/README.md#release)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://MODEL/metrics`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-04

- [ ] **Code:** **Question:** A basic LLM gateway check fails. What would you do first using the CLI?
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m evals.run --manifest release.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-05

- [ ] **Question:** A basic RAG authorization check fails. What would you do first?
> **Covered in:** [Security — Authorization](06-kubernetes/05-security/README.md#authorization)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `nvidia-smi topo -m`. This shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Retrieval must enforce source and tenant access before context reaches a model. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-06

- [ ] **Question:** A basic Evaluation check fails. What would you do first?
> **Covered in:** [Vertex AI and GCP AI platform — Evaluation](08-gcp/07-vertex-ai-and-gcp-ai-platform/README.md#evaluation)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get inferenceservice -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Versioned datasets and calibrated human/automated judges gate quality/safety regressions. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-07

- [ ] **Code:** **Question:** A basic Agent tool security check fails. What would you do first using the CLI?
> **Covered in:** [Amazon Bedrock — Agent](07-aws/11-ai-platform/01-bedrock/README.md#agent)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `curl -s http://MODEL/metrics`. This tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Model output proposes actions; deterministic policy, least privilege and approval authorize them. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-08

- [ ] **Question:** A basic AI governance check fails. What would you do first?
> **Covered in:** [LLM release manifests and compatibility — Governance](11-ai-platform/08-mlops-and-llmops-lifecycle/11-llm-release-manifest/README.md#governance)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `python -m evals.run --manifest release.yaml`. This captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-09

- [ ] **Question:** A basic AI FinOps check fails. What would you do first?
> **Covered in:** [Pulumi — C#](09-iac-delivery/02-pulumi/README.md#c)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `nvidia-smi topo -m`. This shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** Optimize cost per successful quality-controlled task rather than raw token or GPU hour. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-JP-10

- [ ] **Code:** **Question:** A basic Deployment portability check fails. What would you do first using the CLI?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Deployment](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#deployment)

**Answer:** **Stabilize and scope.** Confirm the user-visible symptom, start time, affected tenants/locations and current change owner; freeze unrelated changes and avoid retry amplification. **Inspect.** Verify a read-only identity and exact target, then run `kubectl get inferenceservice -A`. This reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Capture the full error, exit status, resource or request ID and timestamp. **Reason about the mechanism.** SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. Compare declared and effective state, then inspect the immediately adjacent identity, control-plane, network, dependency or capacity layer instead of restarting blindly. **Mitigate versus repair.** A mitigation changes one reversible variable—such as pausing traffic or returning to a known compatible revision—without widening trust. The durable repair updates Git/IaC/policy or the owning service and adds a regression test or alert. **Verify.** Repeat the original Senior AI platform engineering client action, confirm correctness plus latency/error/backlog recovery, and record the timeline and prevention.

## Mid-level — normal, comparison and configuration questions

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-01

- [ ] **Question:** Compare GPU compatibility chain with Inference queue. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** GPU compatibility chain: Hardware, driver, runtime, device allocation, framework and model must be qualified together. Inference queue: Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-02

- [ ] **Question:** Compare Inference queue with Model release. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Inference queue: Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. Model release: Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-03

- [ ] **Question:** Compare Model release with LLM gateway. When would each change your design?
> **Covered in:** [LLM gateway — Model](11-ai-platform/05-llm-gateway/README.md#model)

**Answer:** **Definitions.** Model release: Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. LLM gateway: Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-04

- [ ] **Configuration review:** **Question:** Compare LLM gateway with RAG authorization. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** LLM gateway: Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. RAG authorization: Retrieval must enforce source and tenant access before context reaches a model. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-05

- [ ] **Question:** Compare RAG authorization with Evaluation. When would each change your design?
> **Covered in:** [Vertex AI and GCP AI platform — Evaluation](08-gcp/07-vertex-ai-and-gcp-ai-platform/README.md#evaluation)

**Answer:** **Definitions.** RAG authorization: Retrieval must enforce source and tenant access before context reaches a model. Evaluation: Versioned datasets and calibrated human/automated judges gate quality/safety regressions. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-06

- [ ] **Question:** Compare Evaluation with Agent tool security. When would each change your design?
> **Covered in:** [Vertex AI and GCP AI platform — Evaluation](08-gcp/07-vertex-ai-and-gcp-ai-platform/README.md#evaluation)

**Answer:** **Definitions.** Evaluation: Versioned datasets and calibrated human/automated judges gate quality/safety regressions. Agent tool security: Model output proposes actions; deterministic policy, least privilege and approval authorize them. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-07

- [ ] **Configuration review:** **Question:** Compare Agent tool security with AI governance. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** Agent tool security: Model output proposes actions; deterministic policy, least privilege and approval authorize them. AI governance: Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-08

- [ ] **Question:** Compare AI governance with AI FinOps. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** AI governance: Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. AI FinOps: Optimize cost per successful quality-controlled task rather than raw token or GPU hour. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-09

- [ ] **Question:** Compare AI FinOps with Deployment portability. When would each change your design?
> **Covered in:** [Machine-learning fundamentals for platform engineers — Configuration](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#configuration)

**Answer:** **Definitions.** AI FinOps: Optimize cost per successful quality-controlled task rather than raw token or GPU hour. Deployment portability: SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MN-10

- [ ] **Configuration review:** **Question:** Compare Deployment portability with GPU compatibility chain. When would each change your design?
> **Covered in:** [Pulumi — Configuration](09-iac-delivery/02-pulumi/README.md#configuration)

**Answer:** **Definitions.** Deployment portability: SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. GPU compatibility chain: Hardware, driver, runtime, device allocation, framework and model must be qualified together. **Mechanism.** In Senior AI platform engineering, compare where each acts in the request or resource lifecycle, which state it owns, whether its result is synchronous or eventually reconciled, and how callers observe completion. They may be complementary layers rather than substitutes. **Decision criteria.** Select using workload shape and protocol, identity and tenant isolation, failure-domain independence, consistency and recovery contract, latency/throughput limits, team ownership and total cost. **Failure evidence.** State which metric, event, status field or controlled test would distinguish failure of one from the other, and how rollback differs. **Production example.** For a production Senior AI platform engineering design, use the simpler option until measured SLO, isolation, scale or recovery evidence crosses an explicit threshold; document that threshold so the decision can be revisited instead of becoming permanent folklore.

## Mid-level — procedural, CLI and troubleshooting questions

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-01

- [ ] **CLI/debugging:** **Question:** Production is degraded around GPU compatibility chain; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `nvidia-smi topo -m` plus recent changes and the user-facing SLI. The command shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Hardware, driver, runtime, device allocation, framework and model must be qualified together. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-02

- [ ] **Question:** Production is degraded around Inference queue; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get inferenceservice -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-03

- [ ] **CLI/debugging:** **Question:** Production is degraded around Model release; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [AI evaluation infrastructure — Release](11-ai-platform/07-ai-evaluation-infrastructure/README.md#release)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://MODEL/metrics` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-04

- [ ] **Question:** Production is degraded around LLM gateway; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m evals.run --manifest release.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-05

- [ ] **CLI/debugging:** **Question:** Production is degraded around RAG authorization; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Security — Authorization](06-kubernetes/05-security/README.md#authorization)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `nvidia-smi topo -m` plus recent changes and the user-facing SLI. The command shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Retrieval must enforce source and tenant access before context reaches a model. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-06

- [ ] **Question:** Production is degraded around Evaluation; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Vertex AI and GCP AI platform — Evaluation](08-gcp/07-vertex-ai-and-gcp-ai-platform/README.md#evaluation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get inferenceservice -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Versioned datasets and calibrated human/automated judges gate quality/safety regressions. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-07

- [ ] **CLI/debugging:** **Question:** Production is degraded around Agent tool security; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [SRE and reliability engineering — Mitigation](10-operations/02-sre-and-reliability-engineering/README.md#mitigation)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `curl -s http://MODEL/metrics` plus recent changes and the user-facing SLI. The command tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Model output proposes actions; deterministic policy, least privilege and approval authorize them. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-08

- [ ] **Question:** Production is degraded around AI governance; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `python -m evals.run --manifest release.yaml` plus recent changes and the user-facing SLI. The command captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-09

- [ ] **CLI/debugging:** **Question:** Production is degraded around AI FinOps; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Internal developer platform for AI — CLI](12-platform-engineering/08-internal-developer-platform-for-ai/README.md#cli)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `nvidia-smi topo -m` plus recent changes and the user-facing SLI. The command shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** Optimize cost per successful quality-controlled task rather than raw token or GPU hour. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-MP-10

- [ ] **Question:** Production is degraded around Deployment portability; walk through diagnosis, mitigation and durable repair.
> **Covered in:** [Machine-learning fundamentals for platform engineers — Deployment](11-ai-platform/01-machine-learning-fundamentals-for-platform-engineers/README.md#deployment)

**Answer:** **Incident control.** Declare impact, severity, owner, communications cadence and abort criteria; freeze risky changes and preserve evidence. Segment by tenant, location, revision and request type to reduce the search space. **Diagnosis.** Start with `kubectl get inferenceservice -A` plus recent changes and the user-facing SLI. The command reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Walk identity/policy → API/control-plane reconciliation → DNS/network/TLS → runtime/data path → dependency → quota/capacity, stopping when evidence discriminates the first broken transition. **Mechanism.** SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. This matters because mitigation at the wrong layer can leave the cause intact or amplify load. **Mitigation.** Bound retries, shed or drain traffic, pause reconciliation or return to a known compatible revision only when the action is reversible and preserves tenant/data guarantees. Do not treat broad permission or an unqualified failover target as recovery. **Durable repair and proof.** Update the Senior AI platform engineering source of truth, add a regression test and leading alert, verify the original user outcome plus correctness and backlog, and rehearse the revised runbook in a sandbox or game day.

## Senior — architecture, trade-off and code-design questions

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-01

- [ ] **Question:** Design a production Senior AI platform engineering capability where GPU compatibility chain, LLM gateway and Agent tool security are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** GPU compatibility chain: Hardware, driver, runtime, device allocation, framework and model must be qualified together. LLM gateway: Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. Agent tool security: Model output proposes actions; deterministic policy, least privilege and approval authorize them. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-02

- [ ] **Architecture/configuration:** **Question:** Design a production Senior AI platform engineering capability where Inference queue, RAG authorization and AI governance are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Inference queue: Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. RAG authorization: Retrieval must enforce source and tenant access before context reaches a model. AI governance: Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-03

- [ ] **Question:** Design a production Senior AI platform engineering capability where Model release, Evaluation and AI FinOps are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Model release: Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. Evaluation: Versioned datasets and calibrated human/automated judges gate quality/safety regressions. AI FinOps: Optimize cost per successful quality-controlled task rather than raw token or GPU hour. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-04

- [ ] **Architecture/configuration:** **Question:** Design a production Senior AI platform engineering capability where LLM gateway, Agent tool security and Deployment portability are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** LLM gateway: Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. Agent tool security: Model output proposes actions; deterministic policy, least privilege and approval authorize them. Deployment portability: SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-05

- [ ] **Question:** Design a production Senior AI platform engineering capability where RAG authorization, AI governance and GPU compatibility chain are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — This gives us 66 major branches and several hundred true study leaves. It is broad enough for a conventional Senior DevOps interview while also covering the specialized AI Platform, GPU, LLMOps and RAG responsibilities descried in the job post.](13-scenarios/06-leadership-and-behavioural-questions/README.md#this-gives-us-66-major-branches-and-several-hundred-true-study-leaves-it-is-broad-enough-for-a-conventional-senior-devops-interview-while-also-covering-the-specialized-ai-platform-gpu-llmops-and-rag-responsibilities-descried-in-the-job-post)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** RAG authorization: Retrieval must enforce source and tenant access before context reaches a model. AI governance: Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. GPU compatibility chain: Hardware, driver, runtime, device allocation, framework and model must be qualified together. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-06

- [ ] **Architecture/configuration:** **Question:** Design a production Senior AI platform engineering capability where Evaluation, AI FinOps and Inference queue are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Evaluation: Versioned datasets and calibrated human/automated judges gate quality/safety regressions. AI FinOps: Optimize cost per successful quality-controlled task rather than raw token or GPU hour. Inference queue: Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-07

- [ ] **Question:** Design a production Senior AI platform engineering capability where Agent tool security, Deployment portability and Model release are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Agent tool security: Model output proposes actions; deterministic policy, least privilege and approval authorize them. Deployment portability: SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. Model release: Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-08

- [ ] **Architecture/configuration:** **Question:** Design a production Senior AI platform engineering capability where AI governance, GPU compatibility chain and LLM gateway are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** AI governance: Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. GPU compatibility chain: Hardware, driver, runtime, device allocation, framework and model must be qualified together. LLM gateway: Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-09

- [ ] **Question:** Design a production Senior AI platform engineering capability where AI FinOps, Inference queue and RAG authorization are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** AI FinOps: Optimize cost per successful quality-controlled task rather than raw token or GPU hour. Inference queue: Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. RAG authorization: Retrieval must enforce source and tenant access before context reaches a model. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SN-10

- [ ] **Architecture/configuration:** **Question:** Design a production Senior AI platform engineering capability where Deployment portability, Model release and Evaluation are first-class requirements.
> **Covered in:** [Leadership and behavioural questions — Senior architecture and trade-off questions](13-scenarios/06-leadership-and-behavioural-questions/README.md#senior-architecture-and-trade-off-questions)

**Answer:** **Requirements and assumptions.** Clarify tenants, workload distribution and 10× case, latency/quality SLO, data class and residency, RPO/RTO, deployment locations, team skills and budget. Turn each ambiguous requirement into a measurable acceptance or rejection criterion. **Mechanisms.** Deployment portability: SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. Model release: Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. Evaluation: Versioned datasets and calibrated human/automated judges gate quality/safety regressions. Place each on a control/data-plane and synchronous/asynchronous diagram, then identify its source of truth, owner and failure domain. **Security and reliability.** Use short-lived identity, least privilege, explicit tenant boundaries and recoverable encryption keys; design independent capacity and bounded failure propagation rather than assuming replica count equals availability. **Delivery and operations.** Bind reviewed source to immutable artifacts, qualify with representative tests, release progressively and retain a tested rollback or rebuild path. Instrument user SLO, queue/saturation, audit and cost per successful outcome. **Trade-offs and example.** Compare managed, shared and dedicated Senior AI platform engineering options across isolation, portability, operator load and cost. Pilot the highest-risk dependency, perform a restore/failover test, and define migration and exit triggers before committing the full platform.

## Senior — procedural incident, migration and ownership questions

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-01

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving GPU compatibility chain. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `nvidia-smi topo -m` as one checkpoint because it shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Hardware, driver, runtime, device allocation, framework and model must be qualified together. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-02

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Inference queue. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get inferenceservice -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Token work, KV memory, TTFT and goodput govern admission/batching/autoscaling. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-03

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Model release. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://MODEL/metrics` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Weights, tokenizer, prompt, adapters, runtime, hardware, data/index and evaluator form one lineage. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-04

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving LLM gateway. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m evals.run --manifest release.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Identity, policy, quota, routing, streaming, fallback, metering and audit normalize governed access. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-05

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving RAG authorization. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `nvidia-smi topo -m` as one checkpoint because it shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Retrieval must enforce source and tenant access before context reaches a model. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-06

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Evaluation. How do you lead it end to end?
> **Covered in:** [AI evaluation infrastructure — Ownership](11-ai-platform/07-ai-evaluation-infrastructure/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get inferenceservice -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Versioned datasets and calibrated human/automated judges gate quality/safety regressions. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-07

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Agent tool security. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `curl -s http://MODEL/metrics` as one checkpoint because it tests one part of the end-to-end request path and preserves protocol-level evidence such as resolution, connection, handshake, status or packet direction. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Model output proposes actions; deterministic policy, least privilege and approval authorize them. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-08

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving AI governance. How do you lead it end to end?
> **Covered in:** [AI evaluation infrastructure — Ownership](11-ai-platform/07-ai-evaluation-infrastructure/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `python -m evals.run --manifest release.yaml` as one checkpoint because it captures a read-oriented state snapshot that must be interpreted against a healthy baseline, the exact target and the next adjacent dependency. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Inventory, ownership, risk, data/model/prompt lineage, audit and retirement make controls operable. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-09

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving AI FinOps. How do you lead it end to end?
> **Covered in:** [AI evaluation infrastructure — Ownership](11-ai-platform/07-ai-evaluation-infrastructure/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `nvidia-smi topo -m` as one checkpoint because it shows the host-visible GPU, driver, topology or workload state; it proves hardware visibility but not framework compatibility or useful model goodput. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** Optimize cost per successful quality-controlled task rather than raw token or GPU hour. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

### BRANCH-SENIOR-AI-PLATFORM-ENGINEERING-SP-10

- [ ] **Incident/migration:** **Question:** You own a high-severity failure or migration involving Deployment portability. How do you lead it end to end?
> **Covered in:** [SRE and reliability engineering — Ownership](10-operations/02-sre-and-reliability-engineering/README.md#ownership)

**Answer:** **Lead the work.** Establish incident or change command, impact, decision rights, stakeholder cadence, success measures and abort criteria. Freeze conflicting changes, preserve a timeline and keep one accountable technical owner for each workstream. **Build evidence.** Use `kubectl get inferenceservice -A` as one checkpoint because it reads Kubernetes API desired/status state or recent reconciliation evidence without treating a single `Ready` value as proof of the user journey. Correlate identity, desired revision, events, SLI, dependency state and capacity; never make a destructive move from a single green or red field. **Mechanism and risk.** SaaS, customer cloud, on-prem and air gap require explicit packaging, identity, upgrades and support contracts. Translate that behavior into explicit data, tenant, compatibility and partial-completion risks. **Execute safely.** Rehearse on representative state, protect backups and rollback artifacts, canary one failure domain or tenant, stop at thresholds, and prefer reversible containment. Recover in waves so error, latency, correctness, security and billing evidence can halt expansion. **Close the loop.** Verify Senior AI platform engineering from the user path, reconcile the durable source, publish the timeline and trade-off record or postmortem, assign preventive actions with owners and dates, and prove them in a restore test, game day or migration rehearsal.

## Self-grading

A complete spoken answer names the mechanism, scope/assumptions, one concrete command or configuration field, a failure mode, security/recovery, an SLI and a cost/trade-off. For procedural answers, explicitly separate immediate reversible mitigation from durable source-of-truth repair.

<!-- reading-navigation:start -->
---

**Reading path:** [← Contents](README.md) · [Begin: Interview and role-ownership framework →](00-role-ownership/README.md)

<!-- reading-navigation:end -->
